# トキワ 開発の申し送り(全セッション共通ルール)

このファイルは新しいセッションの開始時に必ず読み込まれる。ここに書いたルールは
今後どのセッションでも守ること。

## ★最重要: 開発モード(使用量=トークンを節約するための運用)

デフォルトは常に **蓄積モード**。ユーザーが「動作確認する」と明示するまで、重い処理はしない。

### 蓄積モード(デフォルト・常にこれ)
- やること: 修正 → **構文チェックのみ**(`node --check` / `python3 -m py_compile`) → コミット → プッシュ
- **やらないこと**:
  - サーバー起動(`python3 server/app.py`)
  - Playwright等のブラウザ動作テスト
  - サンプルデータ再生成(make_sample_data.py) / 埋め込み再生成(embed_fallback.py)
  - **バージョン番号を上げない**(tokiwa-ui.html の TOKIWA_VERSION / docs/CHANGELOG.md)
- プッシュは細かくしてOK(プッシュ自体はトークンをほぼ消費しない)。
  ユーザーは好きなタイミングで pull できる。

### 蓄積モードを抜けるのは「動作確認する」と言われた時だけ
- 抜ける時は必ず **「▶ 蓄積モードを抜けて動作確認します」とアナウンス**してから:
  1. 重い検証(サーバー起動・Playwright実ブラウザ検証など)をまとめて1回
  2. バージョン番号を上げる(TOKIWA_VERSION と CHANGELOG)
  3. 埋め込みフォールバック再生成(embed_fallback.py)が必要なら実施
  4. コミット & プッシュ
- 自分の判断で重い検証が必要と思っても、**勝手にやらず必ず先にユーザーに確認**する。

### モード表示
- 毎回の返答の最後に `[蓄積モード]` または `[動作確認モード]` を表示し、状態を常に見えるようにする。

## その他のプロジェクト運用メモ
- コミット署名: `git -c user.email=noreply@anthropic.com -c user.name=Claude commit ...` で行う。
- 実顧客データ `data/real/` は個人情報。**絶対にコミットしない**(.gitignore済み)。
  画像バイナリ等はこの環境から直接保存できないため、必要ならユーザーにpushしてもらう。
- 診断・分析スクリプトは個人情報(顧客名等)を画面出力しない設計にする。
- バックログ/設計は docs/feedback-backlog.md、docs/roadmap.md に記録する。
- 新機能の実装優先順位(2026-07時点): B-7商品写真 → B-1返品 → B-5ランク更新 →
  B-3マスタ管理 → B-2ポイント編集 → B-4検索分析(+集計対象外フラグ)。詳細は docs/。
