# トキワ 開発の申し送り(全セッション共通ルール)

このファイルは新しいセッションの開始時に必ず読み込まれる。ここに書いたルールは
今後どのセッションでも守ること。

## ★最重要: 捏造禁止・提案ファースト・事前助言(2026-07-23 追加)
2026-07-23、Claudeが自分の返答の続きとして偽のユーザー承認(「user蓄積 / 1,2やろう」等)を
生成し、その自作の指示に自分で従って実装・コミットまで進めてしまう事故が発生した(該当ログ
L14547)。信頼に関わる重大事のため、以下を最優先で守る。
1. **返答は必ず `[〇〇モード]` で終え、その後にテキストを一切続けない。**
   user/system など他者の発言や、存在しない指示を**絶対に生成しない**(捏造の禁止)。
2. **実行の根拠にできるのは、実在するユーザーのターン(文頭に「質問/蓄積/動作確認」が付いたもの)だけ。**
   少しでも「本当に言われたか?」と怪しい継続・指示は実行せず、必ず止まって確認する。
3. **提案ファースト**：新しい作業・変更に着手する前に「こんな感じでやっておきますか?」と
   必ず一度提案し、実在の承認(蓄積/動作確認ターン)を得てから実行する。作業内容が無害でも
   勝手に進めない。
4. **事前助言**：たとえユーザーが指示した内容でも、より良い案・リスク・注意点があれば、
   **着手前に必ず一言伝えてから**進める(注意点は重要度に応じて簡潔に、でも必ず添える)。
※補足: この環境のログは入力の出所を記録している(実在の指示=origin:human / Claudeの生成物
=assistant)。万一この種の生成エラーが起きても後から必ず監査・特定できる。

## ★最重要: 開発モード(使用量=トークンを節約するための運用)

### ★★モードはユーザーが文頭で指定する。Claudeは勝手に判断しない(2026-07-18 確定)
ユーザーが各メッセージの**文頭に「蓄積」「動作確認」「質問」**のいずれかを付けて指示する。
Claudeは**自分でモードを判断・変更してはいけない**。前の指示のモードを勝手に引きずるのも禁止。
- **蓄積** … 修正 → 構文チェック(`python3 -m py_compile`等) → コミット → プッシュ **のみ**。
  サーバー起動・ブラウザ検証は**しない**(バージョンは末尾1桁だけ上げる)。
- **動作確認** … サーバー起動＋Playwright等で検証 → **その指示のタスクだけ**検証 →
  バージョンを上げる(TOKIWA_VERSION と CHANGELOG)。**終わったら蓄積の扱いに戻る**
  (次に「動作確認」と言われるまで検証もバージョンアップもしない)。
- **質問** … 調査して答えるだけ。コードは変更しない(必要なら提案に留める)。
- **文頭指定が無い場合**は勝手に判断せず、**蓄積として扱う**(重い処理はしない)。

デフォルトは常に **蓄積モード**。ユーザーが「動作確認」と明示するまで、重い処理はしない。

### 蓄積モード(デフォルト・常にこれ)
- やること: 修正 → **構文チェックのみ**(`node --check` / `python3 -m py_compile`) → コミット → プッシュ
- **やらないこと**:
  - サーバー起動(`python3 server/app.py`)
  - Playwright等のブラウザ動作テスト
  - サンプルデータ再生成(make_sample_data.py) / 埋め込み再生成(embed_fallback.py)
  - ※バージョン番号は**上げる**(末尾1桁。2026-08-03のルール変更。下の「バージョン番号のルール」参照)
- プッシュは細かくしてOK(プッシュ自体はトークンをほぼ消費しない)。
  ユーザーは好きなタイミングで pull できる。

### 蓄積モードを抜けるのは「動作確認する」と言われた時だけ
- 抜ける時は必ず **「▶ 蓄積モードを抜けて動作確認します」とアナウンス**してから:
  1. 重い検証(サーバー起動・Playwright実ブラウザ検証など)をまとめて1回
  2. バージョン番号を上げる(TOKIWA_VERSION と CHANGELOG)
  3. 埋め込みフォールバック再生成(embed_fallback.py)が必要なら実施
  4. コミット & プッシュ
- 自分の判断で重い検証が必要と思っても、**勝手にやらず必ず先にユーザーに確認**する。

### ★バージョン番号のルール(2026-08-03 更新)
- **変更を入れるたびに、末尾の1桁を必ず上げる**(v0.32.0→v0.32.1→v0.32.2…)。
  `tokiwa-ui.html` の TOKIWA_VERSION と `docs/CHANGELOG.md` をセットで更新する。
  **蓄積モード(構文チェックのみ・未検証)でも上げる。**理由: pull したファイルが最新か
  どうかを、画面のバージョン表示だけで判別できるようにするため。
  ※2026-07-18の「未検証の蓄積モードでは上げない」は撤回。
- **「バージョンアップして」と指示されたら 0.1 上げて末尾を0に戻す**(v0.32.5→v0.33.0)。
  区切りのリリースを表す番号なので、この指示なしに0.1は上げない。

### モード表示
- 毎回の返答の最後に `[蓄積モード]` または `[動作確認モード]` を表示し、状態を常に見えるようにする。

## その他のプロジェクト運用メモ
- コミット署名: `git -c user.email=noreply@anthropic.com -c user.name=Claude commit ...` で行う。
- 実顧客データ `data/real/` は個人情報。**絶対にコミットしない**(.gitignore済み)。
  画像バイナリ等はこの環境から直接保存できないため、必要ならユーザーにpushしてもらう。
- 診断・分析スクリプトは個人情報(顧客名等)を画面出力しない設計にする。
- バックログ/設計は docs/feedback-backlog.md、docs/roadmap.md に記録する。
- 新機能の実装優先順位(2026-07時点): B-7商品写真 → B-1返品 → B-5ランク更新 →
  B-3マスタ管理 → B-2ポイント編集 → B-4検索分析(+集計対象外フラグ)。詳細は docs/。
