# 実データ確認フィードバック・機能要望バックログ(2026-07-15)

実データ移行後の表示確認で出た指摘と、新機能の要望リスト。
「✅対応済み」はコミット済み(要: 再取込 or 画面リロード)。「⏳」は未対応。

## A. 表示バグ・移行の取りこぼし(すぐ直す系)
- ✅ **ホーム右上の日付が固定** → システム日付を表示し自動更新(topbar-date)
- ✅ **レジの購入動機・購入場所** → 要望により非表示(HTML内にdisplay:noneで温存。復活可)
- ✅ **顧客詳細ポイントの「区分」列** → 意味不明のため削除
- ✅ **顧客詳細ポイントの残高が出ない** → 台帳(d_point)に無い顧客は履歴の最終残(連番最大)で補完
- ✅ **ポイント履歴の日付が1種類しかない** → 発行日付(dathakko)が空なら処理日(datinpdate)で補完
- ✅ **処方箋一覧にレンズ・フレームが入っていない** → CSVは商品キーのみ保持のため、
      商品台帳から商品名を解決して lens_name/frame_name に格納
- ✅ **在庫一覧に何も出ない** → 状態区分コードを名前に変換(0=受託/1=在庫/3=売上/5=返品)。
      実データでは在庫2,617点・売上20万点・返品7,904点・受託1,924点
- ✅ **支払方法(掛売区分)がコードのまま** → 1=現金/2=掛売/3=クレジット/4=分割/
      5=クレジットローン/6=トレジャリーカード(0=未指定→空欄)。クレジット種別(JCB/VISA等)も変換
- ✅ **処方箋の用途がコードのまま** → 1=常用/2=遠用/3=近用
- ✅ **性別の対応が逆だった(重大)** → 正解は 1=女, 2=男(diag_kubunで発覚)。ピアス(1=有,2=無)も修正

※対応表は diag_kubun.py で実データのxlsxから抽出した正解(2026-07-15確定)。
※import_csv.py 修正分は「再取込(python3 scripts/import_csv.py)」で反映される。

- ✅ **修理お預かり票の印刷で白紙の2ページ目が出る**(2026-07-15確認→修正済み)
      → 印刷時に doc-sheet/label-sheet の最小高さ297mmを外して解消(v0.11.1〜)。
      ※macの印刷ダイアログで「"開始"フィールドの値が範囲外」アラートがループするのは
      macOS側の既知バグ(プリンタ未登録が誘因)。Escで脱出、Macでの確認はPDF保存を使う。

## A-2. 追加フィードバック(2026-07-15 夕)
- ✅ **帳票の発行元が「トキワ 宝飾店」になっていた** → 正式店名
      **「宝石・メガネ・時計 ヤナセ」**に修正(見積書・請求書・修理伝票の共通ヘッダ)。
      STORE_PRINT_NAME 定数に一元化。**ロゴ画像を受領したらここを画像に差し替える**(受領待ち)。
- ✅ **白紙2ページ目** → doc-sheet(3帳票共通)に加え、住所ラベル(label-sheet)にも同じ
      297mmぴったり問題があったため印刷時の高さ指定を解除(ラベル位置は上左基準なので影響なし)。
- ✅ **レジで「保存に失敗しました: Load failed」** → "Load failed"はSafariの通信エラーで、
      ほぼ「サーバー(python3 server/app.py)が止まっている」が原因。画面の操作(閲覧)は
      読み込み済みデータで動き続けるため、保存時に初めて発覚する。エラーメッセージを
      「サーバーに接続できません。ターミナルで python3 server/app.py が動いているか確認」に改善。
      ※サーバーが動いているのに再発する場合はターミナルのエラー出力を確認する。
- ✅ **レジ→他画面→戻る、の不安** → 実はレジの入力内容は画面を移動しても保持される。
      移動時に「レジの入力内容は保持されています(会計は未確定)」のトースト案内を追加。
- ✅ **家族情報の続柄が数字** → m_kubun種別013で対応表確定、名前に変換
      (0=本人/1=父/2=母/3=夫/4=妻/5=長男/6=次男/7=長女/8=次女/9=他)。
- ✅ **店舗ロゴ画像を受領・組込済み**(2026-07-15。王冠+スクロール意匠、YANASE表記、2000×2000px)。
      `assets/logo.png` に配置(ユーザー側でpush)。サーバーに `/assets/*` の静的配信ルートを追加
      (パストラバーサル対策込み)し、見積書・請求書・修理伝票の共通ヘッダ(STORE_HEAD_HTML)で
      サーバー経由時はロゴ画像、埋め込みプレビュー時は文字表記にフォールバック。

## A-6. 追加フィードバック(2026-07-16 その3。在庫画面)
- ✅(蓄積) **在庫サマリのタイルを在庫画面に追加**(ホームのタイル風)。表示項目:
  - 在庫数(state='在庫'の点数のみ。全商品ではない)
  - 在庫合計(上代・税込) = SUM(list_price)
  - 在庫合計(下代・税別) = SUM(cost_price)
  - 粗利率 = (1 − 下代×(1+消費税率) ÷ 上代) × 100。下代を税込換算して上代(税込)と比較。
    消費税率は当面10%固定(将来 zztaxrate 連動を検討)。build_blob で stockStats として算出。
  - ※式の解釈: ユーザー提示「(1-((下代*消費税)/上代)*100)」を、意味の通る
    (1 − (下代×税率倍率)/上代)×100 と解釈。もし想定と違えば要調整。
- ⏳ **在庫画面も権限で触れないようにする**(パート等) → ログイン認証(④)とセット。
  在庫画面へのアクセス自体を止める + 下代合計・粗利率タイルの非表示。
  設計は docs/access-control.md に反映済み(権限マトリクス+実装TODO)。
  ※現状はログイン未実装のため全員閲覧可。ログイン実装時にまとめて制御する。

## A-3. 追加フィードバック(2026-07-15 夜。処方箋画面のスクショから)
- ✅ **重大バグ: 今日の日付が全箇所で2026-07-11に固定** → TODAY_ISO・TODAY(年齢計算用)を
      実際のシステム日付から計算するよう修正。日報照会のデフォルト日付もこれで直る
      (要望「日報照会のデフォルトを上のシステム日付と一緒に」に対応)。
      影響範囲: 日報デフォルト日、年齢計算、処方箋/修理の日付初期値、伝票番号の日付部分。
- ✅ **「2026年度」の表記も年をまたぐと固定値のままだった** → 現在の年から動的に生成
      (CUR_YEAR_LABEL)。顧客一覧の列見出し・顧客詳細のタイルの両方に反映。
- ✅ **「宝飾品が誤登録」の誤検知(重大)** → 原因判明: 判定用の正規表現に短い断片
      (`PT` `SV` の2文字)が含まれており、メガネのフレーム型番等にたまたま含まれる
      文字列で誤検知していた。**紐づく商品が実際に商品台帳でメガネ分類かどうか**で
      判定する方式に変更(import_csv.pyのis_glassフラグを参照)。サーバー側の二重判定
      (db_query.py の `_mark()`)も削除し、判定は取込時の1箇所に一本化。
      合成データで正常系(メガネ→誤登録なし)・異常系(宝飾品→誤登録あり)の両方を検証済み。
- ✅ **家族情報をクリックしたら該当顧客に飛ぶ** → 氏名が完全一致する顧客が1名だけ
      見つかる場合はリンク表示にして開けるように(同姓同名で複数該当する場合は
      誤って別人に飛ぶのを避けるためリンクにしない)。
- ✅ **レジの顧客名にIDが表示され、消しにくく邪魔** → 検索して選択した後は名前のみ表示
      (新規登録から直接レジに戻る場合も同様)。
- ✅ **「種類未定(後日入力)」はもう不要** → 処方箋側で直接入力できるようになったため、
      レジの「番号なしで追加」からレンズを選ぶと即座に金額入力になるよう変更。
      既存の仮登録データ(過去分)の表示・レポートはそのまま残しています(実害なし)。
- ✅ **顧客一覧の「累計購入額」「当年度」をクリックしてソート** → 列見出しクリックで
      昇順/降順を切替(▲▼表示)。
- ✅ **チェック欄付近の誤クリックで詳細画面に飛ぶ** → 行はダブルクリックで詳細を開く
      仕様に変更(シングルクリックでは何も起きない)。
- ✅ **全部チェック/チェッククリアのボタンを追加** → 顧客一覧の上に設置(住所ラベル
      一括印刷用の選択が楽になる)。
- ✅ **処方箋の合計金額が「―」になる処方箋がある件 → 解決(バグではない)**
      `diag_rx_totals.py` の実データ結果: 総件数50,374件中、合計金額あり49,381件(98%)、
      空はわずか993件(2%)。レンズ/フレームの商品キーは50,374件全件に入っており読込は正常。
      993件が空なのは宝飾ナビ運用時にその項目へ金額入力していなかった古い記録と判断。
      対応不要(実際に情報が無いデータなので「―」のままで正しい)。

## A-4. 追加相談(2026-07-16)
- ✅ **ホームの売上等が朝一で前日のデータのまま** → 原因判明: 「今日の日付」を保持する変数が
      ページを開いた瞬間の値のまま固定され、ブラウザを閉じずに日付が変わっても更新されない
      ためだった(v0.11.4でTODAY_ISOを動的化したが、日跨ぎの自動検知はまだ無かった)。
      1分ごとの時計更新のタイミングで日付が変わったことを検知したら、自動でページを
      再読み込みするよう修正(レジ入力中は再読込しない安全策つき)。
- ✅ **売掛入金の記録方法が無かった** → 顧客詳細の「入金管理」タブは残高の**表示のみ**で、
      入金を記録するボタンが存在しなかった(見つからないのは当然でした)。
      売掛明細を一覧化し、各行に「入金する」ボタンを追加。入金額を入力すると
      該当の売掛残高を減らし、入金履歴に記録する(/api/receivable_payment を新設)。
      ブラウザで実際に入金→残高減少→履歴反映→DB永続化まで動作確認済み。
- ✅ **売掛入金をレジ画面からも受けられるように**(2026-07-16 相談。v0.11.6)。
      現金授受→ドロワーはレジの操作なので、レジで完結する方が自然という要望。
      レジで売掛残高のある顧客を選ぶと入金パネル(#pos-receivable)を表示し、
      「入金を受ける」で同じ入金モーダルを開いてその場で入金→残高即時更新→DB保存。
      将来ハードウェア組込(フェーズ①)後は、この現金授受でドロワーが開くようにする。
      ※将来: 同じ来店で「新規購入＋売掛入金」を1会計にまとめる案は未実装(必要になれば検討)。

## A-5. 追加フィードバック(2026-07-16 その2。蓄積モードで実装、動作確認待ち)
- ✅(蓄積) **顧客一覧の「顧客ID」列を「住所」に差し替え** → 検索でIDはあまり使わないため、
      住所1(+住所2、切れなければ)を表示。IDでの検索自体は引き続き可能。
- ✅(蓄積) **顧客詳細の生年月日を和暦で表示(切替式)** → 「和暦で表示/西暦で表示」トグルを追加。
      令和/平成/昭和/大正/明治を境界日まで考慮して変換(明治より前・不正値は西暦のまま)。
      結婚記念日・家族の生年月日も同じトグルに追随。
- ✅(蓄積) **印刷前に必ずプレビューを挟む** → 見積書・請求書・修理伝票・住所ラベルの全印刷で、
      いきなりブラウザ印刷ダイアログに行かず、まず画面内プレビュー(印刷/キャンセル)を表示。
      「この内容で印刷する」で初めて実際の印刷へ。共通の showPrintPreview() に一本化。
- ⏳ **購入・アプローチ履歴で商品をダブルクリック→商品情報(写真・メーカー・ブランド等)を表示** →
      商品詳細の「参照版」を出す。中身は仕入登録画面の参照版でよい。ただし
      **ログインユーザーの権限で仕入額(原価)を出し分ける**必要あり(パート等には非表示)。
      → **B-7(商品写真)** と **アクセス制御(docs/access-control.md)** に依存するため、
        商品詳細画面を作るB-7の中で一緒に実装する。
- ⏳ **在庫一覧からダブルクリックで商品詳細に飛び、修正・削除ができる** →
      これも **B-7** の「商品登録→確認・修正・削除」フローそのもの。B-7でまとめて実装
      (削除時は画像ファイルも一緒に削除する方針は既に記録済み)。
- ❓ **棚卸しモードの使い道とは?**(回答) 棚卸(たなおろし)=**実地在庫の照合作業**。
      定期的に店頭・金庫の現物を1点ずつ確認し、「システム上の在庫」と「実際にある在庫」の
      ズレ(紛失・盗難・登録漏れ・数量違い)を洗い出す作業。宝飾ナビの d_tana(棚卸台帳)が相当。
      使い道: (1)決算期の棚卸資産の確定、(2)紛失・盗難の早期発見(高額商品なので重要)、
      (3)長期在庫の把握。トキワでは B-7(商品)がある程度固まった後、「棚卸モード」として
      商品を1点ずつスキャン/チェックして実在確認し、差異リストを出す機能を検討する
      (優先度は返品・ランク等より後でよい)。実データにも d_tana はあるが現状は移行対象外。

- ⏳ **代引き手数料・収入印紙などレジからマイナスになる支払いの記録方法** →
  現状は「売上(顧客の買い物)」の仕組みしか無く、こうした**顧客と無関係な現金の出入り**を
  記録する場所が無い(宝飾ナビのd_nyushukkin=入出金台帳に相当する機能が未実装)。
  **提案**: 売上とは別の「レジ入出金」記録機能を新設する。
  - 顧客の選択は**不要**(そもそも顧客の買い物ではないため)。
  - 入力項目: 区分(経費・両替・その他等)、金額(マイナス可)、メモ、日付。
  - ホームの「入出金合計」タイルは現状レジ入金(現金・クレジット等)+売掛入金のみの合算で、
    このような単独の現金支出は反映されない。実装時はこのタイルの集計にも含める。
  - 新規テーブル案: `cash_movements(id, kind, category, amount, note, staff_name, occurred_at)`
    customer_id列は持たない。
  - 実装は優先度としてB-9(仕入登録見直し)やB-3(マスタ管理)と近い時期にまとめて検討。

- ⏳ **「ななし」(名無し)顧客運用 → 採用する。ただし「集計対象外」フラグで分析の歪みを防ぐ**
  (2026-07-16 相談で方針確定)。
  背景: 宝飾ナビでは「顧客必須」制約を回避するため、データをくれない一見客を全員
  「ななし」1顧客に集約していた(システム会社いわく「検索に引っかかるように」)。
  副作用として「ななし」1人に大量購入が積み上がり、顧客ランキング・平均購入額・累計等が
  歪む(架空の超VIP客が生まれる)。
  **合意した設計(名前必須のまま + ななし採用 + 永続フラグで除外)**:
  - 顧客マスタに **「集計対象外」フラグ**(仮: customers.exclude_stats)を追加。
    「ななし」や過去のバケツ顧客に1回付けておけば、以後すべての分析から自動除外される
    (レポートごとにチェックを入れ直す方式は付け忘れ・外し忘れが起きるため不採用)。
  - **除外の適用範囲は「顧客を1人の人物として数える集計」だけ**にする:
    | 集計 | ななしを含める? |
    |---|---|
    | 日次売上・現金合計(ホームのタイル/日報) | **含める**(実際に入った現金のため) |
    | 顧客ランキング・平均購入額・顧客の累計 | **除外**(1人の実在客ではないため) |
    → フラグはランキング等の顧客単位集計クエリにのみ `WHERE exclude_stats=0` を足す。
       日次のレジ締め金額(現金合計)には影響させない=現実の金額と一致したままにする。
  - 既存の is_test(検証ペルソナ)フラグとは別概念(is_testは開発用、exclude_statsは運用用)。
  - 実装タイミング: ランキング/平均等の画面は B-4(検索・分析メニュー)で未実装のため、
    除外ロジックは B-4 と一緒に組む。フラグ自体は顧客編集画面に先行追加も可能
    (先に「ななし」等へタグ付けを始められる)。どちらで進めるかは要確認。
  - 補足: customer_id は元々 NULL許容のため、フラグ運用と別に「顧客未選択のまま会計」も
    技術的には可能。ただし店の運用は「ななし」を選ぶ方式で統一する(現状のposError()の
    ブロックは維持でよい)。代引き手数料等レジ外の現金支出は上記「レジ入出金」機能で扱う。

- ⏳ **DBの桁数(列の長さ)設定について** → 結論: **今のままで問題ない**。
  - SQLiteの `TEXT` 型には(MySQL の VARCHAR(n) 等と違って)**文字数の上限が無い**。
    電話番号欄に20桁入れても技術的にはそのまま保存できてしまうのは事実で、バグではなく
    SQLiteの仕様。
  - **ディスク容量の心配は不要**: SQLiteは可変長で保存するため、列に長さ制限を付けても
    付けなくても、実際に入力された文字数分しか容量を使わない(短い電話番号は短い分だけ)。
    実データ62万行超を移行して `db/tokiwa.db` は65MB程度で、この規模ならまったく問題ない。
  - 桁数制限が効くのは「容量の節約」ではなく「入力ミスの早期発見」という別の目的。
    それが必要なら、DB側に制約を足すより**画面側の入力チェック**(例: 電話番号は
    ハイフン抜き最大11桁までなど)で防ぐ方が、エラー処理も分かりやすくシンプル。
  - 備考・メモ等の自由記述欄は、今のまま無制限で問題ない(容量的なデメリットが無いため)。
  - 対応: 今すぐの変更は不要。もし特定の項目(電話番号・郵便番号等)で入力ミス防止の
    チェックを入れたい場合は、個別に要望をもらえれば画面側のバリデーションとして追加する。

## A-8. 家族の追加・紐づけ画面(2026-07-16。A+Bで実装)
- ✅(蓄積) 顧客詳細の家族情報に「＋家族を追加」ボタンを追加。2モード:
  - **A(新規入力)**: 氏名・続柄・性別・生年月日を入力して家族欄に追加。
  - **B(登録済み顧客とリンク)**: 顧客を検索して選び、続柄を指定。相手の氏名等をコピーし、
    **双方向**に登録(相手側にも本人が家族として表示される)。相手側の続柄は空(あとで相手側で設定可)。
- 実装: customer_families に linked_customer_id 列を追加(schema.sql)。
  server: add_family(A/B)、POST /api/family。UI: family-modal + saveFamily。
  家族の氏名リンクは linked_customer_id を優先(無ければ同名一致でフォールバック)。
- ⏳ 未実装(将来): 家族行の**削除・編集**、Bリンクの続柄を相手側からも設定。

## A-7. 追加相談(2026-07-16 その4。不明在庫の削除)
- ✅(蓄積) **不明在庫を商品番号で削除(除外)する仕組み** → 正式移行時にゴミ在庫を消したい要望。
  方式: 商品番号を1行ずつ書いた `data/real/削除商品番号.txt` を用意 →
  import_csv.py がそれを読み、該当商品番号の商品を**取り込まない**(スキップ)。
  何度移行しても自動で除外される(再現性あり・不明在庫がそもそもDBに入らない)。
  - `scripts/check_delete_list.py`(プレビュー): 一致件数・**同じ番号が複数商品に付いていないか**
    (まとめて消える)・**販売履歴のある商品が含まれていないか**(消すと購入履歴の商品名が
    出なくなる)を事前警告。個人情報は出さない。実際には消さない(dry-run)。
  - 注意点: (1)商品番号は一意とは限らない→重複はcheckで表示。(2)販売済み商品は消さない方が安全→
    checkで警告。不明在庫は通常「未販売の在庫」なので該当しないはず。
  - 手順: ①削除商品番号.txt を作る → ②check_delete_list.py でプレビュー →
    ③問題なければ import_csv.py 実行(除外して本番DB再構築)。

## A-9. バーコード照合と重複品番の後始末(2026-07-26。実データ診断の結果)
実データ(212,722件)を `scripts/diag_product_no.py` / `diag_product_growth.py` /
`diag_stock_scan.py` で診断して分かったことと、移行時にやる作業の申し送り。

**分かったこと**
- **5桁の商品番号で足りる**: 5桁の最大値 22418 → 残り約77,581番。直近5年平均 約2,246件/年
  なので**約35年もつ**。6桁化は不要(採番は `LENGTH<=5` 制限で 22419 から続く)。
- **バーコードは品番(先頭5桁)で照合する**: 宝飾ナビのタグは EAN-13 =
  管理番号の先頭10桁(XXXXX-Y-ZZ-WW)＋`00`＋チェックデジット。一方DBの商品番号は
  **5桁の品番のみ**なので、先頭10桁の前方一致では永久に当たらない。→ 先頭5桁で照合する
  (`_resolve_code_conditions`。10桁/13桁一致も保険で残してある)。
- **末尾区画(VVV)がバーコードに入らない**ため、同じ品番の在庫が複数あると
  **バーコードだけでは個体を特定できない**(宝飾ナビのタグ仕様上の限界)。
- **在庫2,617件のうち5桁品番は2,615件(99.9%)**、うち**98.7%は品番が一意**。
  重複は17組34件のみ。内訳は **①品名・上代が空の仮押さえレコード 10組**、
  **②本当に別商品が同じ番号 7組**(`02914 / 02923 / 03251 / 03372 / 13981 / 20396 / 20623`)。
- **受託は `state='受託'` で1,926件**と別枠。在庫台帳(棚卸し対象)には入っていない。
- 在庫のうち**上代0円が208件(7.9%)・下代なしが127件(4.9%)** → 空レコードは現物が無いので
  **棚卸しで永遠に「未確認(差異)」に残る**。本当の紛失が埋もれるため整理が必要。

**★移行時にやること(申し送り)**
1. 先に**不明品(空レコード)を整理**する。どれだけ重複が解消するかは
   `python scripts\diag_stock_scan.py` の**ドライラン**で事前に分かる(削除はしない)。
2. それでも**残った重複品番は「番号の振り直し」で対応**する。**削除しない**。
   - 内部キー `product_key` で売上・在庫・売掛が紐づいているので、
     表示番号(`product_no`)を変えても**データは壊れない**(振り直しは安全)。
   - **新しく登録された側**に `22419` 以降の新番号を振る(古い方のタグは長く貼られているため)。
   - 7件程度なのでタグの貼り替えも現実的。
3. 在庫の**9桁2件**はスキャン不可(メガネの9桁シリーズ)。手入力で対応。

**未決(運用の判断待ち)**
- 空レコード(上代0円・品名なし)を**棚卸しの対象から外すか**
  → 「集計対象外フラグ」で差異に出さないのが現実的(削除はしない)。
- **受託1,926件を棚卸しの対象に含めるか** → 含めないと預かり品の確認ができないが、
  含めると差異が増える。

## A-10. 2026-07-26 の一括要望(13件)の実施状況
✅=蓄積で実装済(未検証) / ⏸=判断待ち

- ✅ 処方箋に**フレーム種類**(セル/メタル/ツーポ/ナイロール)。処方箋側に置いた
  (仕入先マスタは「仕入先の属性」で商品ごとの種類を持てないため)。
- ✅ **現金＋クレジット併用払いの内訳表示**。内訳は元々 `sale_payments` に入っていたので
  表示だけの対応。日報明細に「支払」列、売掛明細に「同時の支払」列、顧客詳細の購入も金額つき。
  `receivables.slip_id` を新設し、売掛から起票元の会計を辿れるようにした。
- ✅ **顧客詳細の支払が「—」**。移行データは掛売区分が空。`pay_fallback()` で
  掛売区分→クレジット種別→現金 の順に補完(日報の表示と統一)。
- ✅ **購入履歴に商品サムネイル**。
  ⏸ **アプローチ履歴のサムネイルは未対応**: `approach_history` に商品への紐づけが無く
  (customer_id/日付/種別/表題/担当のみ)、出す画像を特定できない。
  対応するには「アプローチに商品を紐づける」項目追加が必要 → やるか判断待ち。
- ✅ **在庫品以外の購入明細を顧客詳細から修正**(ダブルクリック→品名・商品情報)。
  ⏸ **金額の修正は入れていない**: 売上集計・累計購入額・日報が動くため、取消→打ち直しを
  推奨。金額も直せるようにするか判断待ち。
- ✅ 宝石の仕入登録に**はめる指(複数選択)・リングサイズ(フリー入力)**。商品の修正でも編集可。
- ✅ 処方箋の**選択項目にカーソルを合わせて数字を打つと手入力に切替**(選択制は維持)。
- ✅ **文字種の自動整形**: 数字欄19か所に `han-num`(全角→半角)、フリガナに `kana`
  (ひらがな→カタカナ)。
  ⚠ **制約**: ブラウザからIME(かな/英数)自体を切り替えることはできない(`ime-mode` は
  Chrome/Edge で無効)。そのため「打った後に正しい文字種へ直す」方式にした。
  数字欄はかな入力のままでも正しく入る。氏名欄で英数のままローマ字になるのは防げない。
- ⏸ **生年月日が曖昧な顧客の運用**: 別途回答済み。方式(不明フラグ/月日のみ/推定年)を
  決めてから実装。
- ✅ 顧客名検索の複数ヒット候補に**担当者**を表示。
- ✅ 写真プールで**必ず大きなプレビュー**を出し、確認後に「この写真を使う」で決定。
- ✅ ラベル印刷に**家族の連名**と**「ご家族様」**の追記。連名は同居家族のみ。

## A-11. ポイント機能 フェーズ1(2026-07-29 蓄積・動作確認待ち)
仕様(2026-07-29 確定): **100円=1pt** / **ポイントを使った会計は付与なし**(宝飾ナビと同運用) /
**カード有効期限=最終購入日+5年** / ルールは設定画面で変更可。

- ✅ ポイント設定マスタ(設定・マスター→ポイント設定): 付与レート・使用時付与なしON/OFF・
  有効期限年数。app_settings 保存(point_settings/save_point_settings、/api/point_settings)。
- ✅ レジ: 会計ごとの**付与倍率**(通常/2倍/3倍/5倍/付与なし)、**加算予定ポイントのプレビュー**、
  支払方法に**「ポイント」**追加(1pt=1円・残高チェックあり・保有ptも表示)。計算はサーバーが正
  (checkout: point_mult / points_used / 残高不足はエラー)。
- ✅ ポイントの**手動修正**(顧客詳細→ポイント→±修正。理由必須で履歴に「手動修正」として残る。
  /api/point_adjust。パート不可)。履歴タブに区分列を追加。
- ✅ 日報: ポイント利用は現金締めから除外し「ポイント利用」タイルで別表示。
- ✅ **機器モードを起動バッチで分離**(要望: 分かりやすく):
  「トキワ起動.bat」「店内共有で起動.bat」=**機器OFF**(レシート・ドロワー・カードに一切送信しない。
  宝飾ナビ稼働中でも全機能テスト可)/「**機器ありで起動.bat**」= `app.py kiki` で**機器ON**。
  画面右上に「🔌機器OFFモード/🖨機器ONモード」ピルを常時表示。設定画面にも現在モードを表示。
- 単体検証済(DBコピー): 既定100円=1pt、2倍/付与なし、使用時付与ゼロ、残高不足拒否、
  手動修正±と理由必須・残高マイナス拒否。**サーバー+ブラウザの動作確認は未実施**。
- 残り(フェーズ2以降): 会計フローへの機器組込(レシート印字+残高印字・ドロワー・カード磁気書換)、
  カード挿入→顧客呼出、券面リライト印字(唯一の未検証)、有効期限の券面印字。

## A-12. ポイント機能 フェーズ2: 会計フローへの機器組込(2026-07-29 蓄積・実機テスト待ち)
- ✅ **機器制御層 `server/devices.py`**: 機器OFF(通常起動)では全関数が何も送信せず skipped を
  返す(単体検証済)。ONは「機器ありで起動.bat」(app.py kiki)のみ。
- ✅ **レシート印字**(/api/receipt_print): 伝票からレシートを組立(店名・明細・税・支払内訳・
  お預かり/釣銭・**使用/加算/残高ポイント**=カード券面の代替となる残高印字)。
  **スプーラー経由RAW印字(win32print)を優先**(双方向サポートONのまま共存可)、
  pywin32が無ければ直接COM(要・双方向OFF)にフォールバック。SJIS/ESC-POS・58mm幅30桁・
  フルカット。ドロワーキック(ESC p)同送。単体検証済(バイト列組立まで。実機出力は未)。
- ✅ **会計→自動印字**: 機器ONモードで会計確定すると自動でレシート＋ドロワー。
  印字失敗でも会計は成立(エラーはトーストで通知・後から /api/receipt_print で再印字可)。
- ✅ **カードで顧客呼出**(方式B・/api/card_read): レジ「💳カード呼出」→挿入待ち→
  トキワ形式("TKW"+顧客ID・逆7bit)なら顧客を自動選択。宝飾ナビ形式(60文字)なら
  カードを装置内に残したまま「紐付けバー」表示→顧客を選んで**紐付け書換**(/api/card_link)
  →以後は挿入だけで自動呼出。中止は排出(/api/card_read_cancel)。
- ✅ **実機テスト成功(2026-07-29 本番機)**: ①テスト会計→CT-S601でレシート印字OK
  (店名・明細・税・現金/預り/釣銭・**加算440pt=100円1ptが正しく動作**・残高印字。
  文字化け・幅崩れなし) ②**カードの紐付け→書換→再挿入で自動呼出までOK**(方式B成立)。
  ※テスト当日、起動時JSエラー(pointRules がD定義前に実行)で画面がモック状態になる
  障害が発生→即修正(eef834e)。蓄積のみで未検証のUIは本番前に必ずブラウザ確認すること。
- レシートの今後の改善候補(宝飾ナビのレシートとの比較。要望が出たら対応):
  TEL・レジ番号の印字 / 伝票バーコード / 明細ごとの表示価格・割引率 / 掛売時の頭金・残高欄。
- 実物カード券面の実測(2026-07-29): 発行日2025.08.10 → 有効期限2030.08.09
  = **書換日+5年-1日**とみられる(設定の「最終購入日+5年」に-1日の丸めがある可能性。
  券面印字の実装時に確認)。
- ⚠ スプーラーRAW印字には `pip install pywin32` が必要(未導入なら直接COMフォールバック=
  双方向サポートOFFが必要になるため、**pywin32導入を推奨**)。
- ❌ **券面リライト印字は未実装**: 印字データ設定(41h)のデータ形式(位置・フォント指定)が
  仕様書の書き起こし(docs/tcp300ii-protocol.md)にまだ無い。本番機の仕様書から41h/46h周りの
  データ形式を追記してから実装する(次フェーズ)。それまで券面の見た目は変わらない
  (残高はレシート印字で代替)。
- メモ: 移行データの旧ポイント履歴は tx_type が「1」等のコードのまま(表示はされる。必要なら後で名称に変換)。

## B. 新機能の要望(実装優先順位を2026-07-15に確定。詳細は docs/roadmap.md 参照)
優先順位: **B-7(商品写真・最優先) → B-1 → B-5 → B-3 → B-2 → B-4 → B-6**
(番号は起票順のため実装順とは一致しない。以下も優先順に並べ替え済み)

### B-7. 商品画像の登録 ★最優先
- 理由(2026-07-15 相談): 実業務のフローは
  「仕入れ→**商品登録(写真を登録)**→登録確認/修正/削除→顧客が購入(写真閲覧)→
  購入履歴でも写真閲覧→返品(在庫に戻った後も写真が見えるか)」という一連の流れで、
  **入口の商品登録でつまずくと後工程を何度もやり直す**ことになる。そのため他の新機能より
  先に、この一連の流れをテストできる段階まで作り込む。
- テストすべき一連の流れ(このまま検証項目にする):
  1. 商品仕入れ(現実世界。トキワはまだ無関係)
  2. 仕入れ伝票と商品が手元に揃う
  3. **商品登録**(仕入れ伝票を見ながら) ← ここで写真を登録
  4. 登録内容の**確認・修正・削除** ← **削除時は写真ファイルも一緒に削除**
     (DB/ディスクを圧迫させない。孤立画像ファイルを残さない)
  5. 顧客を登録し、その商品を購入 ← レジで商品にカーソル/クリックすると写真が見えるか
  6. 顧客詳細の購入履歴でも、カーソル/クリックで写真が見えるか
  7. **返品した時の動作確認** ← 在庫に戻った後も画像が正しく見えるか
- 宝飾ナビ側のデータ: d_item.strpicfilename(商品ごとの画像ファイル名) + d_item_pic(追加画像82件)。
- **画像ファイル本体を受領済み(2026-07-16。実測 8,094枚 / 738MB / 全て.jpg)**。
  置き場所は `data/real/images/`(.gitignore済み=GitHubには上げない・Mac内のみ)。
  `scripts/check_images.py` で照合した結果(2026-07-16):
  - 画像名を持つ商品 8,934件 → 実ファイル一致 **8,916件(99%)**。名前はあるが欠品18件のみ。
  - 追加画像(d_item_pic) 81/81件一致。孤立画像(未参照)約594件=表示不要。
  - **結論: d_item.strpicfilename のファイル名が実ファイルにそのまま対応**。
    B-7では「商品 → data/real/images/<strpicfilename>」で素直に紐づけ可能(ルール調整不要)。
- **画像の扱い方針(2026-07-16 相談。推奨・確定待ち)**:
  - **リサイズ+圧縮**: ブラウザ側(アップロード前にcanvasで長辺1000〜1200px+JPEG q80程度)。
    サーバーに画像処理ライブラリ(Pillow等)を入れずに済む=標準ライブラリのみ構成を維持。
    アップロードも軽くなる。1枚100〜300KB目安。
  - **正方形化**: 保存はクロップしない(宝飾品が切れるため)。元の縦横比のまま縮小して保存し、
    一覧表示だけCSSで正方形サムネにする(object-fit)。どうしても正方形保存が要る場合のみ
    余白パディング方式。
  - **保存先**: 画像は"ファイル"で保存し、DBには"パス/ファイル名"だけ記録(ロゴと同様)。
    DBにバイナリを入れない(肥大化・バックアップ重・配信遅を回避)。
  - 既存の宝飾ナビ画像(8,916枚)は元サイズのまま置き、B-7実装時に表示用の縮小キャッシュを
    生成するかは容量と表示速度を見て判断。
- 実装: products に画像パス(複数可)、商品詳細・在庫一覧・レジ・購入履歴でサムネイル表示、
  削除時は画像ファイルも削除。B-6(修理写真)とアップロード基盤(スマホ撮影対応)を共用。
  配信は /assets/logo.png と同様にサーバーの静的配信(専用ルート)で。容量対策で
  表示用は長辺1200px程度に縮小したキャッシュを作ることも検討。

### B-1. 返品・訂正機能
- レジ画面で日付や顧客名で過去の会計を呼び出し、行単位の削除 or 伝票ごと削除(返品)できる。
- **宝飾ナビの不満**: 訂正のたびに黒/赤の相殺行が積み重なり、3回直すと
  「黒赤黒赤黒赤正」のように非常に見にくい。
- **トキワの方針**: 画面には「最終的に正しいデータ」だけを表示する。
  取消・訂正の経緯は内部に監査用として保持するが、既定では見せない(必要な時だけ展開)。
- **適用範囲は売上が表示される全画面**(2026-07-15 追記):
  - 顧客詳細の購入履歴一覧 ← 明示要望。取消済み・訂正前の行は出さない
  - 日報・売上伝票明細表・CSV出力
  - ホームのタイル集計、購入額ランキング等の分析系
  - 実装方法: 取消・訂正は sales_slips/sale_lines に「無効フラグ(voided)」等を立て、
    表示・集計系のクエリは共通で無効行を除外する(画面ごとの個別対応にしない)。

### B-2. ポイント機能の拡張(要カードリーダー読み書き連携)
- レジ画面: ポイントを**任意で追加**できる。
- ルール: **ポイントで支払った会計にはポイントを付けない**。
- ポイント編集画面(レジとは別): カードを忘れたお客様に、レジを打たずに
  ポイントだけ追加・修正できる画面。カード読み取り・書き込みと連動。

### B-3. マスタ管理画面
- 担当者・仕入先・分類・ブランド・石・地金・動機・場所・地区 等のマスタを管理する画面。
- マスタの種類が多いので、設定画面に全部並べると見にくい。
  **カテゴリ分け+すぐ選べるナビ**(例: 左に種類リスト、右に内容)で設計する。

### B-4. 検索・分析メニューの拡充(左メニューに追加)
- 誕生日検索(誕生月のお客様一覧 → DM・お声がけへ)
- 購入順位(累計・期間別の上位顧客ランキング)
- ランク検索(ランク別の顧客一覧)
- **メガネ処方箋検索**(2026-07-15追記): 左メニューに「メガネ処方箋」を追加し、
  顧客詳細のメガネ処方箋タブと同じ見た目・操作感の一覧を、顧客横断で検索できる画面にする
  (例: 処方日の期間・用途・宝飾誤登録フラグ等で絞り込み)。処方箋タブの描画コードを
  再利用し、行から元の顧客詳細にも遷移できるようにする。
- その他の分析、宛名印刷(住所ラベル)につなげやすい画面設計。
  ※検索結果→チェック→ラベル印刷 の流れは既存のラベル印刷(24面)を再利用。
- **「集計対象外」フラグの実装もここで一緒に行う**(2026-07-16 相談でBを選択=分析画面と
  まとめて実装に確定)。customers.exclude_stats を追加し、顧客編集画面にチェックボックス
  (例: 「集計対象外(ななし等)」)を設置。購入順位・平均購入額・累計等の**顧客単位の集計
  クエリにのみ** `WHERE exclude_stats=0` を適用する。日次売上・現金合計(ホームのタイル/
  日報)には**適用しない**(実際に入った現金のため)。詳細は上記 A-4 の「ななし顧客運用」参照。
- 要望: 預かり品の写真を修理伝票に載せたい。ただし「カメラで撮る→PCに取り込む→添付」は手間。
- **改善案(推奨): スマホから直接登録**。トキワは店内LANのWebアプリなので、
  スマホ/タブレットのブラウザで修理伝票を開き、「写真を追加」ボタン→その場で撮影→
  そのままサーバーに保存(PCへの取り込み作業ゼロ)。
  - 実装: `<input type="file" accept="image/*" capture="environment">` + アップロードAPI
    (/api/upload)。保存先は data/photos/(gitignore)。repairs に写真パス列を追加。
    印刷時は伝票に縮小画像を配置。サイズは長辺1200px程度に自動縮小(容量対策)。
  - 運用: 受付時にレジPCで伝票を作る→スマホで同じ伝票を開いて撮影、または
    スマホだけで受付(店内共有モードで修理登録は許可済み)。
- 将来のカード連携と同様、店内LANが活きる機能。

### B-5. 顧客ランクの更新画面
- 月に1回自動更新 + 手動更新も可能。
- 宝飾ナビの現行基準(スクショより転記):
  - 基準期間: 2020/01/01〜(データ件数 18,520)
  - **合計金額**によるランク(1が最上位):
    | ランク | 合計金額 |
    |---|---|
    | 1 | 1,000,000 〜 99,999,999 |
    | 2 | 500,000 〜 999,999 |
    | 3 | 300,000 〜 499,999 |
    | 4 | 200,000 〜 299,999 |
    | 5 | 100,000 〜 199,999 |
    | 6 | 50,000 〜 99,999 |
    | 7 | 9,800 〜 49,999 |
    | 8 | 1 〜 9,799 |
    | 9 | 0 〜 0 |
  - 回数基準は全て0(未使用)。
  - 全顧客/宝飾顧客/メガネ顧客 の3通りで更新対象を選べる。
- トキワでは: 基準テーブルをマスタ化し、月初に自動更新(手動ボタンも)。更新前後の変化を確認できると良い。

### B-8. 仕入(商品)登録フォームの見直し(2026-07-15相談)
- **画面を2つに分ける**: 「仕入れ登録(宝石)」「仕入れ登録(宝石以外)」のタブ。
  - 宝石以外(メガネ・時計等)は宝飾関連の入力項目(中石・重量・color/clarity/cut等)が
    不要なので、**「ブランド」「品番」「その他」**のみのシンプルな項目にする。
  - 宝石側は現行のフォームのままで概ねOK(要望より)。
- **税区分の明確化+自動計算**: 仕入単価は**税別**入力、上代は**税込**表示。
  掛率の計算例として要望に挙がった数値: 仕入500円(税別)・上代1,100円(税込)・掛率50%。
  →逆算すると「仕入単価を税込換算(500×1.1=550円)してから上代(税込)で割る」で
  550÷1,100=50%になるため、**掛率 = 仕入単価(税込換算) ÷ 上代(税込) × 100** で実装する。
  現行の `updateNpRate()` は税抜同士で割っており、掛率の意味が実務の掛け率と食い違うため
  修正が必要。上代側は税込入力のためそのまま。
- **「折数」を追加**(DBには保存しない画面表示専用の計算項目): 上代(定価)を半額にした値を
  参考表示する(例: 定価が半額の際の金額)。ラベル表記は要望により付けない
  (店独自の使い方のため画面上に説明文言は出さない)。
- 実装時にまとめて着手(仕入れ登録画面の作り込みフェーズで一括対応)。

### B-9. 売上の四分類(催事/店頭/メガネ/時計)の自動振り分け(2026-07-15相談)
- 現状: 売上伝票明細表を見ながら「催事」「店頭(メガネ・時計以外)」「メガネ」「時計」の
  4分類を**目視で手動**集計している。
- **相談内容**: これをシステム化するか、Excelマクロ等の外部ツールに任せるか。
- **回答(推奨)**: **トキワ側で自動化するのが筋が良い**。理由は、この分類に必要な情報
  (商品が「メガネ」か「時計」か、大分類コード、購入場所コード=催事かどうか)は
  **移行時点ですでに構造化されたデータとしてDBに入っている**(products.is_glasses、
  大分類名、sales_slips.place等)。Excelマクロは逆に「テキストから分類を推測」する
  必要があり、トキワの構造化データを使うより不正確・保守しづらくなる。
  実装は日報・売上伝票明細表の集計に「四分類の内訳」を追加するだけで済むはずで、
  既存のB-3(マスタ管理)・B-4(検索・分析メニュー)の作業と一緒に進めるのが効率的。
- 分類ルールのたたき台: 時計=製品コード等が時計系 or 大分類名に「時計」、
  メガネ=is_glasses=1、催事=sales_slips.place(購入場所)が店頭以外、
  店頭(メガネ・時計以外)=残り。実データで大分類・場所コードの実態を見てから確定する。

## C. 実装済み関連メモ
- ロール別アクセス制御(パートに仕入金額を見せない等)は docs/access-control.md 参照。
- 入出金明細の黒赤問題は B-1 の方針(最終状態のみ表示)と同じ考え方で扱う。
