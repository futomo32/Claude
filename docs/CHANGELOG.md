# トキワ 変更履歴

採番規則(2026-08-03 更新): **変更を入れるたびに末尾の1桁を上げる**(v0.32.0→v0.32.1→v0.32.2…)。
どのファイルが最新かを画面のバージョン表示だけで判別できるようにするため、蓄積モードの
未検証の修正でも必ず上げる。**真ん中の桁を上げて末尾を0に戻すのは「動作確認」の時**
(v0.34.28→v0.35.0)。区切りのリリースを表す番号なので蓄積では上げない(2026-08-31 整理)。
現在のバージョンは画面左上(サイドバーのロゴ下)とログイン画面に表示。
ソースの全履歴はGitHubが保持しているため、このファイルは要約のみ。

| バージョン | 内容 |
|---|---|
| **v1.3.7** | **顧客管理に「💳 カード読取→書き込み」を追加**(店の要望)。カードを忘れたお客様が後日持ってきた時に、**顧客を検索せずに**「カードを入れる → お客様を判別 → 券面を最新にして排出」まで**一度の挿入**で終わる。★これができるのは `card_read` が**カードを装置内に保持**するため。続けて `card_issue` を呼べば、そのカードにそのまま書ける。判別できたら**名前と現在のポイントを出して確認**してから書く(違うお客様のカードだった場合に気づけるように)。**★カードが装置に残らないことを最優先で作った。**読取エラー / 宝飾ナビ形式のカード / 判別できない / 顧客が台帳に無い / キャンセル / 書込エラー / 通信失敗 の**8つの抜け道すべてでカードを返す**(残ると次のお客様が使えなくなるため)。**★あわせて不具合を1つ修正**: 確認画面を**×ボタンで閉じた時に後片付け(onCancel)が呼ばれていなかった**。このままだと×で閉じた時だけカードが装置に残る。×を「キャンセル」と同じ扱いにした(枠の外を押した時は元からキャンセル扱いだったので、動きもこれで揃った)。ボタンは**機器ONの時だけ**出す。新品(磁気が空)のカードは判別できないので、その場合は顧客を検索して顧客詳細の「💳 カードに書き込む」を使うよう画面で案内する。テスト表に3項目(キャンセル・×・新品カード)を追加。 |
| **v1.3.6** | **「💳 会員証発行」を「💳 カードに書き込む」に改名**(店の指摘)。「発行」だと**1枚目を作る時だけの操作**に見え、「カードを忘れて後日持ってきたお客様の券面を刷り直す」時に使ってよいか迷う、とのこと。★処理は**新品のカードにも、既に持っているカードの書き直しにも同じ**なので、名前でそれが分かるようにした。ボタン・確認画面・完了メッセージ・失敗メッセージまで文言を揃え、確認画面には**「新しいカードにも、お持ちのカードの書き直しにも使えます」**の一行を足した。ボタンには同じ説明を吹き出し(title)でも出す。サーバー側(`devices.py` / `app.py`)のコメントとエラーメッセージも合わせ、**画面に「会員証」の語は残っていない**。テスト表(`docs/test-checklist.md`)も更新し、**「カードを忘れた人が後日持ってきた時に書き直せる」**という項目を新たに足した(※ポイントの付け忘れがある場合は**先にポイントタブで直してから**書き込む。券面を刷り直すだけでは残高は増えない、という注意も併記)。 |
| **v1.3.5** | **売上日が当日でない会計では、レシートもドロワーも出さない**(店の指定)。店内共有で使っていると、**機器の有無はサーバー(レジPC)側で決まる**ため、別のPCから後日精算(受託の打ち込みなど)をしても**レジPCのプリンタが動いてしまい、誰も受け取らないレシートが出て紙が無駄になる**。過去日の入力はその場で現金をやり取りしないので、**ドロワーを開ける必要も無い**。判定は会計の応答が返す `sold_at` を今日と比べる(画面の入力ではなく**実際に保存された日付**で判断)。出さなかった時は「売上日が ◯◯ のため、レシートとドロワーは出しませんでした。必要なら購入履歴から領収書を発行できます」と画面に出す。**★カードの券面書換だけは過去日でも行う**——止めるのは「紙」と「現金の引き出し」だけで、カードを保持したままだと**読取機に入れっぱなし**になるため(ポイントも実際に増える)。あわせてレジ画面で当日以外の売上日を選んだ時に、**会計する前から**注意書きを出すようにした(機器を使っていない起動では元々レシートが出ないので、この注意も出さない)。※「同じ日の受託を別のPCから打つ場合」は今までどおりレシートが出る。止めたい場合は別途「レシートを出さない」チェックを足す必要がある(未実装・要相談)。 |
| **v1.3.4** | **Mac(開発環境)でダブルクリックして動かせる `.command` を追加**(`ツール/Mac用/`)。仕入先コードの件を開発環境で先に確かめるため。**1_仕入先コードを下読み**(書き換えない) / **2_仕入先コードを入れる**(`yes` の入力が要る) / **トキワ起動**(お試し起動＋ブラウザを開く)の3本。★`fill_supplier_codes.py` 自体はもともとMacでも動く(パスは `os.path.join`、Windows専用の書き方なし)ので**中身は変えていない**。足したのは「ダブルクリックで動かすための入れ物」だけ。**★Windowsのバッチとは文字コードも改行も逆**である点を README に明記した——`.bat` は **cp932 + CRLF**、`.command` は **UTF-8 + LF**。CRLFのまま置くとMacで `bad interpreter` になって動かない。`.command` は**実行権限**も要るので `chmod +x` し、gitにも `100755` で記録した。機械検査(UTF-8 / LF / シェバン / 実行権限 / `cd` の着地点に `server/` と `db/` が実在するか / 参照先のスクリプトが実在するか)と `bash -n` の構文チェックを通した。既存の `.bat` 23本の検査にMac用が混ざらないことも確認。※初回に「開発元を確認できないため開けません」と出たら**右クリック→開く**、`python3` が無ければ `xcode-select --install`、という案内もREADMEに書いた。 |
| **v1.3.3** | **仕入先マスタにコードを表示・コード順に並べ替え。**★調べたところ、**仕入先コードは取り込んでいませんでした**。取込(`import_csv.py`)は `m_siiresaki` を「コード→名前」の変換表としてしか使っておらず、**コードそのものを保存していなかった**(担当者は `staff.staff_code` を持っているので番号順にできていた)。そこで **(1)** `supplier_master.code` 列を追加(`ensure_schema` が自動で作るので**マイグレーション不要**)、**(2)** 画面にコード列を出し、**既定でコード順**に並べる(見出しクリックで昇順⇔降順。数字は数として、それ以外は文字として比べる。**コードが空の仕入先は昇順でも降順でも必ず最後**)、**(3)** コードはその場で**手入力もできる**(全角で打っても半角に直る)。★**同じコードを2つの仕入先に付けようとすると断る**——コード順に並べた時に見分けが付かなくなるため。**(4)** `scripts/fill_supplier_codes.py` を新設。宝飾ナビの `m_siiresaki.csv` と**名前で突き合わせてコードを後から埋める**。**再取込は不要**で、売上・在庫・顧客には一切触らない。既定は**下読み**(何も書き換えない)で、`--apply` を付けた時だけ書き込む。**既にコードが入っている仕入先は上書きしない**(手で直した値を消さないため。付け直すなら `--overwrite`)。**同じコードが2つに付く場合は書き込まず一覧に出す**。書き込む前に自動でバックアップを取る。検算: コードの設定・全角→半角・重複の拒否・空での解除・解除後の再利用・コード順(空は最後)を確認。 |
| **v1.3.2** | **複合検索: チェック欄でも並べ替え＋「この検索結果のチェック/未チェック件数」を表示**(店の要望)。**(1)チェック欄の見出し(☑)を押すと、チェックした人を上にまとめられる。**もう一度押すと未チェックが上。★**選んだ顧客IDは数万件になり得る**ので、`IN (?,?,…)` ではなく**JSONの配列を1つの引数**で渡して `json_each` で照合する(引数の数の上限に当たらないため)。開発DBで、6人だけ選んだ状態・全員(92人)選んだ状態・1人も選んでいない状態のいずれでも動くことを確認。**(2)選択の内訳を出す。**「選択中 N件」は**検索をまたいだ累計**なので、それだけだと「今の条件では何件チェックしたのか」が分からない。横に **「この検索結果(1,832件): チェック 12件 / 未チェック 1,820件」**を出すようにした。担当者を変えながら足していく使い方で、今どこまで拾ったかが分かる。※チェックで並べる時だけ選んだIDを送る(毎回送ると通信が無駄に大きくなるため)。 |
| **v1.3.1** | **担当者の候補が二重に出ていたのを修正(1件=1行に)。**店の指摘「`002` と `002: 簗瀬 恵子` とかぶった内容が出ている」「もっと余白を減らして10ぐらい候補を出せる?」。原因は候補を `<option value="002" label="002: 簗瀬 恵子">` と**値と説明を分けて**書いていたこと。ブラウザはこれを**2行**(値の行と説明の行)で描くため、同じ内容が重なって見え、**一度に出る候補の数も半分**になっていた。値だけを `002: 簗瀬 恵子` と書くようにして1件1行にした(**同じ高さで倍の件数が出る**)。★候補一覧の高さ・余白はブラウザが描くもので、こちらのCSSでは変えられない。詰められるのは「1件あたり何行にするか」だけなので、そこを直した。**(2)欄には名前ごと入るが、範囲の判定は先頭の数字だけを見る。**`_multi_num` / `_multi_dec` が「002: 簗瀬 恵子」「30歳」「-3.00D」のような入力から先頭の数字を読むようにした(数字で始まらないものは今までどおり断る)。画面側も送る前に番号だけにそろえる(二重の備え)。検算: 「101: 三輪 祐加 〜 102: 簗瀬 智宏」と「101〜102」が同じ42件になることを確認。**(3)番号だけを打った時は今までどおり名前を横に出す**が、候補から選んで名前が欄に入っている時は出さない(同じことの二度書きになり、かえって見づらいため)。 |
| **v1.3.0** | **【動作確認】複合検索(新画面)と日報CSVの支払方法の列を実ブラウザで検証。103項目、NG 0件**(新規39 / 従来18 / v1.1系19 / レジ12 / 税15)。`tests/multi_search_smoke.py` を新設。**(1)複合検索の核心を実ブラウザで確認**: 担当者 101〜102(42件)＋104(21件)= **63件**で足し算になる(OR)/ 違う項目を足すと絞られる(AND)/ 「↑と同じ商品」で **25件→19件**と実際に結果が変わる / 買ったものと処方箋はまとめられない / 選ぶ項目の「から〜まで」が一覧の並び順で切り出される / 「から」を選ぶと「まで」も入る / DM可のみが既定でON / 担当者が「番号: 名前」で選べる。**(2)並べ替えがページをまたいで正しい**: 1ページ目の最小(1,356,000)≧2ページ目の最大 で、**全体を並べてから切り出している**ことを確認。ページ送りで同じ人が2回出ないことも確認。**(3)★日報CSVは実際に書き出される中身を検証**(`downloadCsv` を横取り)。店から報告のあった不具合そのものを再現するため、**カードでの売掛入金**をテストデータで作り、`支払=カード` が入ること・**カード・クレジット列**に金額が入ること・**現金列には入らない**こと(レジ締めが狂わない)・**列の合計=金額の合計**を確認。テストデータは最後に削除。**(4)★本番規模での速さを実測**(以前「測らないと分からない」と保留していた件)。顧客25,358人・明細225,537件のコピーを作って計測したところ、**一番遅い形でも0.51秒**(条件なしで累計購入の降順=0.36秒 / 担当者の範囲2本のOR=0.14秒 / 家族を含める=0.24秒)。**作りを変える必要は無いと判断**。**(5)既存の画面に影響が無いことを確認**: 検索・分析のタブの並びが変わっていない(複合検索は末尾に追加)。従来のテスト64項目も全て通過。 |
| **v1.2.7** | **複合検索の一覧に「年齢」「今年の購入金額」を追加、「地区」を「住所」に変更、全列で並べ替え可能に**(店の指定)。一覧は **顧客名 / カナ / 電話 / 年齢 / ランク / 住所 / 今年の購入 / 累計購入 / 担当** の9列。**(1)今年の購入金額は暦年**(1月〜12月)で数え、**見出しに年を出す**(「今年の購入(2026年)」)。あとで見返した時にいつの「今年」か迷わないため。**(2)★並べ替えはサーバー側で行う。**ページ送りがあるので、画面で並べ替えると**今出ている200件の中だけが並び替わり、「1位」が本当の1位でなくなる**。該当者ぜんぶを並べてからページを切り出す。**(3)空欄(未入力)は昇順でも降順でも必ず最後**にする(先頭に来ると一覧が読みにくいため)。同じ値の時は顧客ID順に固定し、**ページ送りで同じ人が2回出たり抜けたりしない**ようにした。**(4)見出しをクリックで並べ替え**、もう一度で昇順⇔降順。別の見出しを押した時の初期方向は、金額・年齢は大きい順、名前・カナ・住所などは五十音順(最初に見たい並びになるように)。**(5)CSVも画面と同じ列・同じ並び順**で書き出す。列の定義は画面の `MS_COLS` とサーバーの `MULTI_SORTS` が対になっているので、列を足す時は両方を直す(両ファイルに明記)。 |
| **v1.2.6** | **複合検索の「DM可のみ」を既定でONに**(店の指定)。この画面は宛名を作るのが主な用途なので、DMを出せない相手(住所の印ＴＴＴ等・DM区分「送らない」)を毎回外す手間をなくす。「条件クリア」を押した後もONに戻る。★あわせて**件数の横に「／ DM可のみで絞っています」と出す**ようにした。既定でONだと、絞っていることに気づかないまま「思ったより件数が少ない」と悩む可能性があるため。DM以外の目的(顧客数の確認など)で使う時はチェックを外す。※既存の「顧客検索・DM抽出」「詳細検索」の設定は変えていない。 |
| **v1.2.5** | **複合検索: 選ぶ項目にも「から〜まで」＋担当者を「番号: 名前」で選べるように。**店から「全ての項目にからまで検索をつけて欲しい」「コードだけだと分からないので担当者『1:〇〇』みたいに出せる?」との要望。**(1)選択式の12項目が「から〜まで」に対応**(地区区分・顧客ランク・性別・ピアス穴・用途(購入動機)・地金・製品(分類)・ブランド・仕入先・転居の印・店舗・用途(レンズ種類))。これで**範囲を指定できない項目は5つだけ**になった(宝石名・品名・備考3・顧客メモ・レンズ/フレーム名。いずれも自由入力の文章なので範囲に意味が無く、部分一致のまま)。**★2:地区区分は部分一致から一覧選択に変えた**(宝飾ナビと同じくマスタの値なので)。**(2)★範囲は「一覧に出ている並び順」で切り出す。**文字コード順で比べると、画面に見えている並びと結果がずれる。切り出しは**画面側で行い、選んだ値そのものを送る**——サーバーで並べ直すと、選択肢が増えていた時に画面で見えていた範囲とずれるため。**(3)1つだけ選ぶ操作を壊さないようにした。**「から」を選ぶと「まで」に同じ値が自動で入る(範囲にしたければ「まで」を選び直す)。これが無いと、今まで「女」を選べば女だけだったのが「女〜(最後まで)」=全員になってしまう。あわせて**「対象: A・B・C(3つ)」と何が対象かを欄の横に出す**ので、範囲が曖昧にならない。**(4)担当者は「番号: 名前」の一覧から選べる。**入力欄に候補(datalist)を付けたので、**番号を打って範囲を素早く指定することも、一覧から名前を見て選ぶこともできる**(プルダウンだけにすると 1〜100 のような範囲指定がやりにくいため両立させた)。**(5)★選択肢が多すぎる項目の安全策。**一覧の上限を1000件にし、**超えた項目は黙って切り詰めず**、文字の一部で探す欄に自動で切り替えて理由も画面に出す。切り詰めると「選べない値がある」ことに気づけず、**DMの宛先が静かに漏れる**ため。**(6)検算**: 分類「ネックレス〜ブレスレット」(3つ)=60件が、1つずつ検索した結果の和と一致。画面側の切り出しも単体で10通り確認(逆に選んだ場合・片側だけ・一覧に無い値・{値,表示}形式など)。 |
| **v1.2.4** | **日報CSVに「支払方法ごとの列」を追加＋売掛入金の支払が抜けていた不具合を修正。**店から「売掛の入金がカードだったのにCSVで区別できない。列が違えばSUMしやすい」との指摘。**(1)★不具合の修正: 売掛入金の支払方法がCSVだけ空欄だった。**`exportDailyCsv` が支払欄を `""` 固定で書いていた(画面を描く `renderDailyReportWith` は `p.method` を正しく入れていたので、**画面には出ているのにCSVだけ抜ける**状態だった)。データは画面にもCSVにも届いていて、書き出す行で捨てていただけ。**(2)支払方法ごとの列を後ろに足した**(現金 / カード・クレジット / PayPay / 振込 / 掛売 / 分割 / その他)。Excelで列を縦に足すだけで方法別の合計が出る。★**カードとクレジットは同じ列にまとめる**(店の指定)——売掛入金は「カード」、レジ売上は「クレジット」と呼び名が違うだけで同じ支払いのため。「支払」列は今までどおり残す(人が読む時に分かりやすいので消さない)。**(3)★併用払いの割り当ては「按分」ではなく「順に埋める」方式。**現金＋クレジットのような会計では、支払は伝票単位・CSVの行は明細単位なので割り当てが要る。按分だと端数の丸めで**列の合計(方法ごとの受取)か行の合計(明細の金額)のどちらかが必ず1円ずれる**。順に埋める方式なら整数の引き算だけで済み、**両方ぴったり合う**。検算: 6万＋4万の明細を現金5万・クレジット5万で払った会計で、行=6万/4万、列=5万/5万がいずれも一致。**(4)★検算で不具合を1つ発見して修正: 「クレジット(VISA)」が「その他」に落ちていた。**列に振り分ける時に**表示用の文字列**(カード会社名の括弧付き)を使っていたため。素の支払方法を使うようにし、あわせて `pay_column` が括弧書き(半角・全角とも)を落としてから照合するようにした。**(5)レジ入出金は「現金」列へ**(レジ箱の現金の出入りなので)。画面の支払欄も「—」から「現金」にそろえた(画面とCSVで表記が違うのは今回直した不具合と同じ種類のため)。**(6)返品**は返金した方法の列にマイナスで入る。※列と支払方法の対応は `db_query.PAY_COLUMNS` / `PAY_COLUMN_OF` が唯一の正で、画面は `daily_sales` の応答で受け取って列を作る。※受取の記録が売上に足りない不整合データは、落とさず差額を「その他」列に出す。 |
| **v1.2.3** | **複合検索(第3段・完成)。**項目を21→**31**に増やし、宝飾ナビの多目的複合検索でやっていたことが一通りできるようになった。**(1)残りの項目10個**: 10転居・DM不可の印(住所の先頭の記号で判定)・12備考3・顧客メモ(どれでも)・**最終購入日**・店舗・**現残ポイント**・23度数(SPH)・用途(レンズ種類)・レンズ/フレーム名・処方日。★最終購入日と処方箋の4項目は、**「詳細検索」を畳むために必要**な項目(詳細検索の last_buy / rx_purpose / rx_lens / rx_from / rx_to に相当)。**(2)★度数(SPH)の空欄よけ。**度数は文字列で入っているため `CAST('' AS REAL)` が **0** になり、そのままだと「-1.00〜1.00」で**度数が空の人が全員入ってしまう**。数字らしい形のものだけを対象にした(`GLOB` で確認)。右目・左目のどちらかが範囲に入れば該当。**(3)★「↑と同じ処方箋」。**第2段の「↑と同じ商品」を処方箋にも広げた。買ったものの条件と処方箋の条件は**まとめられない**(見る表が違うため)ので、同じ種類が2行続いた時だけチェック欄を出す。**(4)家族も含める。**条件に当たった人＋その家族(顧客として登録され、つながっている人)を宛先にできる。★順方向・逆方向の**どちらのつながり方でも**拾う。条件はSQLに1回だけ書く(`WITH base`)。※手入力しただけの家族(顧客未登録)は宛名にできないので増えない。**(5)よく使う条件ボタン**(今月が誕生月 / 3年以上ご無沙汰 / 購入履歴なし / 今月が結婚記念月)。押すと条件が入り、そこから足せる。DMを出す前提なので「DM可のみ」も自動で入る。※「購入履歴なし」は**回数 0〜0**で表す(最終購入日は「買っていない人」を含まないため)。**(6)開発DBでの検算**(読み取りのみ・サーバー未起動): 度数 -3.00〜-2.00(2件)・最終購入日(50件)・ポイント1000以上(70件)・購入履歴なし(14件)がすべてPythonで別計算した期待値と一致。**★「同じ処方箋」と「家族を含む」は元データでは差が出ない**(処方箋を2枚持つのが検証用ペルソナだけ / 家族の相互リンクが0件)ため、**DBをコピーして**2枚目の処方箋と家族リンクを作り、順方向・逆方向の両方で意図どおり増えること、元の人が消えないこと、家族込みでもページ送りと全選択の母集団が一致することを確認した(コピーは削除。db/tokiwa.db は変更していない)。 |
| **v1.2.2** | **複合検索(第2段)— 買ったものの条件と「同じ商品」のまとめ。**項目を10→**21**に増やした。**(1)買ったものの条件11個**: 14買上日・15回数・16金額・18用途(購入動機)・19地金・20宝石名・22製品(分類)＋**ブランド・仕入先・品名・ジャンル**。★後ろの4つは宝飾ナビには無いが**今の「詳細検索」にはある**項目で、これが無いと詳細検索を畳めないため足した。項目は「顧客」「購入」でまとめて出す(21個を平らに並べると選びにくいため)。**(2)★「↑と同じ商品」を行ごとに指定できる。**購入の条件が2行続くとチェック欄が出る。付ければ**同じ1点の商品**が両方を満たすことを求め(プラチナのダイヤ)、付けなければ**別々の買い物でよい**(プラチナを買い、別の機会にダイヤも買った)。**今の詳細検索は画面全体で1つの切り替えしか無いので、これは詳細検索より細かい。**開発DBで検算し、**19件 vs 25件**と実際に結果が変わることを確認した。**(3)ジャンル**(宝石/メガネ/時計/その他)を**「買ったことがある」/「それだけしか買っていない」**で選べる。★**日報の4分類(`sale_kind4`)とは別もの**にした——日報は「どこで売ったか」を優先して催事を切り出すが、顧客抽出では**催事で買った指輪も宝石として数えたい**ため「何を買ったか」で判定する。番号なしの明細(修理・電池)は「その他」にまとめ、「それだけしか」の判定では数えない(指輪を買って電池交換もした人が外れてしまうため)。**(4)回数は伝票の数**(同じ日に2点買っても1回)、**金額は累計**(取消は除く)。どちらも買上日の指定とは連動しない。**(5)開発DBでの検算**(読み取りのみ・サーバー未起動): 同じ商品あり19件/なし25件がPythonで別計算した期待値と一致、分類2行のOR(66件)・回数×分類のAND(18件)・「宝石しか買っていない」61件もすべて期待値と一致。※「↑と同じ商品」は項目を変えて意味を失った時、送信前にチェックを落とす(画面に見えていない指定が結果に効かないように)。 |
| **v1.2.1** | **複合検索(第1段)を追加。**宝飾ナビの「多目的複合検索」に相当する画面を、検索・分析の新しいタブとして足した。★**既存のタブは一切動かしていない**(末尾に追加)ので、今までの操作はそのまま。**(1)条件を「行」で足していく方式。****同じ項目を2回足すと OR**(担当者 1〜100 **または** 200〜250)、**違う項目どうしは AND**。店が宝飾ナビでやっていた「担当者コードを何区間も足してDMの宛先を作る」がそのままできる。既定5行(宝飾ナビの枠数に合わせた)で、**＋でいくらでも足せる**。**(2)第1段の項目は顧客属性の10個**(郵便番号・地区区分・担当者・顧客ランク・性別・年齢・誕生月・結婚記念月・ピアス穴・登録日)。項目番号は宝飾ナビと同じ番号を出す。第2段で購入まわり、第3段で残りを足す。**(3)★項目の定義はサーバーだけが持つ**(`db_query.MULTI_FIELDS`)。画面は `/api/multi_search_fields` で受け取って欄を描くだけなので、項目を足す時に画面を直す必要がない(消費税のように「画面とサーバーで対に持つ」形をあえて作らなかった)。**(4)★選択は検索し直しても消えない。**「選択中 N件」を常に表示し、「この検索結果を全選択」「選択をすべてクリア」を用意した。**担当者を変えて検索し、選択を足していける**(既存の詳細検索は検索のたびに選択が消える)。全選択は**表示中のページだけでなく該当者すべて**が対象。**(5)2000件の打ち切りを撤廃**し、ページ送り(1ページ200件)にした。宝飾ナビは26,849件を相手にしており、DM抽出には2000件では足りないため。**(6)★空欄よけを入れた。**これが無いと「登録日 〜2020-01-01」で**登録日が空の人まで該当**してしまう(空文字はどんな文字列より小さいため)。範囲条件は値がある人だけを対象にする。開発DBで検算し、結婚記念日が空の38名が正しく除外されることを確認した。**(7)DM可の判定を1か所に集約**(`_dm_ok_conds`)。詳細検索と複合検索の両方が呼ぶ。以前は詳細検索にしか無く、片方だけ直す危険があった。※年齢は「今日のYYYYMMDD − 誕生日のYYYYMMDD」で出す(365.25で割ると誕生日の前後で1歳ずれるため)。※担当者番号が数字でない担当者は範囲に入らない(画面に注記)。 |
| **v1.2.0** | **【動作確認】消費税8%(軽減税率)に対応＋仕入登録に個数。**近いうちに8%の商品(飲食料品)を売る予定が出たため。**64項目、NG 0件**(税8%と個数15 / 画面19 / 従来18 / レジの会計12)。**(1)税率と税額の出し方を1か所にまとめた。**以前は `10/110` が**7か所に直書き**されていた(db_query 2・devices 1・画面 4)。`server/taxes.py` と 画面の `TAX` ブロックに集約。★**サーバーとブラウザで対で持っている**ので、片方だけ直さないよう両方に明記した。**(2)商品ごとに税率**(`products.tax_rate`。既定10%、8%を選べる)。仕入先ではなく**商品**に持たせた——同じ仕入先が10%の宝飾品と8%の食品の両方を扱うため。**(3)★会計時に明細へ焼き付ける**(`sale_lines.tax_rate`)。**商品の税率を後から直しても過去の伝票は変わらない**(税務の記録としてそうでなければならない)。テストでも実際に商品の税率を変えて、伝票が変わらないことを確認した。**(4)レシートを税率別に。**8%が混ざる時だけ2段(`10%対象`/`8%対象`)にし、**軽減税率の品名に ※**、末尾に**凡例**を出す(いずれも適格請求書の記載要件)。10%だけの時は今までと同じ見た目。**返品レシート**も同じ形にした。**(5)日報・見積・請求も税率別に。**日報は明細ごとの税率で、見積はレジの明細から、請求書は品目ごとに税率を選べるようにした。★**端数は税率ごとに区切ってから計算する**——まとめて割ると1円ずれる(テストで実測: まとめて6,961円 / 正しくは6,943円)。これも「税率ごとの消費税額」の要件。**(6)仕入登録に個数**(既定1・最大100)。同じ商品を10個仕入れる時に1回で登録でき、**自動採番で連番**になる。★**トキワは「1商品=1点」で在庫を数える**ので、数量の列は持たせず**行を10本作る**(棚卸し・在庫一覧・レジの数え方を変えないため)。**商品番号を手で書いた時は個数1だけ**(番号が重複するため断る)。写真は**登録した全件に同じものを付ける**(プールから選んだ写真は仕組み上1件だけ)。★**実装中に見つけた落とし穴**: JavaScript は**数字に見えるキーを必ず小さい順に並べ替える**ため、`{10:…, 8:…}` を作っても 8% が先に出てしまう。Python は入れた順を保つので、そのままだと**画面とレシートで並びが食い違う**。順序が要る時は `TAX.ordered()` を使うようにし、理由をコメントに残した。 |
| **v1.1.0** | **【動作確認】v1.0.1〜v1.0.9 の9件をまとめて実ブラウザで通した。49項目、NG 0件。**内訳は (1)**今回の変更 19項目**(`tests/ui_smoke_v11.py` を新規作成) (2)**以前からの機能 18項目**(`tests/ui_smoke.py`。壊していないかの確認) (3)**レジの会計 12項目**(`tests/pos_smoke.py`)。あわせて機器・DB側を9項目(レシートの名前とポイント、電話番号の判定)。★**実際にキーを打って確かめたもの**——お預かり欄に `7500` → `7,500`、続けて `0` → `75,000` で**カーソルが末尾に残る**、先頭の次に `8` を挿入 → `785,000` で**カーソルがその場に残る**(v1.0.6/v1.0.9)。★**IMEの変換を再現して確認**——`compositionstart` → 未確定の `yanase` → `compositionend` の順に流し、**変換中は入力欄に手を出さない**こと、確定時にフリガナがカタカナへ直ることを確認(v1.0.3)。★**ポイント印字の自動/手動の切り替え**——既定OFF / カード保持でON / 排出でOFF / **手で触った後はカードで上書きされない**(v1.0.5)。ほか、明細クリアの確認ダイアログと戻り方(会計担当は残る)、ボタンの配置(テンキーは数字クリア・明細クリアはレジ入出金の隣)、商品情報の入力、機器OFFでドロワーを叩かないこと。★**テスト中に見つけた画面の作りをメモ**: お預かり欄は**現金の支払行がある時だけ表示される**ため、先に明細を入れないと触れない(テストもその順に組んだ)。**この環境で試せていないもの**は従来どおり——レシート印字・ドロワー・カード書込の実機、他端末からのアクセス、Windowsのバッチ、顧客メモの枠(開発DBにメモ付き顧客がいない)。 |
| v1.0.9 | **「明細クリア」の置き場所を直した**(店の指摘。テンキーの位置は遠い/あそこのクリアは入力した数字のクリアだけにしたい)。(1)**テンキーの広いボタンは「クリア」= お預かりに打った数字を消す**に変更(`keyClear`)。**明細には触らない**ことを説明にも書いた。※このボタンは v1.0.8 より前は**押しても何も起きない飾り**だった。(2)**「🧹 明細クリア」はレジ入出金の隣へ移した。**「＋商品番号で追加 / ＋番号なしで追加 / ＋受託品 / 💴レジ入出金 / 🧹明細クリア」と、**明細を足す・引くの操作が一列に並ぶ**。動きは v1.0.8 のまま(何行消すか確認してから、明細・支払・お預かりを戻す。お客様と会計担当は残す)。**(3)★あわせて不具合を修正**——お預かり欄には**古い桁区切りの処理が個別に残っており**、v1.0.6 で入れたカーソル保持が**この欄だけ打ち消されていた**(打つたびに末尾へ飛ぶ)。個別の処理をやめ、共通処理に任せるようにした。レジで一番よく打つ欄なので、ここが直っていなかったのは痛い。 |
| v1.0.8 | **(1)★レジに「明細クリア」を付けた**(店の要望。10点打った後で「やっぱり1点だけ」になった時に、行削除を9回押すのが手間)。★**テンキーの「取消」ボタンは、押しても何も起きない飾りだった**(`onclick` が無かった)ので、そこに繋いで**「明細クリア」に名前を変えた**。押すと**何行消すかを見せて確認**し、OKで明細・支払方法・お預かりを初期状態に戻す。**お客様と会計担当は残す**(打ち直す相手は同じ人なので選び直させない)。「在庫は減っていないので消しても在庫に影響はありません」と確認画面に明記した(消していいか迷わせないため)。明細が0行の時は確認を出さずに知らせるだけ。4通り(0行/10行で確認が出る/OKで戻る/やめるで残る)を実際に動かして確認済み。**(2)カード呼出に残り時間を出した**(店の要望)。「💳 カードを入れてください… (あと28秒)」と数える。**待ち時間は30秒のまま**(店の指定)。★**画面の秒数とサーバー側 `devices.card_read(timeout=30.0)` は同じ値でなければならない**ので、そう書いたコメントを両方に近い形で残した(片方だけ変えると、画面が0秒になっても裏で待ち続ける)。通信に失敗した時のお知らせも⚠付き(押すまで消えない)に揃えた。※**「やめる」ボタンは今回は入れていない**——カードリーダーとの通信路を読み取り処理が掴んだままなので、割り込むには機器を扱う部分の作り替えが要る。本番開始直後に機器まわりへ手を入れるのは避けた(バックログ行き)。 |
| v1.0.7 | **レジで商品情報を打てるようにした**(店の要望)。これまで在庫品の商品情報は**読むだけ**(番号なしの明細は「—」)だったが、**その会計の明細に対して自由に打てる**ようにした。石の重さ・サイズ直しの内容など、**その時だけの情報**を売上に残せる。★**商品台帳は書き換えない**——直すのは「この売上の記録」だけで、在庫の商品そのものの情報は変わらない(次に同じ商品を扱う時に前回の入力が残らない)。★**あわせて見つかった不具合を修正**——`info` は**画面から前から送っていたのに、サーバーの `INSERT INTO sale_lines` に入っておらず、打った内容が黙って消えていた。**そのため番号なしの明細(修理・電池交換など)で商品情報を入れても、購入履歴には何も残らなかった。購入履歴・レシート・保証書はいずれも `sale_lines.info` を見て表示する(宝飾ナビからの取込も同じ列に入れている)ので、ここが抜けているとその会計だけ空欄になっていた。在庫品と番号なしの2明細で実際に会計を通し、**両方の商品情報が保存され、購入履歴に出て、商品台帳は変わっていない**ことを確認済み。 |
| v1.0.6 | **金額の入力欄に桁区切りのカンマを入れるようにした**(店の要望。レジの買上金額と同じ見た目に揃える)。対象は**9か所**——お預かり(現金) / 支払方法の金額 / 買上金額 / 売掛の入金額 / 売掛金額 / レジ入出金 / 受託品の金額 / 見積の金額 / 修理の概算金額。★**「押していない数字が入らないこと」を最優先にした**(店の条件)。(1)**打った数字だけから作り直す**——欄の中身から数字を取り出して組み立て直すので、勝手な文字は入らない。全角で打っても半角に揃う。(2)★**カーソルは「左に数字が何個あるか」で数え直して戻す。**文字数で戻すとカンマの分だけズレて、打つたびにカーソルが飛ぶ(これが「押していない数字が入った」ように見える正体)。(3)**IMEで変換している最中は触らない**(v1.0.3の仕組みをそのまま使う)。(4)**レジ入出金だけマイナスを残す**(出金をマイナスで入れる欄のため)。★**あわせて既存の不具合も直した**——レジの買上金額は以前から `el.value = v.toLocaleString()` で毎回書き換えており、**カーソルが末尾へ飛んでいた**(途中の桁を直せない)。整形を共通処理に任せ、二重に書き換えないようにした。※金額を読む側は9か所とも「数字以外を捨ててから数値にする」作りであることを確認済みなので、カンマが入っても金額はズレない。整形とカーソルの動きを**15通り**(順に打つ/途中に挿入/全角/マイナス/マイナス不可の欄/空/0/先頭0)で確認済み。 |
| v1.0.5 | **レシートへのポイント印字をレジ画面で選べるようにした**(店の要望)。**既定は「出さない」**——ポイントは基本的に「カードが無いと使えない」運用なので、カードを入れていない会計にポイントの行が出ても意味がなく、紙も長くなる。★**カードを保持すると自動でONになる**(＝ポイントを使う会計)。カードを排出すると自動でOFFに戻る。カードが無くても出したいレアケースは、チェックを手で入れれば出せる。(1)置き場所は**「ポイント付与」のすぐ下**(同じポイントまわりなので目が迷わない)。★**いま何で決まっているかを右側に小さく出す**——「既定(カードを入れると自動でON)」「カード保持中のため自動でON」「手動でON/OFF」。自動と手動が混ざる仕掛けは、**なぜそうなっているかが見えないと不安になる**ため。(2)★**一度手で触ったら、その会計の間はカードの出し入れで上書きしない**(店員が決めたことを機械が勝手に取り消さない)。会計を確定すると既定(OFF)に戻り、次のお客様に持ち越さない——レシート下部メッセージやポイント倍率と同じ扱い。(3)★**ポイントを実際に使った会計は、OFFでも必ず印字する。**使ったのに紙に残らないと、お客様と残高の行き違いが起きるため(返品明細のポイント調整を必ず出すのと同じ考え方)。この1点だけは指定より優先させている。3通り(既定/カードでON/OFFだが使用あり)でレシートを組み立てて確認済み。※返品明細のポイント調整は従来どおり必ず出す。 |
| v1.0.4 | **★重複チェックが一度も働いていなかったのを修正**(店で同じお客様が二重登録されて発覚)。原因は電話番号の下限を**9桁**にしていたこと。店では市外局番を書かない **`32-0688`** のような書き方が普通で、数字にすると**6桁**しかない。9桁に満たないと `_tel_key()` が `None` を返し、重複チェックは「氏名と電話の両方が一致した時だけ」警告する作りなので、**電話が無い扱いになった時点で何も調べずに素通り**していた。警告を無視したのではなく、**そもそも一度も出ていなかった**。**下限を6桁に下げた**。9桁の条件は「1111111111 のような穴埋めのダミー番号を弾く」ために入れたものだが(2026-08-13)、**ダミーの判定は「数字の種類が2種類以下」の方で足りる**(`000000`・`121212` はそちらで除外される)。※この関数を使う判定はいずれも**氏名と電話の両方の一致**を条件にしているため、桁数を下げても無関係な人が重複扱いになることはない。★**確認中にもう1つ見つけて直した**——`_digits()` が**半角数字しか拾っていなかった**ため、**全角で入力された電話番号(`０９０－１２３４－５６７８`)は「数字なし」**になり、やはり重複チェックの対象外だった。宝飾ナビから引き継いだデータには全角の番号が混じり得るので、幅をそろえてから取り出すようにした。11通りの番号(市内局番だけ/市外局番あり/携帯/ダミー各種/5桁/空/記号/全角)と、5通りの実際の重複判定(同名+6桁一致・全角・同居のご家族・ダミーどうし・市外局番の有無違い)を通して確認済み。※**市外局番あり/なしの突き合わせ**(`0565-32-0688` と `32-0688` を同じ人とみなす)は**まだ入れていない**。必要なら別途。 |
| v1.0.3 | **★フリガナ欄に、まだ確定していない文字が入ってしまう不具合を修正**(店の報告。`ﾔﾔnｱｧsエ` のような文字化けが入った)。原因は**IMEで変換している最中に入力欄を書き換えていた**こと。日本語入力は「ローマ字を打つ → 候補から選ぶ → 確定」と進むが、**その途中の未確定文字にも `input` イベントが飛ぶ**。そこで「ひらがな→カタカナ」「全角数字→半角」の整形をかけて `value` を書き換えていたため、**変換そのものが壊れて、確定前のローマ字がそのまま残っていた**。`compositionstart` / `compositionend` を見て、**変換中は何もせず、確定した時に整える**ように変更(`e.isComposing` も併せて見る。ブラウザによって `input` と `compositionend` の順が違うため両方で押さえる)。変換の途中で欄を離れた場合に旗が立ったままにならないよう `focusout` でも戻す。※この不具合は `class="kana"`(フリガナ)と `class="han-num"`(電話・金額など)の**両方**に影響していた。★**実機での確認が要ります**——IMEの挙動はブラウザとOSごとに違い、この環境では本物の日本語入力を再現できないため、店のPCとiPadで実際に打って確かめてください。 |
| v1.0.2 | **レシートにお客様の名前を印字しないようにした**(店の指定)。レシートは店頭や袋の中で人目に触れるため。**誰の売上かは伝票番号から辿れる**ので、控えとしては困らない。担当者名は残す。★**印字をやめたのは2か所、残したのは2か所**——判断の理由を書いておく。**やめた**: (1)**御計算書**(通常のレシート) (2)**返品明細のお客様控**。**残した**: (3)**領収書の宛名**——宛名の無い領収書は経費精算に使えず、書類として成立しないため(そもそも「ください」と言われた時だけ発行し、宛名も都度入力する)。(4)**返品明細の店控**——サインをもらって**店内で保管する**紙なので、誰の返品かが分かる必要がある。返品は現金が出ていく取引で、あとから「誰に返したか」を追えないと困る。※(5)会員証カードの券面はお客様自身の持ち物なので対象外。ダミーの伝票データで3種類を組み立てて、**御計算書と返品お客様控から消え、領収書の宛名と返品店控には残っている**ことを確認済み。 |
| v1.0.1 | **売掛入金とレジ入出金でドロワーが開かなかったのを修正**(店の指摘)。★バグというより**配線漏れ**——サーバー側の `devices.open_drawer()` は説明文に**「ドロワーだけ開ける(売掛入金の現金授受など)」**と書いてあり、窓口 `/api/drawer_open` も用意してあったのに、**画面から一度も呼んでいなかった**(`drawer_open` の呼び出しが0件)。そのため**会計だけがドロワーを開き、売掛入金とレジ入出金は開かない**という不揃いな状態だった。しかも売掛の画面には**「現金授受でドロワーが開きます」と書いてあった**ので、案内と動作が食い違っていた。(1)共通の `kickDrawer()` を追加し、**売掛入金(現金のときだけ)**と**レジ入出金(入金・出金とも)**から呼ぶ。カード・振込では開けない(閉め忘れの元になるため。会計と同じ扱い)。(2)**機器OFFモードでは呼ばない**(サーバー側でも送信しないが、無駄に叩かない)。(3)失敗しても記録は保存済みなので、**⚠のお知らせ(押すまで消えない)で知らせるだけ**にした。会計のレシート印字と同じ考え方。(4)**お知らせと画面の案内文を機器の状態で出し分ける**ようにした——機器ONなら「ドロワーが開きます」、OFFなら「ドロワーを開けて現金を受け取ってください」。**嘘を書かない**ため。※ドロワーの実物は店のPCでしか試せないので、この環境では呼び出しが入ったことまでの確認。 |
| **v1.0.0** | **★正式版。宝飾ナビからの移行を完了し、2026-09-02 からトキワ単独で運用を開始する。**(2026-09-01 深夜、実データ取込のあと最終テストに合格して付番)。**実データ**: 顧客25,337 / 商品212,209(在庫1,559) / 伝票51,750 / 明細202,749 / ポイント残高4,079 / 処方箋49,991 / 顧客メモ2,283。不明在庫1,041件を除外、売掛は紙が正式版のため空で開始。★**最終テストは実ブラウザで37項目、NG 0件**。(1)**画面の通し確認18項目** … ログイン(パスワード未設定ユーザーの初回設定)/ デモ注意書きが出ないこと / ローマ字検索(megane がカナ検索と同結果)/ 顧客詳細の4列レイアウトと並び順(名前・フリガナ・性別・生年月日 → 電話・携帯・DM送付・登録日 → 郵便番号・住所1・住所2)/ 続柄の裏返し(夫→妻・妻→夫・長男→父/母・その他→家族)/ メーカー絞り込みが6か所すべてに付いていること / 「顧客変更」のラベル / 重複チェックの比較表が一覧より上 / ⚠のお知らせが消えず赤で残り通常のものは消えること / ユーザー並べ替え / 動作ログAPI / JSエラーなし。(2)**レジの会計12項目** … 会計担当のワンタップ → 商品番号で明細追加 → 顧客をカナで選択 → **お預かり不足では会計させない**(正しく止まることも確認)→ 会計確定で**伝票+1・在庫-1**・明細金額・**品名が商品名になっている**(v0.35.7の回帰確認)・日報に出る → **取消で在庫が戻りポイントも戻る**。(3)**顧客変更(付替)7項目** … 下読み / 同じ顧客・存在しない顧客・担当者や理由が空の場合に断ること / 顧客が変わること / ポイント履歴が一緒に動くこと / **両方の顧客にメモが残ること**。★**この確認を再現できるよう `tests/ui_smoke.py` と `tests/pos_smoke.py` を残した**(次の動作確認からはこれを流せばよい)。**この環境で試せていないもの**(店の実機での確認が必要): レシート印字・ドロワー・カード書込などの機器、他端末からのアクセス、Windowsのバッチ、顧客メモの枠(開発DBにメモ付き顧客がいないためスキップ。本番DBには2,283件ある)。 |
| v0.35.12 | **★停止してから起動するたびに「すでに 8760番 が使われています」と誤って警告が出る不具合を修正**(2026-09-01 本番機で発覚)。原因は `netstat -an ^| findstr ":8760"` で見ていたこと。**netstat は LISTENING 以外(TIME_WAIT など)も出す**ため、サーバーを止めた直後は**ブラウザとの接続が TIME_WAIT で数分残り、空いているのに「使用中」**と判定していた(実際に `localhost:8760` は開けない状態だった)。**実際に 127.0.0.1:8760 へ繋いでみる方式**に変更し、同じファイル内の起動確認や `トキワ停止.bat` と同じやり方に揃えた。TIME_WAIT のソケットは接続を受け付けないので誤検知しない。★この誤警告は**「警告が出ても気にせず2を押す」という習慣を作ってしまう**のが一番まずい——本当に二重起動している時に気づけなくなる(v0.34.25でわざわざ入れた警告の意味が無くなる)。あわせて選択肢1の案内文が古かったので修正(「『トキワ サーバー』のウィンドウを全部閉じる」→ 本番運用は画面を持たないので「**トキワ停止.bat で止めてから、もう一度これを実行**」)。バッチ23件を機械検査済み(文字コード・改行・cdの着地点・gotoの飛び先・ブロック内の丸括弧)。 |
| v0.35.11 | **画面上部の「デモデータ(ダミー)を db/tokiwa.db から表示」という注意書きを消した**(店の指摘。本番運用に入って実データを扱うようになったため、**事実と違う上に紛らわしい**)。サーバーに接続している時は注意書きそのものを出さない。※単体HTML(サーバー無し)で開いた時は、本来の「画面モック — 表示中の名前・数値はすべてダミーです」がそのまま残る(そちらは事実なので消さない)。 |
| v0.35.10 | **★中分類を黙って捨てていた不具合を修正(本番取込の検証で判明)。**`verify_import.py` で見たところ、元CSVでは96%入っている中分類が、**取り込み後は78%(165,505/212,209)**しかなかった。原因は `strcbuncode` を `m_cbunrui` で名前に直す時、**マスタに無いコードを黙って捨てて**いたこと(約31,000件)。**v0.34.23で脇石には同じ対策を入れていたのに、中分類には入れ忘れていた。**脇石と同じく**コードのまま残して件数を報告**するようにした(中分類は今のところ画面に出していないので、コードが入っていても表示は壊れない)。★**大分類(`category`)にも同じ経路がある**ことが分かったので、そちらも件数を数えて報告するようにした。ただし**画面の「分類」に出る列なのでコードは入れず空のまま**にしている(コードを出すと見た目が壊れるため。この違いは意図的)。※**2026-09-01に取り込んだDBは古い動作のまま**なので、`products.sub_category` だけを埋め直す小さなスクリプトで直す予定(バックログA-14)。再取込は不要——手入力した売掛やユーザー設定を消さずに直せる。(2)**`verify_import.py` の目安を実態に合わせた。****脇石重量が41%→0.7%に見えるのは正常**で、元CSVの入力率は41%だが**その大半が `0.00`** であり、0は「未入力」として空にしているため(2026-08-29の判断。そのまま入れると「0.00ct」と表示される)。目安を1%に直し、**なぜそうなるかの一文を画面に出す**ようにした。中分類も既知の78%を目安にして、直った時に気づけるようにしてある。ズレの判定も割合で見るように変えた(目安1%の項目に±15ポイントを当てると何を入れても素通りしてしまうため)。 |
| v0.35.9 | **`--no-receivables` を付けて取り込むと、最後の表示だけが落ちる不具合を修正**(2026-09-01の本番取込で発生)。取り込み件数の表示は `{v:,}` と桁区切りを付けていたが、`--no-receivables` の時だけログに**説明文(文字列)**が入るため `ValueError: Cannot specify ',' with 's'` で落ちていた。**数値のときだけ桁区切りを付ける**ように修正。★**データは無事**——`con.commit()` は表示より前に済んでおり、DBは完成していた(実際に products 212,209 / slips 51,750 / lines 202,749 が入った状態で落ちた)。落ちたのは**参照整合性の確認と検証クエリ**で、これらが見られなかった。原因は**このオプションを付けた場合の表示を試していなかった**こと(オプション自体は前から用意していたが、実際に使ったのが今回が初めてだった)。(2)**`scripts/verify_import.py` を新規作成**。取込をやり直さずに、後から中身を確認できるようにした。**件数 / 商品の状態別 / 顧客メモの番号ごとの件数 / 2026-09-01に追加した4項目の充足率(目安との比較つき) / 売上明細の品名の内訳 / 参照整合性 / 店舗**を出す。★品名の内訳は **v0.35.7の効き目の確認**にもなる(「商品は消えたが品名は残る」件数が見える)。読み取り専用で、**顧客名などの個人情報は表示しない**(取込の最後に出ていた「購入額 上位3顧客」は顧客名が出るため、この道具には入れていない)。列が無い古いDBでも落ちずに「読めません」と出して続ける。 |
| v0.35.8 | **バックログの整理(記録のみ・動作は変わらない)。**(1)**A-7b「消すのをやめて『不明品』の状態にする」を新規に立てた**(店の提案。★こちらの設計の方が正しく、本来こちらを提案すべきだった)。**見つかった時に1操作で在庫に戻せる/間違えても取り返せる/記録が残る/v0.35.7で直した品名の問題が根本から消える**の4点で優位。**9月1日にやらなかった理由も残した**——`state='在庫'` で絞る箇所がサーバー側だけで約10か所あり(在庫一覧・在庫金額・棚卸のスキャン・棚卸の進捗・メーカー別在庫数・レジの商品ピッカー・検索分析ほか)、**1か所でも漏れると不明品がレジや棚卸に出る**。翌日から本番営業のため、入れる方がリスクが大きいと判断した。★**移行に再取込は不要**(外した約1,041件をCSVから読んで `state='不明'` で追加するだけ。既存の顧客・売上・ポイントには触らない)。元CSVはずっと残す方針なので宝飾ナビの契約が切れても復元できる。(2)**A-7に、その後の改良と2026-09-01の実施結果を追記**(在庫のみに限定/価格・商品キーでの絞り込み/品名の保持、リスト1,051行→1,043番号、**在庫2件の15番号のうち元Excelで2回書かれていた7件は「2個とも不明在庫」と判明**、消える見込み1,041件)。(3)**A-13として `strkokname` の取り違えを記録**。教訓は**「列名の見た目で意味を決めない。値で確かめる」**(過去にも `strdbuncode`↔`strcbuncode` の取り違えがある)。発見の経緯——店の「xlsxが漏れていたら怖い」という相談から列くらべを作り、その結果を追う過程で判明した——も残した。**相談が無ければそのまま本番に入っていた。** |
| v0.35.7 | **★★購入履歴の品名欄に「お客様の名前」が出る不具合を修正(本番取込の直前に発見)。**原因は `d_hanbai.strkokname` を**商品名だと誤解**していたこと。実データで数えたところ **202,749明細のうち170,063件がその伝票の顧客名と一致・商品名との一致は0件**で、この列は**顧客名**だった。トキワは「商品が products に無い明細」の品名としてこの列を使っていたため、**削除リストで消した商品が売れていた明細(実データで84件)や、商品台帳に無い明細の品名欄に、買った本人の名前が表示される**ところだった。画面は `COALESCE(free_name, products.name)` で出しているので、購入履歴・日報・レシート再印字のすべてに出る。(1)**修正は「消す商品の名前だけ控えておく」方式**にした。削除リストで外す時に `商品キー→商品名` を控え、売上明細の `free_name` にはそれを入れる。★これで**在庫は消えるのに購入履歴の品名は残る**——v0.35.0で「売れた商品を巻き添えにしない」ようにした狙いが、ここでようやく完成した。(2)**名前が分からない明細は空にする**(嘘の名前を出さない)。d_hanbai には品名の列自体が無いため、これ以上の手当てはできない。(3)取込ログに**「名前を控えた件数」と「品名が空になる件数」**を出す。(4)`diag_unused_columns.py` の対応表(`strkokname`=品名)も**誤りだったので訂正**し、`make_demo_csv.py` の引数名を `name`→`koname` に変えて同じ誤解を繰り返さないようにした。※デモデータは元から顧客名を入れていたので、デモ自体は正しかった。検証: 「削除対象で売れた履歴のある商品／通常の商品／商品台帳に無い商品」の3明細を作って取込を通し、**1つ目に正しい商品名が入り、2つ目は商品台帳から出て、3つ目は空**になることを確認済み(修正前は1つ目と3つ目に顧客名が入っていた)。★発見の経緯: 店から「xlsxのデータが重要なものだった時に漏れるのが怖い」との相談で v0.35.6 の列くらべを作り、その結果を確認する過程で「消える84件の品名がどう出るか」を追ったところ判明した。 |
| v0.35.6 | **★xlsx(11台帳)にしか無い情報が無いかを調べる道具を作った**(店の懸念「xlsxが重要なものだった時に漏れるのが怖い」)。`scripts/diag_xlsx_vs_csv.py` と `ツール\データの調べもの\台帳の列くらべ.bat`。**これは今まで空いていた穴**——「CSVの方が情報量が多いはず」という前提で移行を進めてきたが、**実測していなかった**(CSV側の取込漏れは `diag_unused_columns.py` で確認済みだったが、xlsx→CSV の漏れは未確認だった)。★**列名ではなく値そのもので突き合わせる。**xlsxは加工済みで、`大分類名` のようにCSV側では別マスタ(m_dbunrui)に入っている列が大量にあるため、列名で比べると見せかけの不一致が何十件も出て本物の漏れが埋もれる。そこで xlsx の各列から実際の値を最大12個ずつ拾い、**CSV114テーブルを1回だけ走査**して同じ値がどこかにあるかを見る(値→列 のハッシュ引きなので1パスで済む)。結果は **○対応あり / ─判定できない(その列が空) / ★見つからない** の3つに分ける。★**限界を隠さず書いた**: (a)計算で作った列(経過月・粗利率)は必ず★になる (b)**2つの列を繋げた列(商品キー `01-104714`)も必ず★になる**——CSVでは店コードと商品IDの2列に分かれているため(トキワも同じように繋いで作っているので取りこぼしではない) (c)和暦/西暦など書き方が違うと見つけられない。だから★は「漏れ確定」ではなく「要確認」。偶然の一致を避けるため、2桁以下の数字など弱い値は照合に使わない。日付・小数・桁区切りは同じ形にそろえて比べる。**表示するのは列名と件数だけ**(値は出さない)。検証: デモのxlsxとCSVは**別々に作った無関係なデータ**でほぼ全部★になるため、**同じ元データからxlsxとCSVを作ったペア**を用意して実行し、共通の列が○、意図的に混ぜた計算列(経過月・粗利率)と合成列(商品キー)だけが★になることを確認した。この落とし穴自体も画面と手順書に明記した(`--demo` は動作確認専用)。結果は `data/real/台帳の列くらべ結果.txt` にも書き出す(`--demo` では**作らない**——`data/demo` はGitHubに載るフォルダのため)。※**急ぎではない**: 宝飾ナビの契約が9月いっぱい残っているので漏れが見つかっても取り直せる。ただし10月に入る前に実施すること、と `docs/go-live-checklist.md` に期限付きで書いた。 |
| v0.35.5 | **削除リストで「商品番号＋仕入価格」と「商品キー」を指定できるようにした**(店の要望。同じ商品番号が使い回されているため)。★**実測すると 1,043番号 → 在庫1,049件**で、差は6件しかない。つまり**「在庫だけ消す」ルール(v0.35.0)でほぼすべて1件に決まっており**、追加指定が要るのはごく少数。そこで**全部の書き方を変えるのではなく、決まらないものだけを見つけて手当てする**方針にした。(1)`check_delete_list.py` に **★「同じ番号で在庫が2件以上ある番号」の一覧**を追加。**そのまま `削除商品番号.txt` に貼れる形**(`104714,72000`)で候補を表示する。ここが0件になれば全部1件に決まっている、という確認になる。(2)リストの書き方を拡張: `104714`(今まで通り)/ `104714,72000`(番号＋仕入価格)/ `01-104714`(商品キーで狙い撃ち)。3列目以降はメモとして無視。**今のファイルはそのまま使える**。(3)読み方を `scripts/_dellist.py` に集約し、`import_csv.py` と `check_delete_list.py` が**同じ判定を共有**するようにした(以前は同じ判定を2か所に書いており「片方だけ直す」事故が起きやすかった)。(4)★**書き方が違う行を黙って捨てない**。「2列目が金額として読めない」「商品キーに価格を付けている」などを行番号付きで警告する。**価格で絞ったのに1件も当たらなかった番号**も、実際の在庫の仕入価格と並べて表示する(価格の打ち間違いで「消したつもりで消えていない」を防ぐ)。(5)★実装中に不具合を発見して修正: **Excelから金額をコピーすると `140,000` と桁区切りのカンマが入る**ため、カンマで欄を割ると `140` と `000` に分かれて**価格が140円と解釈されていた**。3桁ちょうどの数字が数字の欄の後ろに来た場合はつなぎ直すようにした(ただし**1列目には決してつながない**——`123,456` を `123456` に化けさせないため)。実データと同じ形(同一番号で在庫・売上・返品・受託が並ぶ／同一番号で在庫2件が価格違いで並ぶ／商品キーで狙い撃ち／リスト外の在庫)を作って取込まで通し、**在庫の該当分だけが消え、売上・返品・受託とリスト外は残る**ことを確認済み。 |
| v0.35.4 | **紙の売掛をExcelから一括登録できるようにした**(店の要望。9/2からの正式運用で、宝飾ナビの売掛は持ち込まず紙の台帳から入れ直すため。件数が多い見込み)。`scripts/import_receivables.py` と `ツール\売掛の一括入力\` の3本(1_入力用ファイルを作る / 2_下読み_取り込まない / 3_取り込む)。番号順に使う。★**この道具の肝は「速さ」ではなく「取り違えを起こさないこと」**——売掛は直接お金の話で、1件の付け間違いが取り立て漏れか二重請求になる。そのため次の守りを入れた。(1)**顧客が1人に決まらない行は取り込まない**。決め方は ①顧客ID ②顧客名＋電話 ③顧客名だけ、の順で、**複数候補があれば必ず人に返す**(同姓同名に付けてしまわないため。実データにも同姓同名は普通にいる)。決まらなかった行は**理由付きで `data\real\売掛_要確認.csv`** に書き出し、直して流し直せる。(2)**既定は「下読み」**で、`--commit` を付けるまで**DBを一切変更しない**。(3)**合計金額を必ず表示**する——紙の合計と1円単位で突き合わせるため。(4)**二重取り込みを警告**する。同じ顧客・金額・日付が**ファイルの中**にある場合と**DBに既にある**場合の両方(同じファイルを2回流す事故を防ぐ)。(5)`--commit` の前に**DBのバックアップを自動取得**し、全件を**1つのトランザクション**で書く(途中で落ちても中途半端に入らない)。★**買上日が空欄の行は空欄のまま登録する**(今日の日付で埋めない)。紙に日付が無いものを「今日買った」ことにすると経過月も日報も狂うし、**二重取り込みの検知も効かなくなる**(実装中にこの穴を見つけて直した)。入力は**csv でも xlsx でも**読め、桁区切りのカンマ・全角数字・`￥`・「円」・氏名の全角半角スペースの違いは吸収する。日付は `2026/6/15`・`2026-6-15`・`20260615`・Excelの日付セルに対応。テスト用DB(顧客108件・同姓同名7組)で、ID指定/名前だけ/同姓同名＋電話/同姓同名のみ/存在しない顧客/存在しないID/金額が文字/0円/日付不正/日付空欄/空行/ファイル内重複/DB内重複の**13通り**を通し、取込・再実行での重複検知・DBの復元まで確認済み。 |
| v0.35.3 | **★v0.35.1 のフォルダ整理で、移動した15個のバッチが全部動かない状態だった不具合を修正。**バッチは `ツール\分類\` の**2階層下**にあるのに `cd /d "%~dp0.."` と `..` を**1つしか書いていなかった**ため、着地点が `ツール\` 止まりになり、`server\` や `db\` を見失う。**`cd /d "%~dp0..\.."` に修正**(15件すべて)。★v0.35.1 で「機械検査済み」と報告したが、**検査が `..` の有無しか見ておらず、数が足りているかを見ていなかった**のが原因。今回から**「バッチのある場所 + 相対部分」を実際に計算し、その先に `server\` と `db\` が実在するか**まで確かめるようにした(cmd と同じ解決をする)。19件すべて着地点がトキワ本体であることを確認済み。あわせて `ツール/README.md` の直し方の説明も `..\..` に訂正した。**`本番運用で起動.bat`・`トキワ停止.bat`・`バックアップ.bat` はルート直下のままなので影響なし**(この3つは元から正しい)。**あわせて `ツール\データの調べもの\台帳の行数しらべ.bat` を新規作成**(店の要望。9/1の取込の答え合わせ用)。ダブルクリックすると11台帳(xlsx)の**行数だけ**を数える(中身の値は表示しないので実データでも安全)。取込ログの件数と見比べて取りこぼしを確認する。画面に**見比べかたも表示**する——**顧客・商品・売掛・ポイントは一致するはず / 販売台帳と処方箋はズレて正常**(販売はトキワ側で伝票と明細に分けている。処方箋は宝飾品の誤登録を除いている)。★このバッチだけ `openpyxl` が要るため、**実行前に入っているかを確かめ、無ければ `python -m pip install openpyxl` という入れ方を画面に出して止まる**(取込そのものには要らないことも明記)。Python が見つからない場合の案内も他のバッチと同じ形で入れた。文字コード cp932・改行 CRLF・`goto` の飛び先・ブロック内の echo の丸括弧を機械検査済み。`ツール/README.md` と `docs/go-live-checklist.md` にも追記した。 |
| v0.35.2 | **実データの置き場を `data/real/xlsx/` に整理した**(店の指摘: CSVだけフォルダで xlsx はバラ置きになっていて不揃い)。あわせて**xlsx と CSV の関係を各所に書き直した**。宝飾ナビからは2つの形でデータが出るが、**本番の取込は CSV だけ**(`import_csv.py` は `*.csv` しか読まない)。11台帳の xlsx は**まとめ版**で、中分類・脇石・タグ品名・DM備考は**CSVにしか入っていない**。(1)置き場を決める共通部品 `scripts/_paths.py` を新設し、xlsx を読む5本(`diag_real_xlsx` / `diag_kubun` / `diag_real_keys` / `analyze_real_data` / `import_to_sqlite`)を差し替えた。★**`data/real/xlsx/` を先に見て、無ければ `data/real/` 直下も見る**——本番前日にフォルダの入れ違いで手が止まるのが一番まずいため、移し忘れても動くようにした(CSV側の診断が前からこの作り方なので揃えた)。(2)`diag_real_xlsx.py` に `--demo` とフォルダ指定を追加し、読み込み元とファイル数を最初に表示するようにした。(3)★**`import_to_sqlite.py`(旧・xlsx取込)の説明が「本番移行ツール」のままだった**ので警告を書いた。**これを実行すると `db/tokiwa.db` を作り直す**ため、本番稼働後にうっかり叩くと取り込んだ実データが消える。`docs/migration-guide.md` も同じ理由で「【旧】このページの手順は本番では使いません」を先頭に立てた。(4)**xlsx は捨てずに答え合わせに使う**: `diag_real_xlsx.py` が出す台帳ごとの行数(中身の値は出さないので実データでも安全)を取込ログの件数と突き合わせれば取りこぼしが分かる。**顧客・商品・売掛・ポイントは一致するはず / 販売台帳と処方箋はズレて正常**(販売は伝票+明細に分割、処方箋は宝飾品の誤登録を除外)。この手順を `docs/go-live-checklist.md` に項目として追加した。xlsx はコードと名前が両方並ぶため**区分コードの正解表**にもなる(実際 `diag_kubun.py` がこれで対応表を作った)。(5)`data/README.md` が「demo に xlsx を置く」としか書いておらず**csv も images も載っていなかった**ので、4つの置き場と CSV/xlsx の違いを書き直した。デモデータで、新しい形・移し忘れ・1件も無い場合の3通りを実行して確認済み(11台帳/22CSVを正しく見つけた)。 |
| v0.35.1 | **バッチ18個を整理し、毎日使うものだけをルートに残した**(店の要望。ショートカットを作る前に、というご判断で9/1の取込前に実施)。ルート直下は **本番運用で起動.bat / トキワ停止.bat / バックアップ.bat の3つだけ**。残りは `ツール\` の下に **更新 / 別の起動のしかた / 機器のテスト / データの調べもの** に分類した。※バックアップは締め時に毎日使うのでルートに残した(当初案ではツール配下だったが、毎日使うものを埋めない方針を優先)。★**移動したバッチは基準フォルダの直しが必須**: 全バッチが `cd /d "%~dp0"`(自分の居場所)で動いていたため、そのまま移すと `server\` や `db\` を見失って**15個すべてが動かなくなる**。`cd /d "%~dp0.."` に直し、戻すと壊れる旨のコメントを各ファイルに残した。あわせて案内文の中のバッチ名にも置き場所を添え(「機器ありで起動.bat」→「ツール\別の起動のしかた\機器ありで起動.bat」)、`docs/lan-access.md`・`docs/hardware-check.md`・`docs/feedback-backlog.md` の記載も更新。`ツール/README.md` に一覧と「直す時の注意」(`..` を消すと壊れる / ブロック内の echo に丸括弧を書かない)を置いた。全18件について**文字コード(cp932)・改行(CRLF)・cd の基準・goto の飛び先・ブロック内の丸括弧**を機械的に検査し、呼び出している .py がすべて実在することも確認済み。 |
| **v0.35.0** | **【動作確認・9/1の最終取込に向けた区切り】不明在庫の削除を「状態が在庫のものだけ」に限定した。**これまでは削除リストの**商品番号に一致した商品を全部**取り込まない作りだった。ところが宝飾ナビは**同じ商品番号を別の商品に使い回して**おり(店の1,043番号のうち**975番号=93%が重複**)、実測すると **1,043番号 → 該当2,553件(在庫1,049 / 売上1,162 / 返品251 / 受託91)** で、**消える商品の約半分が「売れた商品」の巻き添え**だった。そのまま取り込むと**1,248件ぶんの購入履歴で商品名が出せなくなる**ところだった。(1)取込を **「リストの番号に一致 かつ 状態が在庫」** のものだけ除外に変更。売上・返品・受託は必ず残す(返品は履歴、受託は他社の預かり品なので消してはいけない)。「不明**在庫**を消したい」という意図がそのままルールになるので、番号の使い回しに悩まされない。(2)残した件数を取込ログに出す(「◯件は在庫ではないため残しました」)。何が守られたかが分かる。(3)`check_delete_list.py` を同じ判定に揃え、**状態ごとの内訳**と「実際に消える件数」を出すようにした。あわせて「まとめて消える」という誤解を招く見出しも直した。**消える商品に販売履歴があれば警告**する(在庫のまま売れているのは不自然なため)。(4)実データと同じ形(同一番号で在庫・売上・返品・受託の4件が並ぶCSV)を作って取込を通し、**在庫の1件だけが消え、他の3件とリスト外の在庫品が残る**ことを確認。確認スクリプトの出力も一致。 |
| v0.34.28 | **本番運用で黒い画面(コマンドプロンプト)を出さないようにした**(店から「違和感がある」との声)。画面を持たない `pythonw` で起動する。★これができるようになったのは v0.34.26 で**動作ログを入れたから**——以前は黒い画面がエラーの唯一の出口だったので隠せなかった。(1)**起動に成功したかを必ず確認する**: 8760番に繋がるかを1秒おきに最大20回試し、**駄目な時だけ画面を出して理由を知らせる**(成功時は何も出ずにブラウザが開くだけ)。画面を隠すと起動失敗に誰も気づけず、**朝レジを開けて初めて分かる**ことになるため、ここは必須。(2)**`トキワ停止.bat` を新規作成**。画面が無いので「閉じて止める」ができなくなるため。`pythonw` は画面もタイトルも持たないので、実行中のコマンドの中身(`server*app.py`)でトキワのサーバーだけを狙って止める。止まったかをポートで確認して報告する。(3)`pythonw` が見つからない環境では**通常のPythonに落とす**(画面は出るが動く方を優先)。PowerShellが無い環境では起動確認を飛ばす。(4)`docs/lan-access.md` のバッチ早見表に「黒い画面」の列と停止バッチを追加し、**原因を画面で見たい時は `機器ありで起動.bat`**と案内した。全バッチのラベル・goto の飛び先・文字コード(cp932/CRLF)を機械的に検査済み。 |
| v0.34.27 | **二重起動をサーバー側で止めるようにした**(店の指摘: 起動中と気づかずにもう一度バッチを叩いたらどうなるか)。★**ポートを bind してみる方法では防げない**のがこの問題の肝: Windows の `SO_REUSEADDR` は Linux/Mac と挙動が違い、**使用中のポートにも割り込んで開けてしまう**ため、トキワが2つ同時に立ち得る。そうなると**ブラウザがどちらに繋がるか不定**になり、片方が機器OFFだと「レシートが出たり出なかったり」する(自動バックアップも二重に走る)。そこで bind ではなく**実際に 127.0.0.1 へ繋いで `/api/health` の応答を見る**方式にした。起動のしかた(バッチ/手打ち)にもOSにも左右されない。判定は控えめで、**応答が『トキワだ』と名乗った時だけ**止める(他のソフトがたまたま同じポートを使っていた場合にトキワの起動を妨げないため)。そのため `/api/health` の応答に`app:"tokiwa"` の目印を追加した。止めた時は**何が起きているか・どうすればよいかを日本語で説明**して終了し、ログにも残す。※データについては元々**壊れない**(WALモードと `BEGIN IMMEDIATE` による排他で、2台のレジが同時に会計しても成立するのと同じ仕組み)。問題は「挙動が読めなくなる」ことだった。`本番運用で起動.bat` 側の確認は**案内用**として残し、『このまま進めてもサーバー側で止まります』と明記した。誰も居ない/トキワが動いている/別のソフトが使っている、の3通りで判定を確認済み。 |
| v0.34.26 | **動作ログを残すようにした＋レシートが出ない時の手順書**(店の要望)。きっかけは「メッセージが早すぎて読めない」——レシートが出ない原因を追えなかったこと。(1)**`logs/エラー_今日.txt`** に記録する。**日付が変わると自動で作り直し、前日ぶんは `logs/エラー_前日.txt` へ退避**(店の指定どおり使い回すが、夕方のエラーを翌朝見られるよう前日だけ残す。ファイルは2つしか増えない)。残すのは**機器の結果(レシート・ドロワー・領収書)・サーバーのエラー・起動時の状態**。★**成功も残す**のが肝心: 今回の原因は「Windowsは受け取っていたが印刷キューで詰まっていた」で、『成功(spooler経由)』の記録があれば**プリンタ本体を疑えばよい**と分かる。失敗時は**その時のプリンタ名とCOM番号も一緒に**書くので、設定違いが一目で分かる。**個人情報は書かない**(顧客名・住所・電話は残さない)。**ログが書けなくても業務は止めない**(例外は握りつぶす)。(2)**エラーのお知らせ(⚠)が自動で消えないようにした**。押すまで残り、赤で表示する。成功のお知らせは従来どおり数秒で消える。(3)**設定・マスター → レシート・機器 に「最近の動作ログ」**を追加(直近30件を新しい順)。ファイルを開きに行かなくても見られる。(4)**`docs/hardware-check.md` に「レシートが出ない・ドロワーが開かない時の調べ方」を追記**。画面右上の機器モード確認 → ログ → pywin32とプリンタ名 → 単体で叩く → **印刷キュー** → 物理確認、の順。2026-08-29に実際に起きたこと(宝飾ナビの古いジョブがキューに詰まり、後ろのトキワのジョブが出られなかった。プリンタ再起動で解消)と、**COM7はCITIZENのドライバが握っている仮想COMなので直接開けなくて正常**(`機器テスト.bat` が失敗しても故障ではない)ことも書いた。`logs/` は .gitignore 済み。 |
| v0.34.25 | `本番運用で起動.bat` に**「すでにトキワが動いていないか」の確認**を追加。古い『機器OFF』のサーバーが残っていると、後から起動しても 8760番 を取れずに終了し、**ブラウザは古い方に繋がる**。画面は普通に開くのに**レシートもドロワーも動かない**という、原因の分かりにくい状態になるため(2026-08-29 店で発生)。ポートが使われていたら中止/続行を選ばせ、中止を選ぶと「『トキワ サーバー』のウィンドウを全部閉じてから」と案内する(既定は中止)。 |
| v0.34.24 | **`本番運用で起動.bat` が動かない不具合を修正**(店で発覚)。原因は私の書き方のミスで、`if ( … )` の**ブロックの中の echo に半角の丸括弧を書いていた**こと。cmd.exe は echo の文中でも `)` を**ブロックの終わり**と解釈するため、`部品(openpyxl)が入っていません` の `)` でブロックが途中で閉じ、以降の行がバラバラに実行されてバッチが壊れていた。**ブロックをやめて `goto` で書き直し**、再発防止のコメントを添えた(この罠は見た目では気づけない)。あわせて**全16個のバッチを機械的に走査**し、同じ書き方が `カード書き込みテスト.bat` にもう1件あったので直した(こちらは全角括弧に置換)。※`トキワ起動.bat` などの `1)` `2)` はブロックの外なので無害。走査は cmd の挙動に合わせ「深さ0の echo の括弧はただの文字/深さ1以上では ) が閉じる」で判定している。 |
| v0.34.23 | **【9/1の最終取込に向けた取込漏れの解消(4項目)＋メーカー選択の絞り込み】**実データ診断(`diag_unused_columns.py`)で列を実測し、**9月1日の取込を逃すと二度と入れられない**項目を先に片付けた。(1)**タグ品名**(`d_item.strtaghinname` 48.0%)→ `products.tag_name`。値札に刷る短い品名。(2)**中分類**(`d_item.strcbuncode` 96.4% → `m_cbunrui`)→ `products.sub_category`。現状28.6%しかない分類を補える。(3)**脇石**(`d_item.strsecode` 32.9% → 石マスタ / `cursubjuryo1`・`cursubjuryo2` 40.8%)→ `products.sub_stone` / `sub_carat1` / `sub_carat2`。**保証書の石情報が中石だけだった問題**の元データ。(4)**DM備考**(`d_user.strbikname1` 32.7%「転居先不明/手紙出さない/受取拒否」)→ `customers.dm_note`。住所の記号・DM区分とは**別系統の「出さない理由」**。※**品番**(`d_siire.strsirsycode`)は値に符丁(`ｳｽｹﾅ` 等)が混ざり入力率も24.5%のため**見送り**(店の判断)。★あわせて3つの安全策を入れた: ①`lookup()` が**列名の取り違えを自動で吸収**し、読み替えた内容を画面に出す(過去に `strdbuncode`↔`strcbuncode` で「取り込んだつもりで空」の事故があったため。**黙って空にしない**)。②脇石の石種コードが石マスタに無い場合は**コードのまま残し、件数を警告表示**する(想定と違う対応表に気づけるように)。③重量の `0` は**未入力として NULL**にする(そのまま入れると「0.00ct」と表示されるため)。新しい列は `db/schema.sql`(新規DB用)と `ensure_schema`(既存DB用)の**両方**に足してある。実データと同じ列を持たせたCSVで取込を通し、4項目が正しい値で入ること・0がNULLになること・マスタに無いコードで警告が出ることを確認済み。(5)**メーカー(仕入先)選択に絞り込み欄を追加**(172件あってスクロールでは選べないため)。仕入登録・商品修正・受託品(催事)・在庫一覧・検索分析・レジの商品ピッカーの**6か所すべて**。★`<select>` はそのまま残し、合わない選択肢を隠すだけの作りにした(値の読み書きも既存の組み立て処理も変えずに済むので他の画面を壊さない)。半角/全角カナをそろえて照合し、**カナの仕入先名はローマ字でも引ける**(`nik`→ﾆｯｹｲ / `tuchiya`→ﾂﾁﾔ宝飾)。漢字だけの名前はローマ字では引けない(読みを持っていないため)。 |
| v0.34.22 | **ログインユーザーの並び順を変えられるようにした**(店の要望)。ユーザー管理の各行に **▲▼** を付け、押した順が保存される。★**ログイン画面の並びにも同じ順が効く**——毎朝ここから人を選ぶので、よく使う人を上に置けることが本来の目的。(1)`app_users.sort_order` を追加(`ensure_schema` が自動で足すので **pull→再起動だけで効く**)。未設定の人は従来どおり「管理者が先・あとはID順」で**末尾**に付くため、**後から作ったユーザーは下に出る**。(2)保存は**全員ぶんの並びをサーバーへ送って総入れ替え**する。2つを入れ替えるだけの作りにすると、無効ユーザーを隠している時に順番がずれるため。(3)▲▼は**画面に見えている行の上下と入れ替える**。隠れている無効ユーザーと入れ替えると「押したのに動かない」ように見えるため。(4)先に画面を動かしてから保存し、**保存に失敗したら一覧を取り直して元に戻す**(押した手応えを優先しつつ、画面とDBが食い違ったままにしない)。端の行では▲▼を押せなくしている。(5)権限は管理者のみ(`ADMIN_ONLY_POSTS`)。(6)DBの複製で、並べ替えの保存・後から作った人が末尾に付くこと・ユーザー管理とログイン画面の並びが一致することを確認済み。 |
| v0.34.21 | **ログインユーザーの一覧で「無効にした人」を既定で隠すようにした**(使わないユーザーが並び続けて分かりにくい、という店の指摘)。「無効にした人も表示する」のチェックで出せる。隠している件数は「(無効 3人を隠しています)」と横に出すので、居なくなったのか隠れているのかが分かる。★編集・全端末ログアウトのボタンには**絞り込む前の位置**を渡している(一覧を絞ると位置がずれて別の人を編集してしまうため)。**削除機能は作らない**方針を決めた: app_users を参照しているのはログインセッションだけで技術的には消せるが、**同じログインIDを後から作り直せてしまう**ため。取消・付け替えの監査記録は「誰がやったか」を名前の文字列で持っているので、消したIDを別人が再取得すると過去の操作が誰のものか分からなくなる(電子帳簿保存法の訂正削除履歴として意味が薄れる)。あわせて **①管理者は常に2人以上にする(自分のパスワードを忘れた時の復旧手段が無いため。復旧スクリプトは「PCの前に座れる人なら誰でも管理者のパスワードを消せる」ことになるので作らない) ②ログインユーザーは削除せず無効で運用する** の2つを `docs/feedback-backlog.md` の決定事項に記録し、`docs/go-live-checklist.md` に「管理者を2人つくる/2人目で実際にログインできるか確認する/宝飾ナビから取り込まれた不要なユーザーを無効にする」を追加した。※今出ているユーザーは**宝飾ナビの `d_systemuser` を取込時に全部持ってきたもの**(パスワードは取り込まないので全員「未設定」)。 |
| v0.34.20 | **顧客詳細の基本情報を並べ替え、4列に詰めた**(店の指定)。並びは ①顧客名・フリガナ・性別・生年月日 ②電話・携帯電話・DM送付・登録日 ③郵便番号・住所1・住所2 ④結婚記念日・指輪サイズ・ピアス穴、その後に**顧客メモ**、以降(メモ・家族情報)は従来どおり。(1)**列数を画面幅任せ(auto-fit)にせず4列で固定**した。並び順を店が決めている以上、画面幅で列数が変わると「名前・フリガナ・性別・生年月日」のような**組が崩れて読みにくくなる**ため(CSSに `.kv4` / 住所行用の `.kv-addr` を追加)。狭い画面では2列→1列に落とす。(2)**余白が目立つ**とのことなので、読むだけの欄に限って入力欄より一回り小さくした(行間・ラベル・入力欄の高さを縮小)。編集画面など他の `.field` には影響しない。(3)トキワで入力する「メモ」(`customers.note`)は、宝飾ナビから引き継いだ「顧客メモ」とは別物なので**顧客メモの後ろ**に移した。 |
| v0.34.19 | 顧客メモが長いとページ全体のスクロールが大変なので、**メモの枠を8行ぶんに収め、はみ出した分は枠の中だけでスクロール**するようにした(店の要望)。高さは決め打ちではなく**「最大の高さ」**にしてあるので、メモが1件しかない人の欄が無駄に空くことはない。★あわせて**薄い枠線**を付けた: 線が無いと「まだ下に続いている」ことに気づけず、大事な申し送りを見落とすため。見出しの件数表示(「顧客メモ(5件)」)と見えている数を突き合わせれば、続きがあるかも分かる。 |
| v0.34.18 | **本番運用で起動.bat を追加**(2026-09-02からのトキワ単独運用に向けて)。**「機器ON(レシート・ドロワー・カード)」と「店内共有(他のPC・iPad・スマホから使える)」を両立するバッチが無かった**ため。既存は `機器ありで起動.bat`=機器ONだが**このPCのみ**、`店内共有で起動.bat`=共有できるが**機器OFF**で、毎日の営業に使えるものが1つも無い状態だった(`server/app.py lan kiki` と両方の引数を渡せることは確認済み)。(1)**★サンプルDBを作らない**: 既存の起動バッチはDBが無いと `make_sample_data.py` を走らせるが、本番用は**「実データを取り込んでください」と出して止まる**(お試し用の偽データが本物と混ざる事故を防ぐ)。(2)起動前に **openpyxl の有無を確認**し、無ければクロネコB2の書き出しに必要な旨と `pip install openpyxl` を案内する(起動自体は止めない)。(3)他端末から開くURLとファイアウォールの許可について画面で案内。(4)文字コードは他のバッチと同じ **cp932・CRLF**(Windowsのコマンド画面で日本語が化けないように)。(5)`docs/lan-access.md` に**4つのバッチの使い分け表**を追加。 |
| v0.34.17 | **取込済みなのに画面に出していなかった項目を顧客詳細に追加**。DBの全263列を機械的に突き合わせて洗い出した結果への対応。(1)★**顧客メモ**: 宝飾ナビの `d_user_memo.memo01〜10` は**取込では `customer_memos` に入っていたのに、どの画面にも出していなかった**。接客の申し送り(来店のきっかけ・ご家族の話・お人柄など)が書かれている実務上重要な情報なので、基本情報タブに宝飾ナビと同じ **memo01〜10 の順**で表示する。空のメモは飛ばす。**表示のみで編集は付けない**——8月末の完全再取込でメモを入れ直すため、トキワで書いた分が消える事故を避ける(移行後に編集を足す)。(2)**登録日**を基本情報に追加(いつからのお客様かが一目で分かる。宝飾ナビの顧客画面にもある項目)。(3)**指輪サイズ・ピアス穴**を基本情報に追加(これまで編集画面を開かないと見えなかった。接客で使う情報のため)。※メールアドレス・購入動機は店の判断で見送り(購入動機は2026-07に非表示と決めた項目)。(4)この3つは**顧客詳細を開いた時にだけ取得**する(`customer_detail` の返り値に `memos`/`extra` を追加)。顧客一覧の配列(2.5万件)に列を足すとページの読み込みが重くなるため。(5)DBの複製で、メモが seq 順に並ぶこと・空メモが除外されること・メモが無い顧客でも壊れないこと・登録日/指輪サイズ/ピアス穴が返ることを確認済み。 |
| v0.34.16 | 購入履歴のボタンのラベルを **「付替」→「顧客変更」** に変更(店の指定)。「付替」は何を付け替えるのかが伝わりにくいため。ボタンの文字だけの変更で、動きは v0.34.15 のまま。マニュアル(詳細版)とテストチェックリスト(11-7)の表記も合わせた。 |
| v0.34.15 | **売上を別のお客様へ付け替える機能を追加**(レジでお客様を取り違えて会計した時の訂正)。従来は「取消して打ち直す」しかなく、**間違えた側のお客様の履歴に取消の記録が残ってしまう**(「うちの母は買っていないのに」と見える)問題があった。付け替えは**金額・在庫・レシートに一切触らず、伝票の持ち主だけを差し替える**。(1)顧客詳細の購入履歴に**「付替」ボタン**を追加。押すと**その伝票の全明細・合計・ポイント・売掛・処方箋の件数**を先に表示する(★伝票まるごとが動くため、同じ伝票に何が入っているかを必ず見せてから実行させる)。付け替え先は顧客検索(カナ・電話・ローマ字)で選ぶ。(2)**一緒に動くもの**: ポイント履歴(`ref_slip_id`)と**ポイント残高**(付与−使用の差を両者で調整)・売掛(`slip_id`)・メガネ処方箋(その伝票の明細に紐づくもの)。動かさないもの: 在庫の動き(商品側の話で顧客に無関係)・来店記録。(3)**断るケース**: 取消済みの伝票 / **売掛に入金済みの記録があるもの**(入金記録は伝票に紐づいておらず一緒に動かせないため、持ち主だけ変えると入金履歴とねじれる。取消して打ち直す) / 同じお客様 / 存在しない顧客 / 担当者・理由の未入力。(4)記録は取消と同じ3点(ログインユーザーはサーバーが入れるので詐称できない・担当者・理由)＋**両方のお客様のメモ**に残す(どちらの画面を見ても経緯が分かる)。元のお客様のポイント残高が不足する場合は0で止め、不足分をメモと画面に出す。(5)権限は取消と同じ(付け替えは「取消して打ち直す」の代替なので、取消ができる人ができるのが自然)。(6)DBの複製で、ポイントつき伝票・売掛つき伝票・処方箋つき伝票の付け替えと、上記5つの断りケース・残高不足の扱いを実行して確認済み。 |
| v0.34.14 | **ローマ字で顧客を探せるようにした**(レジで顧客を呼び出すたびに半角/全角キーを押すのが面倒、という現場の声への対応)。`tsuchiya` と打つと土屋様が出る。**日本語入力をONにしなくても名前で呼び出せる**ので、半角/全角を押す場面自体が無くなる。★前提として、**ブラウザからIMEをONにする手段は存在しない**(CSSの `ime-mode` はChrome非対応・Firefoxも削除済み、`inputmode="kana"` も仕様から削除)。そのため「IMEを自動でONにする」のではなく「**日本語を打たなくて済むようにする**」方向で解決した。(1)**共通部品として実装**(将来ほかの検索にも使うため): `romajiKey(カナ)` でデータ側をローマ字の索引にし、`romajiQuery(打った字)` で問い合わせを同じ形にそろえ、`romajiOf()` が索引をキャッシュする。**ヘボン式(shi/chi/tsu/fu/ji)でも訓令式(si/ti/tu/hu/zi)でも当たる**ように、両方を同じ正規形(訓令式寄り)に寄せてから照合する。促音(ハットリ→hattori)・長音(オータ=オオタ→oota)・拗音(ジュンコ→junko/zyunko)・姓名の間の空白無視(tsuchiyamiwa)にも対応。半角カナの濁点は NFKC で正しく合成してから変換する。(2)適用先は **レジ・修理の預かり・家族リンク・新規登録の住所コピー**(共通の顧客検索ボックス)と、**顧客管理の一覧検索**。レジで覚えた打ち方が一覧でも通じないと混乱するため両方に入れた。(3)**英字が打たれた時だけ**ローマ字照合が働く(カナ・漢字・電話番号・IDの従来の検索はそのまま)。(4)索引は行(配列)をキーにしたWeakMapに保持し、**カナを直したら作り直す**。実データ規模(2.5万人)で初回93ms・2回目以降19msを実測。(5)**フリガナが未登録の顧客はローマ字では出ない**(従来どおり漢字・電話番号で探す)。 |
| v0.34.13 | **重複チェックで「◯◯と△△の内容を確認」を押しても何も起きないように見える問題を修正**。比較表(統合前の確認)が**候補一覧の下**に追加される作りだったため、候補が何組もあると画面の外に出て、押しても反応が無いように見えていた(機能自体は動いていた)。(1)比較表のブロックを**候補一覧より上**に移動(仕切り線を上→下に付け替え)。(2)★あわせて「内容を確認」を押した時に**モーダルの中を一番上までスクロール**するようにした。これが無いと、下の方の組のボタンを押した人は今度は「上に戻らないと見えない」という同じ状態になる。表示位置だけの変更で、比較・統合の処理そのものは一切変えていない。※統合の実行は**管理者アカウントのみ**(`ADMIN_ONLY_POSTS`)なので、社員・パートでは最後の統合ボタンで弾かれる仕様は従来どおり。 |
| v0.34.12 | **家族(リンク)の続柄を両方向とも入力できるようにした**(§1-7の追加指摘)。v0.34.11では相手側に一律「家族」を入れていたが、正しくは「Aから見てBが妻」なら「Bから見てAは夫」で、片側だけ直せる状態だと「Aでは妻・Bでは家族」というズレが残る。(1)「登録済みの顧客とリンク」の画面に**続柄の欄を2段**にした。どちら向きの続柄か間違えないよう、欄のラベルに**実際のお名前**を出す(「築瀬 智宏 から見て 佐藤 花子 は」「佐藤 花子 から見て 築瀬 智宏 は」)。(2)**上段を選ぶと下段が自動で入る**: 夫⇄妻は確実に、長男・次男・長女・次女→**父/母**(本人の性別で判定)。**父・母→何番目の子かはデータから決めようがないので「家族」**を入れ、欄の下に「自動では決められません」と注意書きを出す(決まった時は出さない)。裏返しの規則は `reverse_relation()`(サーバー)と `reverseRelation()`(画面)を**対で持つ**(コメントで相互参照)。(3)**修正も追加と同じ2段の画面**にし、**相手側の対の行の続柄も一緒に直る**ようにした(氏名・性別・生年月日は相手の顧客情報に追随するため編集させない)。削除は v0.34.11 で両側から消えるようにしてある。(4)**空欄は選べないようにし、最低でも「家族」**が入る(空欄=「ー」表示だと未入力か間違いか区別できないため)。「家族」は正しい続柄が分からない時の逃げ道として**両方の欄と自由入力側にも選択肢として追加**。(5)登録し直し(既に対の行がある場合)も画面で選んだ続柄で**両側とも上書き**する。(6)**マニュアル(詳細版)に「家族の登録」の節を新設**し、2つの登録方法・両方向のルール・自動で決まる範囲の表・「家族」の意味・修正削除が両側に効くことを記載(`scripts/build_manual.py` が生成元。HTML再生成済み・PDFは未再生成)。裏返しの規則は全パターンを実行して確認。 |
| v0.34.11 | **【最終テスト §1 顧客の指摘9件を修正】**(1)**重複判定を厳格化(最大の指摘)**: 「電話番号だけ一致」で同居のご家族が、「氏名だけ一致」で同姓同名の別人が大量にヒットし、登録時の警告も重複チェック一覧も使い物にならなかった。**氏名と電話番号の両方一致**(または氏名と生年月日の両方一致)のみを重複とするよう変更。あわせて実データに多い**ダミー電話番号(1111111111・1111-11-1111 等=数字の種類が2以下)を判定から除外**(`_tel_key`)。(2)**編集保存で毎回重複警告が出る**のをやめ、**氏名・電話を変えていない編集では警告しない**。(3)**顧客検索に住所を追加**(名前・カナ・ID・住所の一部で探せる)。(4)**編集後に一覧の住所が古いまま**の不具合を修正(保存後に一覧を作り直す)。(5)**家族(リンク)の非対称を修正**: A側で削除しても**B側にAが残る**→**対の行も一緒に削除**、B側の続柄が「ー」→**「家族」**を入れる(未入力と壊れの区別)、B側に**修正・削除ボタンが出ない**→サーバーが `reverse_id` を返し画面が行のidを持つようにした。二重表示の防止も追加。(6)**年齢の自動表示**: 顧客編集の生年月日の隣にリアルタイムで「(56歳)」、顧客詳細の基本情報の生年月日にも併記(入力ミスに気づけるように)。(7)**明治・大正を和暦の候補から削除**(実務で使わない。既存データの表示・変換は従来どおり対応)。(8)**基本情報にDM送付を表示**。(9)**新規登録でご家族から住所・電話・郵便番号をコピー**できる欄を追加(同居家族の打ち直しをなくす。新規登録時のみ表示)。ダミー電話・同居家族・同姓同名・真の重複を並べた実DBで判定を検証し、家族の対称追加/削除も確認済み。 |
| v0.34.10 | **返品の集計を「締めた日の日報は変えない」方式に変更(顧問税理士の確認に基づく・最終テスト中の重要変更)**。従来は返品(取消)すると元の売上が過去の日報から遡って消え、返品した日の日報には何も出なかった。新ルール: **当日中の取消=訂正**(その日の日報・現金締めから消える。5回打ち直したら最終だけ残る)/**日をまたいだ取消=返品**(売った日の日報・現金締めはそのまま。**返品した日**の日報に「元の売上日の注記つきマイナス行」が載り、現金締めには返金分が減算される)。宝飾ナビの移行データが元々「返品日にマイナス明細」方式だったのとも整合する。(1)**返金方法の選択を追加**: 取消ダイアログに現金/クレジット/銀行振込/ポイント/その他(既定=現金)。カードの返金期限切れ等で支払と違う方法になるケースがあるため固定にしない(店の指定)。`sale_lines.refund_method`・`sales_slips.refund_method` を追加(ensure_schemaで自動)。現金締めには現金返金だけが効く。(2)対象: 日報照会(明細・タイル・支払方法別内訳・CSV)・ホームのタイル・売上伝票明細表(明細・四分類・CSV)・返品行は赤字表示。(3)**変えていないもの**: 顧客の累計購入額・ランク(返品差引後のまま)・在庫の復活・取消の監査ログ・購入履歴の「取消済みも表示」。(4)検証: 税理士指定のシナリオ(12/15リング30万現金→1/10返品)で、12/15の日報・締めが不変のまま1/10に−30万(注記・返金(現金))が載ること、当日5回打ち直しで最終1件だけ残ること、期間明細で売上行+返品行の両建て(純額0)を確認。チェックリスト§11を新ルールに書き換え。 |
| v0.34.9 | **【最終テストで発見・修正4件目/5件目】顧客詳細が10秒超かかる不具合と、商品詳細ジャンプの再修正**。(1)**明細の多い顧客の詳細表示が体感10秒**: 購入一覧の品名を処方箋の正式名に差し替える処理が、明細1行ごとに処方箋5万行を総なめしていた(`prescriptions.sale_line_id` に索引が無い唯一の外部キーだった)。対応は2段: ①**索引 `idx_rx_line` を追加**(「検索に使う外部キーには必ず索引」の原則の抜けを埋める。既存DBは `ensure_schema` が自動で張るので **pull→サーバー再起動だけで効く**。明細の品名修正も同じ索引で速くなる)。②**相関サブクエリをやめ、顧客の処方箋を1回だけ取って辞書で突き合わせる形に書き換え**(速度のためではなく可読性のため。同じ関数内の処方箋候補と同じパターンに統一)。実データ規模(顧客2.5万・明細20万・処方箋5万)で明細1,168行の常連顧客が **12.4秒→0.010秒**。差し替えの動作(レンズ名優先/フレーム名への切替/複数処方箋は最初の1件/処方箋なしはそのまま)は書き換え前と同一であることをテストで確認。(2)**v0.34.5の商品詳細ジャンプが実データで効いていなかった**: 商品キーを `Number()` で数値化していたが、**実データのキーは「01-104714」形式の文字列**のため NaN になり照合が空振り→従来どおり同番号の別商品が開いていた(スクリーンショットで発覚)。文字列のまま渡すよう修正(購入履歴・レジ🔍の2か所)。※v0.34.5の検証は整数キーで作った偽DBだったため見逃した。今回は**実データと同じ文字列キー形式で検証し直し**、正しい方(リング)が選ばれることを確認。(3)roadmap に「正式運用リリース前の大掃除」(効率見直し・未使用コード削除・コメント整理。合格→大掃除→再確認→v1.0.0 の順)を記録。 |
| v0.34.8 | 保証書の備考の位置を**担当者の右6cm→左6cm**に修正(v0.34.7で「右」と聞いて実装したが左と判明)。A4は 左78mm−60mm=**左18mm**。A5は暫定の担当者位置(左55mm)から60mm引くと−5mmで紙の外に出てしまうため、**実測値をもらうまで紙の左端(3mm)に寄せ、画面に「A5の備考は仮の位置」と警告を出す**(勝手に紙の外へ流して欠けさせない)。A5の座標が未実測であることの裏付けにもなっている。 |
| v0.34.7 | **保証書の印刷を実装**(台紙へ値と写真だけを重ね刷り)。顧客詳細の購入履歴に「保証書」ボタンを追加(在庫品の購入のみ。商品番号・石情報が必要なため)。**用紙は買上金額で自動判定: 10万円以上=A4 / それ以外=A5**(画面で変更も可能。自動判定と違う用紙を選ぶと注意が出る)。**台紙は縦向きで給紙**(宝飾ナビと同じ入れ方。プリンタが縦しか給紙しないため)。刷る項目は買上日(「2025 年 12 月 5 日」形式)・お客様名・商品名・商品番号・石の情報・鑑定書No・担当者・備考(印刷時に手入力)・商品写真。**下段の控えブロックは廃止**したため、住所・電話・カナ・家族は一切刷らない(店保管の控えはデータで保存しているため不要との判断)。座標は `WARRANTY_POS` に集約し、ここだけ直せば印字位置が変わる。取消済みの明細は保証書を作れない(サーバーで拒否)。A4は実測済み、**A5の座標は未実測のため暫定値で、画面に「台紙に刷る前に白紙で重ねて確認」の警告を出す**。★**宝飾ナビの「脇石1・脇石2」がトキワに取り込まれていないことが判明**したため、石の情報は中石のみになる(実物は「BO28.720ct D2.550ct」のように脇石も入る)。8月末の実データ再取込で脇石の列を足すまでは中石のみ。A4/A5の自動判定・日付形式・石未登録時の空欄・取消済みの拒否を実DBで確認済み。 |
| v0.34.6 | **商品詳細に「買った方」を追加し、その顧客の購入履歴へ飛べるようにした**。修理で持ち込まれた品のタグ番号から購入者と購入日を辿れるようにするのが狙い。`get_product` が購入者を新しい順で返し(`product_buyers`)、商品詳細に「買った方 ◯◯ 様(買上日) [購入履歴を見る]」を表示する。**同じ品が「売れて→返品→また売れた」場合は最後の売上を出し、残りは「他○件の売上あり」と件数だけ添える**。取消済みの明細・伝票は数えない。飛んだ先では顧客詳細の購入履歴タブが開き、**戻るボタンが「商品・在庫へ戻る」「レジへ戻る」に変わる**(どこから来たかで切り替え)。在庫一覧・購入履歴・レジの🔍のどこから商品詳細を開いても共通で効く。取消済み売上の除外・複数売上の並び・未販売品が空になることを実DBで確認済み。※パート権限でも見える(パートは在庫画面に入れないが**レジ明細の🔍から商品詳細に到達できる**ため。顧客情報自体はパートも閲覧できる設計なので、新たな情報開示にはならない)。 |
| v0.34.5 | **【最終テストで発見・修正3件目】購入履歴・レジの🔍から開く商品詳細が、同じ商品番号の別の商品を開くことがある不具合を修正**(実機で発覚: 番号04714が宝飾ナビではPT900ブラックOP・ダイヤリング¥10,285,812なのに、トキワではシトリンナチュラルブレスレット¥600・写真なしが開いた)。原因: 宝飾ナビは**同じ商品番号を複数の商品に使い回すことがあり**(実データ診断で17組34件を確認済み)、商品詳細へのジャンプが「商品番号」で探していたため、同番号の別商品が先にヒットしていた。**商品キー(内部の一意なキー)で特定する方式に変更**: `customer_detail`の購入履歴の行に商品キー([10])を追加し、購入履歴のダブルクリック・サムネイルクリック・レジ明細の🔍は、キーが分かる場合は必ずキーで開く(キーが無い/一致しない場合だけ従来どおり番号)。同番号の別商品2件を作った実DBで、履歴が正しい方(リング・画像あり・キー一致)を返し、検索は2件とも返して画面側がキーで選別できることを確認済み。※履歴のサムネイル自体は元からキーで紐づいているため正しい商品の写真が出る設計だが、この件の「写真も出ない」は誤った商品(写真未登録のブレスレット)が開いていたことによる。修正後も写真が出ない場合は、その商品に写真が無いか、画像ファイルが `data/real/images/` に無い(ファイル名不一致)可能性があるので商品番号を添えて報告してほしい。 |
| v0.34.4 | **【最終テストで発見・修正2件目】保証書の位置合わせ用紙で、マス目・目盛り線・100mmのものさし線が消えて数字だけになる不具合を修正**(実機のプレビューで発覚)。線を「背景色」で描いていたのが原因。ブラウザは印刷時に背景色を初期設定では刷らない(インク節約)ため、文字である数字だけが残り、線が全て消えていた。線を枠線(border)で描く方式に変更(枠線は文字と同じ扱いで必ず刷られ、プレビューの縮小表示でも最低1ピクセルで描画される)。※値札(タグ)の見当十字・目盛りも同じ背景色方式だが、実機で刷れることを何度も確認済みのため触っていない(動いているものを変えてデグレさせない)。もし今後値札側でも線が消える症状が出たら同じ修正を適用する。 |
| v0.34.3 | **【最終テストで発見・修正1件目】openpyxl が無いPCでトキワ全体が起動できない不具合を修正**。v0.33.1のクロネコB2書き出しで `openpyxl` をサーバー起動時に読み込む作りにしてしまい、店のPC(未導入)で `ModuleNotFoundError` → サーバーが即終了していた。起動バッチはサーバーを別ウィンドウで開くためエラーごと一瞬で消え、「バッチを押しても立ち上がらない」ように見えた。B2書き出しは付加機能なので、読み込みを使う瞬間まで遅らせ、**部品が無くてもトキワ本体は必ず起動**し、B2書き出しを押した時だけ「python -m pip install openpyxl を実行してください」と日本語で案内するようにした。openpyxl無しの環境を再現して起動・案内の両方を確認済み。※サーバー起動時に読み込む外部部品が他に無いことも確認した(この罠はopenpyxlのみ)。店のPCへの恒久対応は `python -m pip install openpyxl` の実行。 |
| v0.34.2 | **v0.34.1で見つけた「画面に見えているが中身が無い機能」のうち3つを実装。**(1)**★ホームの「今週のお声がけ」を実データから作るようにした**(最重要)。従来はHTMLに5件(山田花子・大岡さゆり・渡辺真宏・妻木亜子・前田富佐代)を直接書いていたため、本番運用でも実在しない顧客の誕生日・記念日・修理納期が毎朝出続ける状態だった。`db_query.care_list()` が4種類を集める: **誕生日・結婚記念日=今月ぶん(過ぎた日は非表示)**、**ポイント失効=失効日(最終購入日+card_expiry_years年)の30日前から**、**修理納期=引渡予定日の7日前から(過ぎた分は出し続け背景色と赤字で強調し、一覧の先頭に並べる)**。★**死去(ＳＳＳ)等のDM不可の顧客と集計対象外(ななし)は、こちらから連絡する用件から必ず除外する**(亡くなった方の誕生日をお声がけ候補に出すのは事故のため)。修理納期はお預かり品を返す義務があるため除外しない。✓を押すと `approach_history` に `ref_key`(例「誕生日:1234:2026」)つきで記録し、**再読み込みしても再出現しない**(従来は画面上で行を動かすだけで、再読み込みで元に戻っていた)。顧客詳細の「購入・アプローチ履歴」にも残る。二重押しで履歴が増えず、押し間違いは取り消せる。表示は12件までで残りは「他○件」。(2)**データ入出力(CSV書き出し)を実装**: 顧客一覧・在庫一覧をCSVで出せるようにした(既にあった5つのCSV出力=売掛・日報・売上伝票明細・棚卸し差異・詳細検索は各画面のまま、案内だけ載せた)。パート権限では下代の列そのものを出さない。宝飾ナビからの取り込みは既存データを作り直す操作のため画面には出さずコマンド専用と明記。(3)**保証書の位置合わせ画面を実装**: 保証書(A4)・ミニ保証書(A5)に1mm刻みの目盛りと10mm間隔のマスを刷る用紙を追加。宝飾ナビで刷った保証書と重ねて項目の位置(左から○mm・上から○mm)を測るためのもので、値札(タグ)と同じ考え方。原寸で刷れたか確かめる100mmのものさしも入れた。あわせて**「帳票・プリンター」のカードをこれに置き換えた**(ブラウザからWindowsのプリンタを指定する手段は無く、A4帳票の印刷先はOSの印刷ダイアログに委ねるしかないため)。(4)**オフライン同期は作らないと決定**: サーバーがレジと同じPCで動いており`localhost`に繋いでいるだけなので切れる回線が無い。クラウド移行後(将来の2.0)の課題として§18に明記。(5)チェックリストに§19〜§21(29項目)を追加。 |
| v0.34.1 | **最終テストのチェックリストを第三者視点で再点検し、漏れを補充(123→137項目)。** 文章を読み返すのではなく、`server/app.py` のAPI一覧(84本)・画面とタブ・設定カードを機械的に洗い出して突き合わせた。(1)**漏れていた14項目を追加**: 郵便番号→住所検索(オフライン時の案内含む)・集計対象外フラグの設定・ドロワー単独開放・カードを会計せず排出・掛率と折数の自動計算(旧チェックリストにあったのに落としていた)・写真プールの削除・仕入先のジャンル設定・修理のステータス更新・発行済み帳票の一覧・使われていない担当者の掃除・強制ログアウト・番号なし明細の品名修正(金額は不可/在庫品と取消済みは拒否)・ホームの日次タイル。あわせて**返品を「明細1行」と「伝票まるごと」に分離**(別のAPIで別の挙動のため、1項目にまとめていたのは不十分だった)。(2)**★§18「テスト前に判断が必要なこと」を新設**: 画面に見えているのに中身が無い機能が判明した。**設定画面の3カード(オフライン同期・帳票プリンター・データ入出力)は `onclick` が無く押しても何も起きない**(オフライン同期は切替関数が存在するがどこからも呼ばれていないデモの残骸で、接続表示は常に「オンライン」・未送信件数も固定文字)。さらに**ホームの「今週のお声がけ」5件がHTMLに直接書かれた見本**で、実データから作られていない(山田花子・大岡さゆり・渡辺真宏・妻木亜子・前田富佐代の固定表示。JSは残り/対応済みの間で行を動かすだけ)。本番運用すると毎朝ホームに実在しない5人の誕生日・記念日・修理納期が出続けるため、扱いの判断が必要。推奨は「最終テストでは枠を隠し、正式運用後に実データから作る実装を入れる」。 |
| v0.34.0 | **最終テストの開始(この版を検証対象にする)。** 合格したら `v1.0.0` を付けて正式運用を開始する。(1)**`docs/test-checklist.md` を全面的に作り直した**。従来版は最終更新が2026-07-22で、末尾の「追加待ち(まだ未実装)」に並ぶ5項目(ポイント拡張・修理写真・家族の修正削除・棚卸し・ハードウェア連携)が**すべて実装済み**になっており、逆に直近3週間の主要機能(値札印刷・レシート34桁とロゴ・会員証発行・カード券面書換・バックアップとデータ点検・符丁・受託・クロネコB2書き出し・DM送付不可の判定・棚卸しの読み取り失敗案内・戻るボタン対策)が1行も載っていなかった。全項目を**「操作」と「合格」の2行**で書き直し、合格条件を「正しく動く」ではなく目で見て確かめられること(件数が減る・この文字が出る)で表現。全17セクション＋事前準備＋後始末。(2)**機能テストだけでは足りない項目を追加**: 実データでの1日通し、テスト後にバックアップから戻す後始末、B2ファイルをヤマトのシステムに実際に取り込む確認。(3)**懸案・許容事項の節を新設**: 値札バーコードの読み取り率(規格80%に対し72%で12枚中5枚。手入力前提の運用になることを明記)・DM区分の想定外表記・詳細検索の2000件上限を、承知の上で運用に入る形で明文化。(4)**`docs/backup.md` の復元手順にWALファイルの削除を追記**(下記)。(5)**CLAUDE.mdにメジャーバージョンの基準を追記**: `0.x`=開発中・`1.0.0`=正式運用開始、MAJORは操作の覚え直し/戻せない移行/動かす場所の変更。**バージョンは「事実」に付けるもので「予定」に付けない**ため、最終テストの開始時点では1.0.0にしない。 |
| v0.33.3 | **DM区分(`dm_ok`)の表記を「送る/送らない」に統一し、DM不可の判定を1本化**。(1)**表記の統一**: 同じ列に取込元ごとの表記が混在していた(CSV取込=`送る/送らない`、xlsx取込=元の列の値そのまま、旧UI=`可/不可`、サンプルデータ=`可`固定)。顧客編集画面の選択肢を`送る/送らない`に変更し、読み取り側(`dm_is_blocked`/`dmIsBlocked`)で古い表記を吸収するため**既存データの変換は不要**。★編集画面を開いた時に古い値(`可`等)が選択肢に無く選択が外れて設定が消える事故を防ぐため、`dmNormalize`で寄せてから表示する。(2)**判定の1本化**: 「住所の先頭の記号(ＴＴＴ等)」と「DM区分」のどちらか一方でも「出さない」なら出さない(OR判定・安全側に倒す)。片方の記入漏れでも事故にならない。24面ラベル・B2書き出し・詳細検索で同じ判定を使う。(3)**詳細検索の「DM可のみ」を実際に機能するよう修正**: 従来は`COALESCE(dm_ok,0)=1`と数値の1を比べていたが、実際に入っている値は文字列のため**一件も一致せず何も絞れていなかった**。空欄は「可」として残す(店の運用で、出したくない意思がある時だけ値を入れているため)。住所の記号も`normjp`で全角・大小を吸収して除外する。※`dm_ok`が「送らない」を意味する値の一覧は`db_query.DM_NO_VALUES`(画面側は`DM_NO_VALUES`)にあり、**ここに無い値は「送る」扱い**になる。xlsx取込は元の列の値をそのまま入れるため、実データに別の表記(`有/無`等)がある場合は追加が必要。 |
| v0.33.2 | **DM送付不可の印(住所の先頭のＴＴＴ等)を機械にも読ませ、宛名の印刷・書き出しから自動除外**。店では以前から、DMが届かない/出さない顧客の住所の先頭に記号を3つ重ねて書く運用をしている(Ｔ=転居・Ｓ=死去・Ｍ=店の都合・Ｋ=本人希望・Ｆ=福祉/生活保護)。住所を見ただけで理由が分かり万一印字されても気付ける良い仕組みだが、判定が人の目だけに委ねられていた。とくにクロネコB2の書き出しは目視で止める工程が無く、ヤマトへ不正な住所(や死去された方への発送)を送ってしまう恐れがあったため、この記号を判定して**24面ラベル印刷とB2書き出しの両方で自動的に除外**し、除外した件数と理由(「転居1件・死去2件」)を案内するようにした。全角・半角・小文字のどれで書かれていても拾う。判定はサーバー(`db_query.dm_block_reason`)と画面(`dmBlockReason`)の両方に持ち、コメントで相互参照している(B2=サーバー側・24面ラベル=画面側で完結するため)。あわせて**B2の電話番号・郵便番号に文字列書式(`@`)を明示**し、Excelで開いた時に「数値として保存されていません」から数値変換して先頭の0が落ちる(0060000→60000)のを防いだ。※調査中に別件を確認: `dm_ok`列は取込が「送る/送らない」・画面が「可/不可」・詳細検索の絞り込みが`1`と比較しており表記が三者で食い違うため、詳細検索の「DM可のみ」が何も絞れていない(今回は未修正・別途対応)。 |
| v0.33.1 | **クロネコB2(ヤマト運輸DM便)への書き出しを追加**。24面の住所ラベルとは別に、実際に使っているクロネコB2の取込テンプレート(xlsx)を分析し、その列位置(送り状種類=3固定・出荷予定日=当日・お届け先電話番号/郵便番号/住所/アパートマンション名/名前・敬称=様固定・ご依頼主郵便番号/住所/名前)に合わせて顧客データを書き出すサーバー機能(`server/kuroneko_b2.py`・`POST /api/kuroneko_b2_export`・openpyxlでxlsxを直接生成)を実装。ご依頼主(自店)の郵便番号・住所を`store_info.py`に追加(`SENDER_POSTAL`/`SENDER_ADDRESS`)。電話番号・郵便番号・住所のいずれか未登録の顧客は書き出しから除外し、除外した顧客名を案内する。既存の住所ラベル印刷と同じ顧客選択(顧客一覧・顧客検索・詳細検索の3画面)から「📦 クロネコB2で書き出す」ボタンで利用可能(蓄積のみ・未検証)。 |
| v0.33.0 | 値札(タグ)印刷を実機で動作確認、棚卸しスキャンの読み取り失敗案内を追加。(1)**値札の位置ズレ・白紙2ページ目・見当十字の消失を一通り修正**(v0.32.1〜v0.32.3の一連の不具合)。見当十字の座標を台紙の寸法から直接計算する方式に変更し、`.tag-sheet`の高さを146mmにして2ページ目を防止。A4テスト印刷にもオフセット・倍率補正を適用し、枠線を中身より前面に描画して倍率補正時も消えないようにした。台紙寸法を実測で確定(タグ29×32mm・隙間3mm・余白5mm・帯16mm)。(2)**棚卸しスキャンの読み取り失敗を明確に案内**: バーコードが規格(80%)未満の縮小率(72%)のため一定数の読み取りエラーが実機で確認されたことを受け、桁数不足・チェックデジット不一致の入力を「商品が台帳にない」ではなく「読み取りに失敗した」と区別して案内するよう修正(`scan_failed`)。5回連続失敗した場合は、台紙の管理番号を手入力できることも案内する。(3)**セキュリティ**: 応答にキャッシュ禁止(no-store)を追加し、ログアウト後の「戻る/進む」でブラウザ内の保存画面が復元されないようにした(pageshowのpersisted検知で読み込み直す)。動作確認: サーバー+Playwrightでログイン・値札プレビュー(見当十字4つ・反転設定)・棚卸しスキャン(12桁エラー案内・5回連続失敗時の手入力案内)・no-storeヘッダーを確認。JSエラーはfavicon.ico の401/404のみ(既存の仕様・実害なし)。 |
| v0.32.3 | A4テスト印刷で**台紙原寸の外枠が消える不具合を修正**。倍率補正(101.1%等)をかけると中身(.tag-sheet)が枠(103×147mm)より大きくなり、中身の白い背景が枠線を塗りつぶしていた。中身は position:relative のため、親要素の outline より前面に描かれるのが原因。枠線だけを独立した要素(.tag-a4-edge)にしてDOM順で中身の後ろ=前面に配置し、倍率をかけても必ず枠線が見えるようにした。以前この症状に対して「A4では倍率を無効化する」という対症療法を入れていたが(v0.32.2で撤回)、今回が本来の修正。 |
| v0.32.2 | **A4テスト印刷にもオフセット・倍率の補正を反映**するよう修正。従来はA4モードだけ 0mm/100% に固定していたため、オフセットを -1/0/2 と変えて刷り比べても出力が完全に同一で、「専用台紙を使う前にA4で予行演習する」という目的が果たせていなかった。固定していた理由は「倍率を適用するとA4の外枠の右・下辺が消える」ことだったが、その真因は見当十字の座標計算ミス(v0.32.1で修正)と切り取り用の囲い(撤去済み)だったため制限を解除した。A4の説明文に、その印刷に反映されている補正値を明記するようにした。※A4(カセット給紙)と専用台紙(手差し)ではプリンタ自身の位置合わせが異なるため、A4で確認できるのは「補正が効くか・何mm動くか」であり、最終値は台紙1枚で詰める必要がある。あわせて台紙の寸法を実測で再確認し、タグ29×32mm・隙間3mm・余白5mm・帯16mm(=32mmの中央)で確定(コードの値は変更なし)。台紙ごとに余白の振り分けが±1mmばらつくため、左右対称の5mm/5mmが最も安全と判断。 |
| v0.32.1 | 値札(タグ)印刷の位置ズレと白紙2ページ目を修正。(1)**見当十字(トンボ)の座標を台紙の寸法から直接計算**するよう変更。従来は右下側を `calc(100% - ..)` で指定していたため、印刷時だけ `.tag-sheet` の高さを縮める細工と組み合わさって十字だけがズレていた(プレビューでは正しいのに印刷だけズレる)。あわせて `(余白-2)` と `(余白+2)` を取り違えていた符号ミスも修正(右上・左下・右下の3つが用紙外の x104mm/y148mm に配置され表示されていなかった)。修正後の中心は 左上(5,5)・右上(98,5)・左下(5,142)・右下(98,142) で4つとも用紙内。(2)**`.tag-sheet` の高さを147mm→146mm**(用紙より1mm低く)。用紙とぴったり同じ高さだとブラウザが丸め誤差で1ページに収まらないと判断し白紙の2ページ目を作るため。下端1mmは余白地帯でタグの位置には影響しない。これにより印刷時だけ高さを変える細工が不要になり撤去(プレビューと印刷で同じ値を使う)。(3)**キャッシュ禁止(no-store)**を応答に追加＋**戻る/進むで復元された画面は読み込み直す**(bfcache対策)。ログアウト後に「戻る」で顧客名・売上が表示されたまま復元されるのを防ぐ(データ自体は401で保護済み)。(4)バーコードと管理番号の内側寄せを1mmまで緩め幅27mm(倍率66%→72%)に。実機スキャンで12枚中3枚しか読めなかったため。 |
| v0.32.0 | レシート印字幅・ロゴの実機確定と、カード運用の完成(★ロードマップ最後の難関「カード書込の実機検証」を通過)。(1)**印字幅を実測で確定**: 桁数テスト(`レシート桁数テスト.bat`)で36桁=432ドットまで印字可と判明(旧28桁の根拠「30桁ではみ出た」は誤実測)。ただし432は余白ゼロでロール紙の左右の遊びにより右端が欠けたため(¥998→¥99等)、安全マージン2桁を引いて **RECEIPT_WIDTH=34桁(408dot)** で運用。PAPER_DOTS=432。罫線・金額の右寄せ・中央寄せは桁数基準のため自動追従し、右余白が解消。(2)**ロゴ**: 絵柄の周りの白い縁を自動トリミングしてから拡大(実ロゴは絵柄が画像の高さ30%しかなく上下に大きな空白が出ていた)→336x119dotで紙の使用量も約27mm減。ラスタのビット単位オフセットに対応し(`_raster_from_image`)、文字の中央軸(34桁枠の中央204dot)に合わせて **LOGO_OFFSET_DOTS=-12**。中央寄せ確定テスト(`ロゴ中央寄せテスト.bat`)と見比べテスト(`ロゴ印字テスト.bat`・7パターン)を追加。(3)**感謝の一文を設定化**(receipt_thanks): 「お買上げありがとうございます」を修理のお客様にも合う文言に変更可能。空欄=印字なし。あわせて `point_settings()` が**保存された空文字を尊重**するよう修正(従来は空を未設定扱いして既定値へ戻すため空欄指定が保存できなかった)。(4)**カード運用を案A(入れっぱなし方式・宝飾ナビと同じ)に**: 読取後もカードを装置内に保持し、会計確定で券面(名前・有効期限・最新ポイント)を書き換えて排出。レジに「💳カード保持中」バー(会計せず排出ボタン付き)。券面書換に失敗しても会計・ポイント残高(DB)は成立済みとして扱い、次回来店時に書き直せる。カードの顧客と会計の顧客が違う場合は書き換えない。`db_query.card_face_data`・`POST /api/card_face`。(5)**会員証発行を実装**(デモ表示から実機能へ): 顧客詳細のボタンで1回の挿入から 磁気書込(TKW+顧客ID)→券面印字→排出。読み取りを挟まないため**新品(磁気が空)のカードでも発行できる**。実行前に「挿入カードの磁気を上書きする」確認あり。磁気成功後に券面が失敗した場合は ok+face_error で「呼出は使える」ことを通知。`POST /api/card_issue`。(6)**券面のポイント数値を右揃え**(FACE_RIGHT_X=290・安全弁つき。6桁までは縦倍、7桁以上は自動で通常サイズに落として右揃えを維持)。(7)レジの「レシート下部メッセージ」欄をテンキー(取消/会計を確定)の下へ移動。実機テスト: 会員証発行→カード呼出(保持)→会計確定で券面更新→排出 の通しを予備カードで確認済み。動作確認: サーバー+Playwrightで23項目(画面13: 一言欄の配置・共通メッセージ編集ボタン・感謝の一文の初期表示/保存/空欄保存/復帰・会員証発行ボタンが機器OFF時に案内のみ・カード保持バーの表示/名前/非表示・JSエラーなし / サーバー10: 罫線34桁・感謝の一文・登録番号・共通メッセージ空・ロゴ幅376dot/高さ119dot・カット前6行送り・券面ポイント3ケースの右端290と自動サイズ切替) |
| v0.31.0 | ポイント機器組込・レシート刷新・バックアップの3本柱(v0.30.0以降23コミットの一括リリース)。(1)**ポイント機能フェーズ1〜2**: レート設定(100円=1pt・画面から変更可)・レジで会計ごとの倍率(2倍デー等)・ポイント払い(残高確認/使用時付与なし)・手動修正(理由必須で履歴に残る)・機器モード分離。会計フローに機器を組込(カード挿入→顧客呼出/会計→レシート印字＋ドロワー)。起動時にポイント初期化が早すぎて画面全体が止まる障害も修正。(2)**カード券面リライト印字**: 仕様書4-2から実装し実機較正でレイアウト確定(縦置き・名前縦倍・ポイント強調・フリーメッセージ2行・半行余白)。(3)**レシート刷新**: 28桁化ではみ出し解消・TEL・割引率(定価と%引き)・ロゴ印字。実機見比べ(7パターン)でロゴを幅いっぱい42mm・しきい値160に確定し、目盛り実測で印字幅384dotと判明→中央寄せを確定(PAPER_DOTS=384・1ドット単位のオフセット対応)。**インボイス登録番号**をTEL下に印字。**下部メッセージ**: 全レシート共通(ポイント設定/レジの両方から編集・app_settings)＋この会計だけの一言(レジ入力→sales_slips.receipt_noteに保存・再印字でも出る・確定後自動クリア)。印字順は一言→共通。カット前の紙送り3→6行で最終行の切れを解消。※メッセージが28桁を超えると自動折り返し(継続行も中央寄せのため、2行にしたい時は入力時に改行するときれい)。(4)**バックアップと異常検知**(正式運用の絶対条件): server/backup.py=オンラインバックアップAPI+integrity_check+世代管理(既定14)+店外複製(最大5か所・失敗しても店内は残す)。サーバー起動中1日1回自動+設定画面から手動。**件数の急減警告**(主要14テーブル・20件or1割減で誤削除を検知)。**データ点検**(server/integrity.py)=quick_check/参照切れ/空伝票/マイナス値/ポイント残高と履歴の一致/二重販売の6項目を日次+手動で実行し画面に表示。バックアップ.bat(タスクスケジューラ用)・docs/backup.md(運用・復元手順)。.gitignoreにdb/backups/追加(顧客データを含む複製をGitに載せない)。(5)**店舗情報の一元化**: server/store_info.py が唯一の正(店名・TEL・登録番号・振込先)。レシートはimport・帳票はwindow.TOKIWA_STORE注入で参照し二重管理を解消。単体プレビューは「※プレビュー用・印刷不可」の見本値。ロゴ読込失敗時は帳票ヘッダを店名文字に自動差し替え。nologo起動モード＋ロゴなしで起動テスト.bat(表示確認用)。動作確認: サーバー+Playwrightで24項目(ログイン・TOKIWA_STORE注入・請求書の登録番号/口座・バックアップ画面の状態取得/設定保存/今すぐ実行→店外ファイル生成/データ点検表示・共通メッセージ保存→レジのモーダルに反映・一言つき会計→伝票保存→レシートバイト列に登録番号/一言/共通メッセージ/ロゴ/6行送りの順で入ること・nologoで404/通常200・JSエラーなし)を確認 |
| v0.30.0 | バーコード運用の実データ対応＋一括要望13件。(1)**バーコード照合を品番(先頭5桁)に変更**: 実データの商品番号は5桁の品番のみで先頭10桁では当たらないため(10桁/13桁一致は保険で維持)。**全角スキャン対応**(かな入力モードのままでも `norm_code` で半角化して照合)。棚卸しで同じ品番の在庫が複数ある場合は「残り◯点」を表示し1点ずつ読めば順に消える。(2)**棚卸しのカーソル維持**: 画面表示時・クリック後・リセット後・エラー後にスキャン欄へ自動フォーカス、外れていても1文字目から流し込み(空撃ち防止)。(3)**レジ商品ピッカーの取り違え修正**: 追加ボタンが商品番号参照だったため重複品番で別商品が入るバグを行番号参照に修正。(4)**併用払いの内訳表示**: 日報明細に「支払」列(現金 ¥30,000 / クレジット ¥50,000 / 掛売 ¥20,000 形式)、売掛明細に「同時の支払」列(receivables.slip_id 新設)、顧客詳細の購入も同表示。移行データで支払が「—」だった行は 掛売区分→クレジット種別→現金 の順に補完(`pay_fallback`)。支払方法別の合計API(`payment_totals`)。(5)処方箋に**フレーム種類**(セル/メタル/ツーポ/ナイロール)。(6)宝石の仕入登録に**はめる指(左右5指・複数選択)とリングサイズ(フリー入力)**(products.ring_fingers/ring_size)。商品詳細・修正にも表示/編集。(7)処方箋の度数セレクトは**数字を打つとそのまま手入力に切替**。(8)**文字種の自動整形**: 数字欄19か所(han-num=全角→半角)・フリガナ(kana=ひらがな→カタカナ)。(9)購入履歴に**商品サムネイル**列。(10)**在庫品以外の購入明細の修正**(ダブルクリック→品名・商品情報。金額は不可・在庫品/取消済みはサーバーで拒否。/api/sale_line_update)。(11)顧客検索候補に**担当者**表示。(12)写真プールは**大きなプレビュー→「この写真を使う」の2段階**に(サムネイルも150pxに拡大)。(13)住所ラベルに**宛名の書き方**(本人のみ/ご家族様/家族を連名。連名は同居家族のみ・同姓は名前だけ)。あわせて診断3種(diag_product_growth/diag_stock_scan/diag_product_no)で実データを分析: 5桁採番は残り約77,581番・約2,246件/年で**約35年もつ**(6桁化不要)、在庫の重複品番は17組34件(98.7%は一意)。動作確認: サーバー+Playwrightで40項目(全角/半角バーコードスキャン・残り点数・確認済み・フォーカス維持/流し込み・ピッカー重複選択・併用払い会計→日報/売掛/購入履歴の内訳・明細修正・フレーム種類の保存/復元・手入力切替・採番20557〜継続・指/サイズの保存と詳細表示・修正フォーム復元・かな変換・全角→半角・検索候補の担当・ラベル連名/ご家族様・写真プール2段階UI)を確認 |
| v0.29.0 | **バーコード(EAN-13)照合**を実装。宝飾ナビのタグは「管理番号の先頭4区画(10桁)＋00＋チェックデジット」のEAN-13で、末尾区画はバーコードに入らないため、スキャン値の**先頭10桁**で商品を照合する(`_ean13_base`=13桁数字＋チェックデジット一致のみ採用・手入力番号は誤判定しない / `_resolve_code_conditions`=EAN-13は先頭10桁前方一致・それ以外は完全一致)。商品検索(在庫一覧・レジの商品ピッカー)と棚卸しスキャンに適用。スキャナは各検索/スキャン欄にそのまま打てるためクライアント改修は不要。あわせて店内共有(lan)モードの起動案内を実態(ロール制御。旧「他PCはレジ不可」は廃止済み)に修正し、**別端末アクセス手順(docs/lan-access.md)**を追加。動作確認: サーバー起動＋APIで実タグ `2097220126007`→`20972-2-01-26-083`・`0010610108003`→`00106-1-01-08-007`・棚卸しスキャン一致・手入力`R-5000`は完全一致・チェックデジット不一致の13桁は無視、lanモードで 0.0.0.0 バインドと案内文表示を確認 |
| v0.28.0 | 符丁・受託(催事)・操作マニュアルを追加。(1)**符丁(店内符牒)**: 仕入登録・商品修正に「符丁」欄を新設。仕入先マスタに登録した「符丁文字」(メーカー頭カナ・漢字名対策)＋**下代の上3桁**を『エビスアキナイカミ』でカナ化して自動生成(連続数字は先頭＋ﾀ1つ。例 下代30800→ｽｹｶ)。符丁は原価そのものを表すため**パート権限には非表示/未送信**。仕入先マスタ画面に符丁文字の列を追加。(products.fucho・supplier_master.fucho_head・/api/supplier_fucho)。(2)**受託(催事)Phase1(A方式)**: レジに「＋受託品(催事)」を新設。メーカー(仕入先)と金額だけで受託商品を即作成(state=受託・is_consignment=1・原価は納品書まで未確定)→会計すると通常どおり売上に。受託品は在庫数に混ざらない。番号を毎回作る運用は不要。後日納品書での原価入力・精算(粗利補正)はPhase2予定(products.is_consignment・/api/consignment_add・パートも可)。(3)**棚卸しに開始日時**を表示。(4)**操作マニュアル**(簡潔版/詳細版)をHTML＋PDFで追加(docs/manual-quick・manual-full、実画面スクリーンショット入り・大きめ文字・ゴシック・符丁の説明は含めない=パートが見る想定。scripts/build_manual.py で生成)。動作確認: サーバー+Playwrightで全画面のスクリーンショットを取得し、受託品の作成(/api/consignment_add)・棚卸しスキャンと開始日時の記録(started_at)・仕入登録の符丁欄表示・レジの受託品ボタン表示をUI/APIで確認 |
| v0.27.0 | 残タスク①〜④をまとめて実装。(①B-6 修理写真)修理お預かり品の写真をスマホ撮影/選択(複数可)でその場登録→伝票に紐付け。修理一覧にサムネ・📷件数、お預かり票の印刷にも写真掲載(repairs.photo_files・POST /api/repair_photo・配信は既存/product_imageを再利用)。(②家族の修正・削除)顧客詳細の家族行に修正/削除。リンク(B)行は続柄のみ・自由入力(A)行は全項目。add_familyが新規idを返し追加直後でも操作可(update_family/delete_family)。(③go-live準備)scripts/backup_db.py(sqlite3オンラインバックアップでWAL整合のコピーをdb/backups/へ)＋docs/go-live-checklist.md(取込→権限→定期バックアップ手順)。(④棚卸しモード)左メニューに「📋棚卸し」新設。商品番号を1点ずつ読み取り→在庫台帳と照合し、未確認に残った在庫を差異(紛失・盗難・登録漏れ疑い)として一覧・CSV出力・リセット。同番号在庫は繰返しスキャンで順に確認、売却済み番号は警告(stocktake_checks・scan/summary/reset)。パート権限では棚卸し・商品在庫画面を非表示＋API403。あわせて蓄積分: レジの未配線メモ欄を削除、会計担当を毎回リセット(既定なし・記憶しない)。動作確認: サーバー+PlaywrightでAPI20項目(家族修正削除/修理写真アップロード配信/棚卸しスキャン差異警告リセット/パート403)＋UI21項目(棚卸し画面のスキャン→差異更新・リセット/家族の追加修正削除/レジのメモ欄無し・会計担当既定未選択/パートのナビ非表示)を確認 |
| v0.26.0 | レジ周りの改善一式＋売掛の実務対応を確定。(1)**在庫品の二重販売を防止**: 同じ在庫品(同一商品キー)が明細に重複追加でき会計も通ってしまい在庫が-1しか減らないバグを、クライアント(追加ブロック)とサーバー(会計時に重複キー/売却済みを拒否)の二段で修正。(2)**売掛入金に支払方法**(現金/銀行振込/カード/その他)を追加。現金以外は残高だけ減らし日報の『入出金合計(現金締め)』には含めず『売掛入金(振込・カード等)』として別表示。入金履歴の区分に方法を併記。(3)**売掛を初期運用で空にする手段**: `scripts/clear_receivables.py`(バックアップ自動取得＋確認)と `import_csv.py --no-receivables`(取込で売掛を入れない=再取込でも復活しない)。手入力は既存の『＋売掛を追加』。(4)**レジ上部レイアウト刷新**: 会計担当を専用の帯に分離(折返しで崩れない)、顧客は未選択=検索＋大きい新規ボタン/選択後=情報カード(氏名・カナ・年齢・電話・最終購入・ランク・売掛残・**住所・担当**)に切替。(5)**レジ顧客検索の改善**: Enterで勝手に先頭を選ばず↑↓で選択・候補1件ならEnter確定・複数未選択は無反応(誤確定防止)、候補行に住所を追加(同姓同名の判別)。(6)**会計担当バーの並びを担当者番号順**に。(7)一覧→詳細の**ダブルクリック連打対策**(取得中ガードで多重fetch/多重描画を解消)。(8)詳細検索の**半角全角・大小文字の吸収**(サーバー正規化関数 normjp を共通化)。動作確認: サーバー+PlaywrightでAPI9項目(重複キー会計拒否/売却済み再販売拒否/正常販売でstate=売上/現金・振込入金の残高減とmethod保存)＋UI(重複追加ブロック/会計担当の専用帯/顧客カードの住所・担当/候補の住所/半角カナ検索/入金の支払方法と履歴表記)を確認。※あわせて再発防止としてCLAUDE.mdに『捏造禁止・提案ファースト・事前助言』を明文化 |
| v0.25.0 | 検索・分析に「詳細検索」タブを新設(B-4拡張)。顧客属性(氏名/担当/ランク/誕生月/性別/地区/最終購入/DM可)＋購入した商品の属性(分類/ブランド/地金/石種/仕入先/商品名/購入期間)＋メガネ処方箋(用途/レンズ・フレーム名/処方期間)を横断してANDで顧客を抽出。プルダウン候補はサーバーが重複除去して返す(/api/search_options)。購入商品グループは「同一商品で全条件」(既定＝ブランド・地金・石種などを同じ1点が満たす)と「別々の購入でも可」を切替可能。結果は住所ラベル印刷・CSV出力・顧客詳細への遷移に接続(サーバー: /api/detailed_search・EXISTSで取消明細を除外・上限2000件)。あわせて蓄積分の検証も実施: レジ明細に在庫品のサムネイル＋🔍で商品詳細(写真・原価はロール別)、在庫数タイルを会計確定/返品/在庫画面表示でリアルタイム更新(/api/stock_stats)、売掛明細を買上日の既定降順＋ヘッダで昇順降順切替、サイドバー左下の固定ダミーユーザー表示を削除。動作確認: サーバー+PlaywrightでAPI26項目(同一商品vs別購入・グループ横断AND・期間・処方箋・在庫サマリ)＋UI19項目(候補読込・各種絞り込み・同一/別購入トグル・ダブルクリック遷移・レジ🔍→商品詳細・売掛ソート・左下ダミー削除)を確認 |
| v0.24.0 | ログイン認証＋ロール別アクセス制御(④/access-control.md)を実装。サーバーに認証を追加し、未ログインは専用のログイン画面(ユーザー選択+パスワード)へ。パスワードは本人が初回ログイン時に設定(PBKDF2ハッシュ・app_users/app_sessionsテーブル・セッションはDB保存で再起動後も維持・60日で失効)。役割は管理者/社員/パートの3種: パートには仕入額(下代)・粗利をサーバーが**そもそも送らず**(起動データ・在庫検索・商品詳細API)、商品・在庫画面と設定画面への導線も非表示＋商品登録/修正/削除/写真/マスタ編集/ランク更新のAPIを403で拒否。担当者マスタとログインユーザー管理は管理者のみ。設定に「ログインユーザーと権限」画面を新設(追加・役割変更・有効/無効・パスワードリセット。最後の管理者の降格/無効化は禁止)。旧「店内共有モード」(PC単位のremote判定)は廃止しロール制御に一本化。埋め込みモックのログイン画面はサーバーログイン済みなら自動スキップ。あわせて蓄積分: 売掛管理メニュー(合計・誰がいくら一覧・CSV)、売掛の修正・削除、ランク更新の期間指定+最下位9据え置き(チェックで復活可)+ランク別人数分布。動作確認: サーバー+Playwrightで管理者/社員/パートのログイン・下代表示の出し分け・パートの画面遮断とAPI 403・ユーザー管理・ログアウト・最終管理者ガードを確認 |
| v0.23.0 | 在庫一覧・レジの商品ピッカーの既定並び順を「商品番号の降順」に変更(動作確認: 在庫一覧R-9000→R-5000で▼表示・ピッカーも降順をUIで確認)。あわせて直近の蓄積分を反映: レジ入出金(cash_movements。代引手数料・収入印紙・両替・経費等を±で記録→日報/ホームの入出金合計に反映)、既存客に売掛を手動追加(入金管理タブの「＋売掛を追加」)、顧客ランク更新をマスタ管理から独立＋ランク基準(金額区切り→ランク名)を画面で編集・保存(app_settings)、マスタ管理を1画面に集約(担当者/仕入先へのリンク＋項目検索＋各画面から戻るボタン)、宝飾誤登録の誤検知修正(取込のメガネ判定を全角/半角統一＋表示時にレンズ/メガネ等の名前は誤登録扱いしない補正)、処方箋の品名を購入履歴に反映(仮名→正式名)、掛売購入後の売掛残高を即時反映、購入履歴に「取消済みも表示」トグル(監査ログ・既定OFF・取消日つき)、仕入登録「宝石以外」で地金を非表示＋例文言を切替、レジ明細の区分列を削除。スモーク: ランク基準の保存/再読込、宝飾誤登録の補正(メガネ品→非誤登録/宝飾品→誤登録)、レジ入出金の記録、売掛追加をAPIで確認 |
| v0.22.0 | Bタスク第2弾＋検索改善。(B-1返品・訂正)売上明細に取消フラグ(voided)を追加し、売上を表示・集計する全クエリ(累計・売上履歴・日報・期間集計・購入順位・処方箋候補・入金)から取消済みを横断除外。顧客詳細の購入履歴に「取消」ボタン→取消すると履歴・集計から除外され在庫品は在庫に戻る(検証: 累計¥318,000→¥298,000、商品stateが売上→在庫、明細が消えるのを確認)。(B-5顧客ランク更新)設定に「顧客ランク更新」を新設。合計購入額の9段階基準(宝飾ナビ準拠)で全/宝飾/メガネ別にプレビュー→一括更新(集計対象外・取消は除外)。(B-8仕入登録見直し)「宝石/宝石以外」トグルで宝飾情報を出し分け、税区分を明確化(仕入=税別・上代=税込)、掛率を税込換算に修正(500税別/1100税込=50.0%を確認)、折数(上代の半額)を参考表示。(検索改善)処方箋検索の行クリックで顧客の「メガネ処方箋」タブを直接開き「←処方箋検索へ戻る」で結果一覧に戻れるように。赤い行=宝飾誤登録の凡例を明記。ランキングの行クリックも同様に遷移。顧客検索・DM抽出に「集計対象外(ななし等)を除く」を追加。動作確認: サーバー+Playwrightで返品の在庫戻し・集計反映、ランク更新プレビュー、仕入トグル/掛率/折数、処方箋クリック→処方箋タブ→戻る、集計対象外除外をUI経路で確認。※部分取消時の分割入金の按分戻しは未対応(伝票ごと返品なら入金も除外) |
| v0.21.0 | Bタスク第1弾(低リスク・依存なし)。(B-3)各種マスタ管理に「ブランド・石種・購入動機・地金」を追加。ブランド=products.brand、石種=center_stone、購入動機=sales_slips.motive に連動(使用件数表示・改名時のデータ追随つき)。地金は専用列が未整備のためスタンドアロンのリスト(Pt900/K18等をseed。商品に地金列を足せば連動に切替可能)。(B-9)売上伝票明細表に売上四分類(催事/店頭/メガネ/時計)の内訳タイル・分類列・CSV列を追加。分類はサーバー側で自動判定(優先: 催事=購入場所が店頭以外 → メガネ=メガネ商品 → 時計=分類に時計 → 店頭=残り)。目視集計をシステム化。(B-4)顧客一覧に「誕生月」の絞り込みを追加(1〜12月)。ランク・最終購入と組み合わせて誕生月DM・お声がけ対象を抽出→住所ラベル印刷へ。ランク検索は既存のランク絞り込みで対応済み。動作確認: サーバー+ブラウザでマスタ8種の表示・地金の追加/改名/削除・四分類タイル/列・誕生月抽出(6月=一致3件, 11月=0件)をUI経路で確認 |
| v0.20.0 | (1)レジの会計担当ワンタップバーに「レジ表示」フラグを新設(staff.is_register)。担当者マスタが宝飾ナビ由来の「Aランク：三輪」等の旧タグで汚染されていてもレジには実在の担当者だけを表示。担当者マスタ画面で表示ON/OFF切替、旧タグは「🧹ゴミ担当を一括整理」で一括削除/停止(未使用は削除・使用ありは停止+レジ非表示で購入履歴は保持)。(2)写真プール機能: スマホでまとめて撮影した商品写真を先にアップロードしておき、商品登録・商品詳細から「プールから選ぶ」でサムネイルをタップして紐づけ(既存のその場撮影機能はそのまま維持)。商品・在庫画面の「写真プール」からアップロード・不要写真の削除も可能。(3)B-7: 商品詳細から「修正する」「削除する」を追加。修正は在庫一覧項目を編集して即反映。削除は画像ファイルも一緒に削除し、販売履歴のある商品は削除できないようサーバー側で保護。動作確認: サーバー+ブラウザでレジ担当表示・担当者トグル・写真プールの一括アップロード/割当/削除・商品修正/削除(販売履歴ありの拒否含む)をUI経路で確認。原価(下代)の権限別非表示はログイン認証実装後の別対応 |
| v0.19.0 | マスタ管理を拡充。(1)各種マスタ管理(汎用エディタ): 設定・マスターから商品分類・保管場所・支払方法・顧客ランクを選んで項目の追加・名称変更・削除。削除時は「◯件のデータと繋がっています。削除しますか?」の警告(改名は実データにも追随)。(2)担当者マスタに使用件数(顧客/売上/アプローチ/処方箋/修理の合算)と削除を追加し、宝飾ナビ由来の不要担当者(ライオンズ役職・ランクタグ等)を整理できるように。(3)レジの担当を「会計担当(レジを打つ人)」のワンタップ選択に変更(担当者マスタ連動・選択保持・ログイン不要で交代打ちに対応・会計担当を伝票に記録)。顧客の担当とは別概念に整理。(4)顧客一覧にランク・最終購入(◯年以上購入なし/購入履歴なし)の絞り込みを追加し、住所ラベル印刷と組み合わせてDM抽出(例:VIPかつ3年以上購入なし)。動作確認済み(サーバー＋ブラウザ) |
| v0.18.0 | 担当者マスタ(最優先)を実画面連動に。設定・マスターの「担当者マスタ」から担当者の追加・名称変更・利用停止ができ、レジ・仕入登録・処方箋・修理・日報・顧客編集などの担当者プルダウンがすべてこのマスタから引かれるように(利用停止した担当者はプルダウンから除外)。レジの会計担当も選択した担当者で記録。担当者はstaffテーブル(取込済み)を使用し、無い名前は記録から自動取り込み。設定画面のマスター管理を「担当者マスタ」「仕入先マスタ」のカードに分割。動作確認: マスタの追加/停止→各プルダウン反映、レジ担当の記録をUI経路で確認 |
| v0.17.0 | 仕入先マスタ(分類フラグ)を新設。設定・マスターの「マスター管理」から仕入先の分類(宝石/メガネ/時計/その他)を割り当て(その場で保存)。在庫一覧に「仕入先ジャンル」の絞り込みを追加し、マスタで設定したジャンルで商品を絞り込めるように(実画面がマスタから情報を引く動き)。仕入先マスタは商品の仕入先から自動で取り込み(sync)。テーブルは既存DBでも自動作成。動作確認: マスタ割り当て→保存→在庫一覧のジャンル絞り込み反映までUI経路で確認 |
| v0.16.0 | 在庫一覧に「仕入先」「下代(仕入単価)」の列を追加。列ヘッダのクリックで昇順/降順の並び替え(サーバー側でページングと整合。商品番号・商品名・分類・上代・下代・仕入先・保管場所・状態が対象、▲▼で表示)。商品詳細にも下代・仕入先を表示。※下代は原価情報のため、将来のログイン権限(パート等)では非表示にする予定(access-control) |
| v0.15.0 | 仕入商品登録の画面から写真を選んで(スマホは撮影して)登録できるように。登録ボタンで商品を作成→採番された商品に写真を自動で紐付け(ブラウザ側でリサイズ→保存)。顧客詳細の購入履歴で、在庫商品の行の商品名を**ダブルクリックすると商品情報(写真つき)を表示**(購入履歴に商品番号を持たせ、サーバー検索で引き当て)。詳細画面からその商品の写真登録/変更も可能。動作確認: 仕入登録→写真保存→在庫反映、購入履歴のダブルクリック→商品詳細表示をUI経路で確認 |
| v0.14.0 | 商品写真(B-7 第2段): 商品詳細から写真を撮影/選択して登録できるように。スマホは背面カメラ起動(capture)。ブラウザ側で縦横比を保ったまま長辺1200pxに縮小しJPEG圧縮(切り抜きなし・宝飾品を切らない・容量削減)してからアップロード。サーバーが data/real/images/ に保存し商品に紐付け(POST /api/product_image)。登録後は一覧サムネイル・詳細写真に即反映。新規仕入商品は登録後に商品詳細から写真を付ける運用。動作確認: 画像投入→リサイズ→保存→DB更新→表示までUI経路で確認 |
| v0.13.1 | 修正: 商品写真が表示されない不具合。UIが画像URLを /api/product_image と組み立てていたが、配信ルートは /product_image のため404になっていた。UIの実経路(商品詳細を開く→画像読込)で写真が実際に表示されることを確認 |
| v0.13.0 | 見積・請求を左メニューに独立(タブで見積書/請求書を切替・押し間違え防止、様/御中の敬称選択、適格請求書の登録番号と振込先口座を請求書に自動印字、発行履歴に保存して呼び出し→編集→再発行)。商品写真(B-7 第1段): 在庫一覧に写真サムネイル・行クリックで写真つき商品詳細・レジの商品ピッカーにもサムネイル(画像はdata/real/images/から配信、サブフォルダ・大文字小文字も対応)。重大バグ修正: レジで在庫商品を会計しても在庫状態が変わらず二重売りできてしまう&購入履歴の商品名が空欄になる問題(内部キーの取り違え)を修正。仕入先での絞り込み(在庫一覧・レジ)、修理の進捗をドロップダウン+更新ボタンに(前の状態に戻せる)、日報/ホーム/在庫/修理は画面を開くたび自動最新化、住所ラベルの宛名をセンター寄せ。堅牢化: 起動時に不足列を自動補完(pull後のmigrate忘れで在庫0件になるのを防止)。※起動データ量の遅延読み込み化はv0.12.0で対応済み |
| v0.12.0 | 起動時のデータ量を大幅削減(遅延読み込み化)。実データで初期表示データが約60MBに達しログイン直後に10秒ほど固まる問題を解消するため、起動時は顧客一覧＋小さな集計だけを送り(約5MB)、重い明細は必要時に取得する構成に変更。顧客詳細(売上・処方箋・処方箋候補・ポイント履歴・アプローチ)は開いた時にサーバーから取得(/api/customer_detail)、商品(在庫21万件)はサーバー側検索＋ページング(/api/products, /api/product_categories)、日報・売上集計はサーバー集計(/api/daily_sales, /api/slip_lines)に変更。単体HTML(サーバー無し)で開いた場合は従来どおり埋め込みデータで動作。クラウド化(④)にも必須の土台。※商品名検索のカナ正規化は当面サーバー側では簡易(品番検索は従来どおり) |
| v0.11.8 | 顧客一覧の住所を電話の隣へ移動+住所もソート対象に、顧客詳細に携帯電話・eメール・メモ欄を表示、顧客編集に携帯電話入力と自由メモ欄を追加(DB保存)、モーダルの背景クリック誤クローズ(入力欄でドラッグして枠外で離すと閉じる)を修正、在庫画面に在庫サマリタイル(在庫数・上代合計・下代合計・粗利率)を追加。在庫の権限制御はログイン認証(④)で対応予定 |
| v0.11.7 | 顧客一覧の顧客ID列を住所(住所1+住所2)に差し替え(IDでの検索は維持)、担当列もソート対象に追加、顧客詳細の生年月日を和暦/西暦で切替表示(結婚記念日・家族の生年月日も追随)、全帳票・住所ラベルの印刷前に画面内プレビューを挟み確認後に印刷/キャンセルできるように |
| v0.11.6 | 売掛入金をレジ画面からも受けられるように(現金授受→ドロワーがレジで完結)。売掛残高のある顧客をレジで選ぶと入金パネルを表示し、その場で入金→残高即時更新→DB保存 |
| v0.11.5 | 日付が変わったら自動でページを再読込するよう修正(PCを閉じずに一晩置いても翌朝は最新の日付・売上で表示。会計中は再読込しない安全策つき)。売掛入金を記録する機能を新設(顧客詳細の入金管理タブに「入金する」ボタン、/api/receivable_payment) |
| v0.11.4 | 重大バグ修正: 今日の日付が全箇所で2026-07-11に固定されていたのをシステム日付から動的計算するよう修正(日報デフォルト日・年齢計算・処方箋/修理の日付初期値に影響)。「宝飾品が誤登録」の誤検知を修正(判定を商品分類ベースに一本化、PT/SV等の短い文字列一致による誤検知を解消)。家族情報から該当顧客への遷移リンク、顧客一覧の列ソート・全選択/クリア・行はダブルクリックで詳細を開く仕様に変更、レジの顧客名からID表示を削除、「種類未定(後日入力)」を廃止 |
| v0.11.3 | 店舗ロゴ画像を帳票(見積書・請求書・修理伝票)ヘッダに組込。サーバーに /assets/* 静的配信ルートを追加 |
| v0.11.2 | 実データ確認の修正(続き): 帳票発行元を正式店名「宝石・メガネ・時計 ヤナセ」に変更、帳票・住所ラベルの白紙2ページ目を修正、保存失敗メッセージをサーバー未起動と分かる文言に改善、レジの入力内容が画面移動後も保持される旨のトースト案内、家族続柄のコード→名前変換 |
| v0.11.1 | 実データ確認の修正: ヘッダー日付をシステム日付に、レジの購入動機・購入場所を非表示、顧客詳細ポイントの区分列削除+残高/日付補完、処方箋のレンズ/フレーム名解決、性別対応の修正(1=女/2=男)、商品状態・支払方法・処方箋用途のコード→名前変換(在庫一覧が機能するように) |
| v0.11.0 | 住所ラベル印刷(LDW24UC/A4 24面、開始位置指定対応)、日報・売上集計を実データ連携化(ホームタイル/日報照会/売上伝票明細表+CSV出力)、修理伝票(宝飾ナビに無い新機能。預かり中→修理中→連絡済み→引渡済みの進捗管理、ホームの引渡予定と連動)、見積書・請求書(保存はせずその場で印刷)を追加。レジは支払方法を複数に分割可能に(現金+クレジット等、掛売を内訳に混ぜると自動で売掛起票)、過去の日付での登録に対応(店外イベントの後日精算など。未来日は不可、入力日時は別途自動記録) |
| v0.10.1 | 内部最適化: 埋め込みフォールバックデータをdemo5,204件→サンプル100件+ペルソナに軽量化し tokiwa-ui.html を1.8MB→約0.23MBに削減(サーバー経由の本番運用は無変化、Python無しプレビューはサンプルで描画)。生成を embed_fallback.py に一本化(build-demo-data.py 廃止) |
| v0.10.0 | 店内共有モード: 同一LANの他PCからブラウザで閲覧・顧客登録・商品登録が可能(店内共有で起動.bat)。他PCではレジを非表示+サーバー側でも会計を拒否。仕入商品登録フォームを実装(自動採番・掛率自動計算・DB保存)。SQLiteをWAL化し複数PC同時アクセスに対応 |
| v0.9.5 | 処方箋の「PDを左右別で入力する」トグルを復活(v0.9.4で誤って削除)。オンで左右別のPD遠/近入力欄を表示し両眼欄を隠す。「(イレギュラー)」表記のみ削除 |
| v0.9.4 | 処方箋レイアウト整理: レンズ名・金額・フレーム名・金額を1行に(金額は短く・読取専用)、合計は別。PD・視力を横一列のプルダウンに(PD遠/近=50〜70+手入力、裸眼/矯正は右・左・両を規定リストから選択)。「(両眼)」表記を廃止 |
| v0.9.3 | 処方箋の金額をレンズ・フレーム・合計(自動計算)に分割。処方箋を後から編集できる「この処方箋を編集」を追加。レジで現金以外(クレジット/PayPay/掛売/分割)を選ぶと預り金額へ請求額を自動セット |
| v0.9.2 | 処方箋: フレーム購入はフレーム名欄へ取り込み(分類でフレーム/レンズ判別)、全度数プルダウンに手入力を追加、右→左コピー・度数クリアのボタン、度数欄の幅を拡大 |
| v0.9.1 | 処方箋のSPH・CYLを「符号(＋/ー)」と「数字」の2プルダウンに分割し選択を軽減。プリズムの斜め(2方向: 例 0.25 UP + 1.0 OUT)入力に対応 |
| v0.9.0 | 処方箋を全面刷新: 度数をプルダウン入力(SPH ±0.25刻み15.00/CYLマイナス基本5.00/AX 5刻み180/PRI・BAS追加/ADD)+SPH・CYLは手入力可、PDは両眼値が基本で左右別も入力可、裸眼・矯正視力を追加、購入データとの紐付け(未紐付けの購入を選択) |
| v0.8.0 | 和暦入力(生年月日=明治〜令和/結婚記念日=昭和〜令和)、郵便番号+住所2段化と住所検索、必須項目を名前・電話・担当者に変更、全角数字の自動半角化、番号なし追加のピッカー化(電池交換/レンズ/宝石修理/メガネ修理/時計修理/手入力)、処方箋の入力画面を新設、会計後に購入履歴へ即時反映されないバグ修正、バージョン表示 |
| v0.7.x | レジ実用POS化(空明細から組立・商品ピッカー・レジから新規顧客)。入力エラーのインライン表示、同姓同名識別(年齢・最終購入日)、担当者名を田中一槻へ変更 |
| v0.6.x | 検証用ペルソナ(メガネ太郎・ポイント花子など7名)とテスト顧客ボタン |
| v0.5.x | 顧客検索の正規化(全角カナあいまい)、新規/編集モーダル、顧客管理の初期非表示 |
| v0.4.x | SQLite実装(スキーマ・移行スクリプト)、ローカルAPIサーバー、会計のDB永続化 |
| v0.3.x | デモデータ組込(顧客5,204件)、処方箋の宝飾品混入の振り分け |
| v0.2.x | お声がけチェック、処方箋一覧+詳細展開、レジ改善(種類未定レンズ・金額編集) |
| v0.1.x | 初版画面モック(ログイン・ホーム・レジ・顧客・在庫・日報) |
